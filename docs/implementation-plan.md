# Prompt-DJ — Implementation Plan

_How far along we are, what's in flight, what's left, and the drift log. Living document — updated at each milestone and at `/zuko:handoff`._

## Status: **LATEST 2026-08-08 (NEW FEATURE — internal Developer / Ops dashboard; branch `feat/dev-ops-dashboard`, stacked on `feat/randomized-rule-assignment`, NOT merged): DONE + verified.** A read-only team control room over a new tiny event log — the first real use of the long-planned SQLite store. **Built:** `app/events.py` (a `data/events.db` SQLite log, one row per mix/set outcome — songs, rule/label, take, device `user_id`, status, `health` red/amber/green, fail reason, the `anomaly.py` records, tempo/key facts; non-fatal by construction; caller-supplied `data_dir` → hermetic under the test monkeypatch); recording hooks in `_run_mix` + `_run_set` (both now carry an optional `user_id`/`via`, recorded only — the cache id + audio are untouched; `start_mix`/`start_set` thread `req.user_id` in); `routes/admin.py` (read-only `GET /admin/summary|events|devices`, bounded pagination, behind an optional `PROMPTDJ_DASHBOARD_TOKEN` shared-token gate — open on localhost, required once deployed); `library.song_names()` for readable labels; web `AdminScreen` reached by a `#dev` hash gate in `main.tsx` (bypasses the 4-step flow; no router added) with the health strip, a newest-first paged feed (red=broke / amber=degraded-but-played with the engine's own "what to do" note / green=clean), a ▶ play button per finished mix, the **rule/style shown per mix** (founder ask — team sees it, user doesn't), and a **"By device" rollup** (a device, not a person — no login yet). **Retention ADDED (2026-08-08, founder ask):** honest device-level retention — `events.retention()` + `/admin/retention`, `devices()` extended with `first_at`/`last_at`/`active_days`; the web "By device" view shows a "returning vs new today" strip + a "returning · N days" badge + first→last dates, loaded non-fatally (a retention failure never breaks the core dashboard). Counts persistent browsers, not verified people — directional until login. Backend tests: +3 events, +2 admin-route (the dedicated frontend assertion is the one remaining item, pending the founder-approved `.test.tsx` edit). **Deferred by design (still — cheap to add later):** login/usernames, true per-person retention & cohorts, behaviour funnels. **Security flagged:** exposes user song choices, so it MUST get the token before any internet-reachable deploy. **Verification: full backend 614 passed (169s); web 63 passed; typecheck + lint clean; verified live end-to-end** (own backend on an alt port + seeded demo rows → dashboard rendered the feed, expand-anomaly, play-disabled-on-failure, and the by-device view, zero console errors; demo `events.db` deleted after). **No render/validate/storage/config file touched** (the one dangerous-surface touch was the new `AdminScreen.test.tsx`, added via the founder-approved `.zuko/approve.js` flow; approval cleared after landing). DRIFT NOTE: per the product-manager pass, the founder's "everything user-by-user + retention + login" ask was scoped down to a mix-event stream with a device view — the failure-visibility job an MVP dashboard can actually do — with retention/login deferred until there's a login; captured here so the reduced scope is intentional, not drift. _(Below: the 2026-08-07 fix #3 status.)_

## Status: **LATEST 2026-08-07 (fix #3, same branch): auto-silence rule REMOVED — beats sing by default; only a hand-picked list is music-only.** The fix #2 auto-detect ("silence any beat reading ≥90% vocal") was WRONG: the vocal separator mis-hears instrumental bleed/pads as singing, so a beat with real vocals ('I Adore You', reads ~99%) got silenced along with a genuinely-instrumental one (Rapture). Removed: `instrumental_beats.is_instrumental_only(a1)` now checks ONLY the hand-picked `INSTRUMENTAL_ONLY_BEATS` list — never a coverage number. **Rapture (Black Coffee) added to the list** (founder's ear: it has no real lyrics; its whole-song vocal read is a false separator read) — so Rapture × Uff Teri Ada still builds (music-only) AND 'I Adore You' sings again. `vocal_coverage()` kept but now feeds ONLY the backend anomaly (`suspicious_beat_vocal`, reworded: "likely a false read — mark music-only or re-scan"; never auto-acts). Reproduced + guarded by `test_instrumental_beats.py::test_an_unlisted_beat_sings_even_if_its_analysis_reads_whole_song_vocal`. Verified on real songs: I Adore You s1_vocal_regions has 4 gaps + validate PASSES; Rapture s1_vocal_regions [] + PASSES. `render.py`/`validate.py`/`models.py` untouched. **Full backend 591 passed.** PROCESS NOTE: the removed auto-rule was added by the agent this session WITHOUT founder sign-off — a silencing rule must be a human/ear decision, not an automatic guess; corrected. _(Below: fix #2 — which introduced the now-removed auto-detect.)_

## Status: **LATEST 2026-08-07 (fix #2, same branch): R1 "vocals overlap" skip fixed + set cap language unified to 5.** Root cause of the founder's "Rapture × Uff Teri Ada skipped (R1)" report: Rapture's analysis is a SINGLE vocal region spanning the whole 8-min track (a degenerate/vocal-song read), so the "keep Song 1's vocal for contrast" feature wove huge Rapture passages that overlapped Song 2 → the R1 quality guard correctly rejected it. NOT a tempo issue (Rapture 120 × Uff 133 = ~11% apart). Fix: `instrumental_beats.is_instrumental_only(a1)` now AUTO-DETECTS a beat whose own vocal covers ≥90% of the track and treats it instrumental-only (no contrast vocal woven) — general, so any future beat with a degenerate/vocal-song read is handled without hand-listing. Signature changed `song_id`→analysis (one caller, `plan.py`; the Merrygo test updated). Verified on the REAL pair (`s1_vocal_regions: []`, validate PASSES) + `tests/test_instrumental_beats.py` (5) + an anomaly report (`instrumental_beat_auto`). `render.py`/`validate.py`/`models.py` untouched. Also: **set cap unified to 5** across the docs (functional spec, CLAUDE.md non-goals, this plan) — the core rule is now `MAX_MIXES_PER_SET = 5`, not 2. **Full backend 591 passed.** _(Below: fix #1 — never-decline in sets + anomaly reporting.)_

## Status: **LATEST 2026-08-07 (fix, same branch `feat/randomized-rule-assignment`): (1) NEVER-DECLINE now applies in SETS; (2) BACKEND ANOMALY REPORTING.** **(1)** Root cause of the founder's "Set 3 skipped" report: `routes/set.py`'s mixability gate called `fence.arrangement_options(a1, a2)` WITHOUT `force_tempo`, so a set still used the old "too far apart in tempo" decline while a single mix (`plan.py`, `force_tempo=_FORCE_TEMPO_ENABLED`) forced the same pair. The set gate now passes `force_tempo=force_tempo_enabled()` — the two production callers are consistent, and the ONLY mixability decline left anywhere is a no-beat track. Reproduced + pinned: `tests/test_set_route.py::test_set_NEVER_declines_a_far_tempo_pair_it_forces_it_onto_the_beat` (122×95 forced, kept). The old test that asserted the buggy "drop for tempo" behaviour was corrected to the never-decline behaviour (not weakened — it encoded the bug). **(2)** New `app/planner/anomaly.py` (pure `scan()` + `format_line()`): the mix pipeline still generates from imperfect inputs but now logs a structured "what was off + what to do" per condition (forced stretch, low-confidence grid, key measured from audio). Wired into `_run_mix`; reporting only, never changes a mix; `tests/test_anomaly.py` (5). **`render.py`/`validate.py`/`models.py` untouched.** **Verification: full backend 585 passed.** DRIFT NOTE: the never-decline base rule (2026-08-07) was merged as applying to "every mix", but the set builder's own gate was never updated — a real divergence between the intended rule and the code, now closed and guarded by a test. _(Below: the AUTOMATIC RULE ASSIGNMENT work on this same branch.)_

## Status: **LATEST 2026-08-07 (branch `feat/randomized-rule-assignment`, NOT merged — pending founder review): AUTOMATIC RULE ASSIGNMENT.** The three manual rule buttons are removed; rule choice is auto-assigned by a deterministic shuffler (`app/planner/rule_shuffle.py`) with ONE shared core (`rule_at_from_seed(seed, position)`) and two callers differing only in seed: the single-pair re-roll (seed `user+song1+song2`, position = the Nth take — cap raised **2→5** via `MAX_GENERATIONS_PER_SESSION` in `types.ts`) and the SET (seed `user+set_index`, position = the mix's index — set cap raised **2→5** via `MAX_MIXES_PER_SET` in both `routes/set.py` and `types.ts`). Properties, all pinned by tests: every aligned group of 3 is balanced; no style repeats back-to-back anywhere (incl. group + cycle seams); different users differ; a user's consecutive sets differ (perturb guard); pure/deterministic (portable sha256 seed → cache-safe). The assigned rule feeds the EXISTING cache-id formulas (`mix_id_for`/`set_id_for`) unchanged; **`render.py`/`validate.py`/`models.py` untouched.** Each result shows its style label; the "only 3 styles" ceiling stays a silent code contract (founder call). **Verification: full backend 580 passed (149s); web 52 passed; typecheck + lint clean; Setup screen browser-checked (rule buttons gone, catalog loads, no console errors).** Two protected test files (`App.test.tsx`, `SetupScreen.test.tsx`) edited under the founder-approved `.zuko` flow (additive tests + the cap-at-5 update; approvals recorded, then cleared after landing). NOTE a pre-existing flaky `test_cache_sweep.py` (order/timing-dependent on Windows; git-confirmed untouched by this change) for a future harden. Nothing committed/merged. _(Prior status, on `main`:)_ **NEW BASE RULES live for Rule 1/3/4 — never-decline tempo auto-match + empirical (measured-from-audio) key match, ON TOP of the now-merged four-rule product.** No pair is ever refused: a far-apart vocal is stretched fully onto the beat and per-bar beat-locked (`fence.legal_options(force_tempo)`, `plan._FORCE_TEMPO_ENABLED=True`, `MixPlan.tempo_forced`); when a key label is untrusted the shift is measured from the audio (`app/audio/chroma.py`, AutoMashUpper, ±3 formant). Referee (`validate.py`) widened ONLY for `tempo_forced` plans (all other guards intact) — founder-approved + applied via the approval flow; adversarially reviewed. Chop/Echo balance = beat-full + vocal-subtle (`workers/rule3.py`). Beat-sensor health logged (`planner/beatgrid.py`). **Verification: full backend suite 554 passed; Rapture × Jugni (previously refused) renders end-to-end live.** Sole remaining decline: a track with no beat grid. Parked: Rule-4 echo balance (needs `render.py` + ear) and a deeper beat-detector rebuild. _(Prior status, now also on `main`:)_ **The FOUR-RULE product** — Rule 3 (chop & repeat) promoted from the throwaway harness into real engine code (`app/planner/rule3.py` + `workers/rule3.py`, reusing the guarded `render.py` by import so it's UNTOUCHED), wired into the mix route behind a per-mix `rule` field; Rule 4 gated per-rule (dry default, echo on rule 4); best-parts + set transitions made COMMON to every rule; a per-song rule picker in the set builder (Simple / Chop & repeat / Echo). **Foundation (BPM stretch + key match) runs first for every rule; only the best-parts highlight plays, never the full song, in every rule.** Rule 2 reserved. **Verification: full backend suite 536 passed (176s); web 49 passed + typecheck clean; a real mixed-rule set (Rule 3 → Rule 4) rendered & joined (362s, seam at 173s).** NO guarded files edited. See the drift log's 2026-08-06/07 entries + [RULEBOOK.md](RULEBOOK.md). Ready for the founder's final product testing (app runs locally: API :8000, web :5173). _(Baseline below:)_ **M1–M6-ish complete and LIVE on `main` (@ `05e2a8e`) — the 2026-07-15/16 catalog-expansion AND set-transition batches are MERGED and pushed (catalog audio stays LOCAL-ONLY; both merged with no PR at the founder's explicit request — see the handoff's process note). 427 backend + 49 web + typecheck + lint green.** The catalog now sits in **two tempo camps**: the fast camp (I Adore You, Father Ocean, Innerbloom, Rapture, Anchor Point) renders at 120–127, the slow camp (Merrygo alone) at 85–92. **A set is never refused for tempo:** within a camp the seam BLENDS as always, across camps it CUTS like a real DJ. The shipped app: pick from a curated catalog → a DJ-style mix; stack up to 2 into a beat-matched set; the vocal chain is ON; and BEST-PARTS is the DEFAULT — every mix/set is auto-cropped to its ~3-min highlight (build-up → drop/breakdown → wind-down) chained on the beat. Live steering was retired; the Play screen just plays/exports the finished mix or set. **The catalog grew this session to 6 beats + 12 vocals (18 entries, local-only):** +7 Bollywood/Punjabi vocals and a Drum & Bass "bridge" beat ("Merrygo") that lets the slow vocals (80–103 BPM) blend where the house beats (120–122) can't. Two new per-song planner overrides shipped for Merrygo (both dormant for every other song): **instrumental-only beats** (a beat that is really a vocal song contributes music only) and **hand-marked main drops** (the founder marks the drop by ear when the energy is too flat to auto-detect). **420 backend + 49 web green; web typecheck clean; `render.py`/`validate.py` UNTOUCHED.** Remaining before the ~50-user test: loudness master + short-clip export polish, the `storage.py` cache-eviction sweep (now more urgent — the disk filled twice this session), and the R1 hand-off pre-launch items. _(Older milestone detail below is kept as history.)_**

## _(historical)_ **M1–M5 complete and founder live-confirmed — both V1 features DONE (offline DJ mix + live steering). 188 tests passing (162 backend + 26 web).** M4 = the full arrangement (vocal weaves in/out, beat-breath), Regenerate, the arrangement screen (Slice A) + contrast (both songs trade vocals), one subtle filter sweep, confidence fallbacks (Slice B, reviewed SAFE), the **energy-arc** (Slice C: vocal spreads across the WHOLE song with a strong finish) and **per-bar beat-lock** (Slice D: the vocal no longer drifts — 315 ms → 0 ms on the real 7:52 pair, reviewed SAFE). **Founder live-confirmed the full M4 "perfect" on 2026-07-06 → the offline-mix feature (V1 Feature 1) is DONE.** Next: the pre-user-test housekeeping (~~one-click "Studying your songs" screen~~ **done 2026-07-06**; the cache-eviction sweep for mix WAVs + live-vocal + suggestion caches, in `storage.py`, gated), ~~the "beat up" move~~ **done**, the live-vocal beat-lock polish, ~~the UI design pass (founder's designs)~~ **done 2026-07-06 (Electric Violet 4-screen redesign + AI mix names)**, then **M6** (loudness master + short-clip export) and the ~50-creator validation test.

Demand is founder-validated, so we skipped the hand-made validation gate (former "M0"); the first real proof point is **M3** — the first genuinely good mix.

**M2 was split into M2a (stem separation) and M2b (analysis), stems first.** The heavy audio-AI runs in the cloud (Replicate), because PyTorch cannot run on the founder's Windows-ARM machine (proven — see [cloud-and-cost-plan.md](cloud-and-cost-plan.md)).

## Milestones

| #   | Milestone           | Goal                                                                                                                        | Acceptance                                                                   | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | ------------------- | --------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M1  | Skeleton            | Two-file upload → normalized WAV, stored; app runs end to end                                                               | Upload two songs, get them back re-encoded                                   | ✅ Done                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| M2a | Stem splitting      | Split each song into vocals/drums/bass/other (Replicate Demucs), cached; play each part in the app                          | Isolated vocal intelligible; cached so each song splits once                 | ✅ Done                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| M2b | Analysis            | `TrackAnalysis` (BPM/beat/downbeats/key/sections/energy/vocal-regions) with per-field confidence, cached; overlays in UI    | BPM ±1 and downbeats on the one for demo pairs                               | ✅ Done                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| M3  | Basic mix           | Song 1 bed + Song 2 vocal placed on the drop, tempo-locked, phrase-aligned; export WAV                                      | Drift-free, on-phrase, click-free, single vocal — **the first "whoa"**       | ✅ Done (live-confirmed)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| M4  | Full DJ arrangement | Judgment layer: varied placement, beat-drop breaths, keep-S1-vocal, ≥2 placements, FX, confidence fallbacks, **regenerate** | Success criteria S1–S4 on demo set; regenerate yields a different valid plan | ✅ **Done (live-confirmed 2026-07-06)** — Slice A (arrangement + Regenerate) + Slice B (contrast+FX+fallbacks) + Slice C (energy arc) + Slice D (per-bar beat-lock, 315 ms → 0 ms); all reviewed SAFE + verified; founder confirmed "perfect"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| M5  | Lean live control   | Stem-bus player + on-beat scheduling; the lean command set; orchestration; out-of-scope decline                             | Every command lands on the beat, no artifact/stall                           | ✅ **Done — founder live-confirmed 2026-07-06 (V1 Feature 2 DONE).** Slice 1: live stem player + bass on-beat + decline. Slice 2: full live mix — arranged-vocal bus + all four parts (Beat/Bass/Melody/Vocals) tappable/typed on the beat + "drop everything but the beat". Slice 3: AI smart-suggestion chips (per-section, closed vocabulary, cached, fallback-safe) that change with the playhead + "fade away". Plus ear-test fixes: AI actually turned on (was silently falling back to rules), live Vocals now Song 2's continuous vocal, chip-merge + now-playing indicator. **Deferred polish (→ M6):** the continuous live vocal uses a single global stretch so it drifts slightly off the beat over minutes — beat-locking it bar-by-bar (like the Download) is a focused M6 item. "beat up" energy move — **done 2026-07-06** (the beat-takes-over duck; live command set now complete) |
| M6  | Polish + share      | Loudness/limiter; short-clip export; the ~50-user test                                                                      | Clean master; ~50 creators feel the magic                                    | ☐                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

## Deltas from the original PRD (decided during discovery, 2026-07-05)

- One target user (casual creators), not three.
- Feature 2 leaner + Regenerate promoted to first-class.
- Short-clip (15–30s) export added as a hero output.
- Live BPM change explicitly deferred to V2.
- Right-sized stack: SQLite + local storage + in-process jobs for validation (audio toolchain unchanged).
- Time-stretch: SoundTouch (free) by choice, swappable to Rubber Band.

## Curated demo pairs (to assemble before/with M3)

~10 pairs: mostly _compatible_ (close BPM, Camelot-adjacent, clear structure) to showcase quality; 2–3 _hard_ pairs to exercise fallbacks. Hand-verify grid/key/sections. Include a couple of Indian pairs, hand-checked (weaker analysis there).

## Drift log

_(record any place the docs and code diverge, and how it was resolved)_

- 2026-08-07 (goodnight, branch `feat/auto-match-keymatch`, NOT merged) — **NEW BASE RULES for Rule 1/3/4: empirical key-match + never-decline tempo auto-match, from the founder's research brief (AutoMashUpper / fuzzy keymixing).** Root cause of "Rapture × Jugni sounds like two songs" = a **key clash** the app refused to fix because BOTH songs' key labels are flagged/low-confidence (`keys.KEY_UNTRUSTED_SONG_IDS` lists Rapture; Jugni conf 0.55). Fix = measure, don't guess. **APPLIED (non-dangerous, this branch):** (1) `app/audio/chroma.py` — added the empirical matcher (`rank_shifts`/`best_shift`/`empirical_shift`), AutoMashUpper cosine over beat-sync chroma, ±3 formant cap, **reusing** the existing `chroma()` primitive; wired into `routes/mix.py` as the fallback when `resolve_key_shift` returns a "key-skip" (untrusted labels) → measured shift applied formant-preserved. For Rapture × Jugni it measures **+3 st → 9A (E-minor, Rapture's exact key)**, cos 0.907 vs 0.855 unshifted (supersedes the −2 perfect-fifth first sent by ear). (2) `planner/fence.py` — `legal_options(..., force_tempo=)` **never declines a far-apart pair**: beat stays master (native), vocal stretched fully onto it (`tempo_forced`), per-bar beat-locked with a widened grip (`WARP_*_FORCED`); `_fold_source` now folds across ALL octaves so the forced stretch is provably in `[FORCE_STRETCH_LO, FORCE_STRETCH_HI]` for any pair (fixes the >2× "disguised decline"). (3) `planner/plan.py` flag `_FORCE_TEMPO_ENABLED` (**default OFF** — tonight's applied state still declines far-apart, unchanged; the flag flips WITH the staged validator at the founder's morning approval), `_attach_warp(forced=)`, stamps `MixPlan.tempo_forced`; `force_tempo_enabled()` folded into `ENGINE_VERSION` (`m11rule`→`m12match`). (4) `workers/rule3.py` — Chop balance = **beat full + vocal subtle** (founder: Chop & Echo, not Simple): `_DUCK` −3→−1 dB, `_CHOP_GAIN` 1.10→0.90, `_WET` 0.40→0.55; ear-confirm in the app. (5) `planner/beatgrid.py` (NEW) — beat-sensor health check (downbeat regularity + BPM/grid agreement), logged per mix so a mis-detected grid is visible. **STAGED for the founder's morning tap (dangerous file, NOT applied):** `planner/validate.py` — widen the B3 stretch band + R7 warp band **only for a `tempo_forced` plan** (all other guards + the on-beat boundary check unchanged), plus a forced single-segment drift guard (added after the adversarial review). **Proven:** forced pipeline renders a valid quality-passing mix on Rule 1/3/4 for Rapture × Jugni (tempo_forced, master 120, stretch 1.26, +3 key); full suite **553 green** (536 + 17 new: chroma/beatgrid/forced-tempo). **Adversarial review verdict `not-proven-safe`** (correct — it's half a feature gated OFF): the staged file is inert until the flag flips; findings #3 (octave-fold bound) and #4 (engine-version) FIXED, #2 (tests) ADDED, #1 (single-segment drift) guarded. **Rule-4 (Echo) balance PARKED** — needs `render.py` (dangerous) + founder ear-tuning, do attended. **Deeper beat-DETECTION rebuild PARKED** — a project of its own (founder chose "harden + verify" tonight).

- 2026-07-05 — Repo bootstrapped from discovery. No code yet; specs are intended design, not as-built.
- 2026-07-05 — **M1 complete.** Minor deviations from the plan, all harmless: `uvicorn` (not `uvicorn[standard]`) because `httptools` has no prebuilt wheel for Windows/ARM64; pytest config lives in `pyproject.toml` (not a guarded `pytest.ini`); corrected the danger-glob paths to the real `services/api/**` layout so the upload handler is actually guarded; two security hardenings from the adversarial review folded in (FFmpeg timeout; streaming upload size-cap). No DB/queue in M1 (as planned). Live HTTP round-trip verified: mp3+wav in → 44.1k stereo WAV out.
- 2026-07-05 — **Follow-up for M2+:** CI's coverage job expects `coverage/coverage-summary.json` at repo root, but web coverage writes to `apps/web/coverage/`; and CI has no Python (pytest) job yet. Wire both when a GitHub remote is added.
- 2026-07-05 — **Big architecture pivot in M2a:** the heavy audio-AI (Demucs, PyTorch) **cannot run on the founder's Windows-ARM machine** — PyTorch crashes on import (Bus error), confirmed after a thorough WSL attempt (which also destabilized WSL). So stem separation runs in the **cloud via Replicate** (`ryan5453/demucs`, htdemucs). API keys live in a gitignored root `.env`, loaded at startup. Results cached by content id → each song splits once. This confirms the buy-not-built map: the local machine is dev-only; real users will always need cloud audio. M2b analysis will follow the same cloud pattern.
- 2026-07-05 — **M2a complete.** Backend: `app/audio/stems.py` + `app/routes/stems.py` (split + serve, hex-id validated), `StemSet` model, 7 tests (download/store, cache-hit-no-call, error wrap, route validation). Front end: per-song "Split into parts" → 4 stem players, +1 test. Live end-to-end verified through the running server (upload → split → isolated vocals returned). 23 tests total.
- 2026-07-05 — **M2b complete.** Cloud: Replicate `sakemin/all-in-one-music-structure-analyzer` (allin1) → BPM, beats, downbeats, sections. Local pure-numpy (librosa/madmom don't install on ARM Windows — numba/llvmlite have no wheels): key via chromagram + Krumhansl–Kessler → Camelot; energy = RMS/bar; vocal regions from the split vocal stem; phrase starts = every 8th downbeat; confidence on every field (beat-regularity for bpm, profile margin for key, fixed 0.6 for sections — the weak link). Async start-then-poll route, cached JSON per song. UI: "Analyze track" → BPM chip, Camelot chip, proportional section timeline. Live verify: Father Ocean read at **122 BPM (correct)**, D major 10B (conf 0.7, honestly flagged), full structure map, 11 vocal regions. 33 tests total.
- 2026-07-05 — **Ops note:** C: drive hit 0 bytes free mid-M2b (the WSL Ubuntu vhdx had ballooned to 11.8 GB from the failed PyTorch attempts). With the founder's explicit consent, the unused Ubuntu WSL distro was removed (~12 GB reclaimed); pip caches and scratch clips cleared. WSL is no longer part of this project (cloud pivot).
- 2026-07-05 — **Bug fix (M2a): long songs failed to split.** Root cause: splitting held one HTTP request open for the full ~2min cloud job, and long songs (7:56) exceeded the request timeout while short ones (5:08) squeaked under — a fragile synchronous design. Fixed by making it **asynchronous**: `POST /songs/{id}/stems` starts a background thread and returns `202 processing` at once; `GET /songs/{id}/stems` reports processing/ready/error; the web app polls. In-memory `_jobs` registry (fine for single-worker validation; a persisted job table when hosted). Reproducing test added (async contract). Verified live on Father Ocean: POST returns in 1s, ready after ~90s. This matches the PRD's original async-jobs intent, deferred in M1/M2a and now needed.
- 2026-07-06 — **M3 built (the first real mix).** The brain plans, the engine executes (LLM never touches audio). New pieces: the **fence** (`app/planner/fence.py`) computes legal, safe options from both analyses (phrase-aligned high-energy drops, safe tempo stretch with octave-folding, vocal slice, key fit); the **AI driver** (`app/planner/plan.py`) has Claude pick among them with a deterministic fallback so a mix never blocks on the AI; the **referee** (`app/planner/validate.py`) checks the plan (R3 on-beat, B3 safe stretch) and the finished audio (R6 no clip / not silent); the **engine** (`workers/render.py`) sums S1 drums+bass+other, atempo-stretches + places S2's vocal slice with click-free fades, optional one-bar beat-breath, peak-normalizes to −1 dBFS. Async route (`app/routes/mix.py`, mirrors stems/analysis) caches the WAV by content hash of (song1, song2, prompt). Web: a **Make my mix** card → poll → play + download + DJ-language note. `MixPlan`/`Mix` models added. 35 backend + 2 web tests added (70 total); real-render end-to-end passes through FFmpeg on synthetic songs.
- 2026-07-06 — **Drift resolutions (M3):** (1) **Time-stretch = FFmpeg `atempo`, not SoundTouch.** Same class of tool (pitch-preserved tempo change), already installed, no extra binary to ship, and license-clean (LGPL core) for a commercial build — the local dev FFmpeg _does_ carry GPL `librubberband`, but the pipeline deliberately does **not** depend on it. Stretches kept within ±8% (fence declines wider pairs). SoundTouch/Rubber Band remain swappable if a demo pair ever needs a heavy stretch. (2) Added the `anthropic` SDK to the API venv (pure-Python, ARM-fine) for the driver. (3) `workers/render.py` lives at the repo root (matching the danger-glob) and is imported by the API via a small `sys.path` bootstrap in the mix route; it takes plain file paths so it stays decoupled from the app/db. Risk scorer rated both dangerous files `auto-apply` (low): new, additive, pre-launch; created via the confirm-and-apply flow with founder approval and an independent adversarial + scalability review before merge.
- 2026-07-06 — **M3 follow-ups (deliberately deferred):** the web flow still needs the two songs analyzed + split manually (per-card buttons) before "Make my mix" — the one-click "Studying your songs" screen (functional-spec Screen 2, auto analyze+split then mix) is a small UI task queued next. Final mastering (loudnorm + true-peak limiter) stays in M6; M3 uses peak-normalize to −1 dBFS. Short-clip export is M6. Live listen on Father Ocean + Tere Bina is the remaining M3 acceptance step.
- 2026-07-06 — **M3 live-listen fix (first real mix on a compatible pair).** Founder heard a real merge — milestone hit — and reported a ~2-second silent "pause" right before the vocal enters. Root cause: the AI set `beat_breath=True` ("punchy re-entry"), and the engine renders that as one full bar of **dead-air silence** before the vocal (~2s at 122 BPM) — which reads as the song pausing, and also makes the beat's re-entry feel off. Fix: the M3 planner now forces `beat_breath` **off** so Song 1's beat plays continuously under the vocal (honours the "music never stops" invariant); `ENGINE_VERSION` bumped `m3.1→m3.2` so cached gappy mixes aren't re-served. The engine's beat-breath capability stays (for M4) but is no longer triggered in M3. **M4 backlog:** rework beat-breath as a real tension move (bass-cut / filter / build, not silence); add drift/phase-lock across long placements (Handbook 9.6) — some vocal-vs-beat drift over a long single placement is a known M3 limitation. (Expected-not-a-bug, confirmed with founder: the vocal enters once on the loudest section, so the first stretch is untouched instrumental — the full weave is M4.)
- 2026-07-06 — **Independent reviews (scalability + adversarial safety) on M3.** Scalability verdict: ship at validation scale; architecture clean and right-sized. Two items applied immediately: a cache-version tag (`ENGINE_VERSION` folded into `mix_id`) so tweaking the engine/prompt never serves a stale cached mix; and logging swallowed render failures (`log.exception`) so a systematic bug isn't invisible. Adversarial safety verdict: could not breach data, corrupt a mix, or serve a broken render through the real flow (one-vocal/one-bassline guaranteed by construction, id/path-injection closed, ready-ordering sound, clipping capped). Two "should-fix" gaps in the dangerous-surface guards — not reachable through today's fence but real — were **hardened with founder approval**: (1) `validate_render` now catches near-silence (≥2% audible), not just exact-zero, closing a hole in the very guardrail the profile calls the app's worst-failure defense; (2) `render.py` caps decoded audio at 12 min (byte cap ≠ duration cap → memory-DoS on a tiny-but-hours-long file) and adds defensive tempo/anchor guards. 4 tests added (65 backend, 74 total). Follow-up logged: add a **duration cap at upload** too (belongs with the pre-public hardening list). **Carried into the M4/scale backlog (do these deliberately, not by surprise):** (a) mix WAV cache has no eviction — the fastest-growing data, worse once M4 adds regenerate; add a "keep newest N"/age sweep or stop persisting the intermediate PCM_16 WAV. (b) When M4 adds multiple vocal placements, extend `MixPlan` **additively** (new optional `placements` list; the engine prefers it when present) — do **not** rename/repurpose `anchor`/`vocal_src`, because plans are cached to disk as JSON and old ones must still parse. (c) The async-job skeleton (`_HEX_ID` + in-memory `_jobs` + daemon-thread spawn + file-inferred readiness) is now in three routes (stems/analysis/mix) — extract a tiny `async_job` helper the next time the job model is touched. (d) In-memory `_jobs` + local-disk readiness are correct for single-worker validation but break at the first multi-worker deploy (duplicate renders, lost state) and on the move to object storage (exists() → network round-trip) — the persisted job table + storage seam the code comments already anticipate.
- 2026-07-06 — **M4 Slice A built (the living arrangement + regenerate).** Design + plan in `docs/superpowers/specs|plans/2026-07-06-m4a-*`. `MixPlan` extended **additively** with `placements`/`take` (M3 cached plans still parse). Fence gains `arrangement_options` + `vocal_slices`; the driver arranges 2–3 non-overlapping placements (Claude, with a deterministic fallback), `take` rotates them for Regenerate; the referee checks no-overlap across placements; the engine renders each placement and **ducks the bed to 35%** for the beat-breath (never silence — the M3 dead-air fix stands, and the M4-backlog "rework beat-breath as tension" item from 2026-07-06 is thereby **done**); the route threads `take` (`ENGINE_VERSION m4a.2`); the web mix screen shows a two-lane arrangement timeline + Regenerate + Download. 88 tests (78 backend + 10 web).
- 2026-07-06 — **M4 independent reviews.** Scalability: **ship**, additively extensible, no scale cliff at validation scale. Adversarial safety: **found a real must-fix** — FFmpeg `atempo=stretch` outputs `source/stretch` seconds, not `source*stretch`; the driver+referee used the inverted `*stretch`, so for `stretch<1` (a faster vocal, ~half of compatible pairs) vocals could **overlap (two voices)** past the checks, and the original tests asserted the same wrong formula. **Fixed with founder approval:** one shared `fence.placement_end` (source/stretch) used by both driver and referee; `_default_arrangement` fit corrected; AI slices re-clamped to `MAX_VOCAL_SECS`; real overlap tests at `stretch<1` added; `ENGINE_VERSION m4a.1→m4a.2`. Lesson: a green suite proves nothing if the tests encode the same bug as the code — the independent adversarial pass is what caught it.
- 2026-07-06 — **M4/Slice-B + scale backlog (logged, deliberate):** (a) **mix-WAV cache eviction is now the top item** — Regenerate multiplies write-once WAVs (~30 MB each); add a keep-newest-N / age sweep **before the ~50-user test** (deletes finished mixes → belongs in `storage.py`, a dangerous surface, so gate it). (b) render decodes the vocal once _per placement_ (≤3 ffmpeg calls, `_MAX_PLACEMENTS`-capped) — fine at this scale; decode-once-slice-in-memory is a note-for-later micro-opt. (c) beat-breath ducks stack multiplicatively (0.35×0.35≈0.12) if two breathing placements land within a bar — audible-not-silent, low severity, Slice-B polish. (d) deterministic fallback can start the vocal at `anchor=0` (no instrumental intro) — a Slice-B/UX nicety (kept out now to preserve Regenerate variety). (e) live-AI arrangement behaviour + prompt-injection via `user_request` unverified (all AI tests mock `_ai_arrange`); output is structurally clamped (anchors snapped, count capped, slices clamped) so blast radius is low. **Slice B** proper: keep-Song-1-vocal for contrast (add an optional `MixPlan.s1_vocal_regions`, do **not** overload `Placement.vocal_src`), FX (optional `Placement.fx`), richer per-element confidence fallbacks.
- 2026-07-06 — **M4 Slice A live-confirmed done.** Founder listened (compatible pair) and confirmed the arrangement weaves and Regenerate works — the M3-era gap is gone. Also verified end-to-end without cloud cost by rendering a real 3-placement arrangement from cached songs (122-BPM beat + 125-BPM vocal, stretch 0.976 — the fixed sub-unity case): placements at 0:18/0:33/0:49, **no overlap**, valid 7:56 WAV (peak 0.891). M4 Slice A acceptance met.
- 2026-07-06 — **M4 Slice B built (contrast + subtle FX + confidence fallbacks).** Design + plan in `docs/superpowers/specs|plans/2026-07-06-m4b-*`. Additively: `Placement.fx` (`"sweep_in"`) + `MixPlan.s1_vocal_regions`. `fence.contrast_windows` finds beat-only gaps where Song 1 itself sings; `plan._apply_flourishes` adds one contrast + one sweep on a confident song and plays safe (≤2 placements, no flourishes) on a shaky one; the referee guarantees Song-1 and Song-2 vocals never overlap (R1 across both songs) via the shared `placement_end`; the engine mixes Song 1's vocal stem into the gaps (ratio 1.0) and applies a leading-edge-eased low-pass sweep; route passes Song 1's vocals, `ENGINE_VERSION m4b.1`. Web timeline shows both voices in distinct colors + a sweep mark. 91 backend + 10 web tests.
- 2026-07-06 — **M4 Slice C built (the energy arc — vocal spans the whole song).** Approved via `/zuko:discover` (research: EDM/Fred again../Black Coffee/mashup craft all converge on "shape an arc across the whole track"; our own DJ Handbook already says it — H1/H2/H5/H6). Hero-output question resolved: **full-length mix is primary** (functional-spec open-assumption #3 closed). Problem: the planner ranked anchors by **energy only**, so on a long song (real 7:52 pair) the 2–3 placements clustered in the loud middle, leaving the first half + ending empty (founder heard this). Fix (planning code only — `fence.py` + `plan.py`, no dangerous surface): `fence.arc_anchors` splits the timeline into thirds and takes the best-energy anchor per band (one vocal moment per third, strong entry saved for the final third, `take` rotates within-band for Regenerate variety); `_default_arrangement` uses it instead of a sliding window of consecutive anchors; a new **arc guard** `_spans_song` rebuilds any clustering plan (incl. a misbehaving AI) as the deterministic arc, so the acceptance test holds **by construction**; the AI prompt/payload gained `track_length_seconds` + `song1_sections` + `recommended_spread_anchors`. `ENGINE_VERSION m4b.1→m4c.1`. Same placement count → **no render-cost change**; referee (`validate.py`) and engine (`render.py`) untouched. TDD: 5 new tests (fence spread/rotation/fallback; plan spans-whole-song; guard-rebuilds-clustered-AI). **Verified end-to-end, no cloud cost**, on the cached 122-BPM 7:52 beat + 125-BPM vocal: Take 1 = 1:52/5:01/5:32 (+ S1 vocal 2:41, sweep on last), Take 2 = 1:36/4:29/6:51 — both span first-half→final-third, takes differ, render passed the referee. 96 backend + 10 web green. **Backlog (deliberate next layer):** still 2–3 vocal moments → a very long song can feel a touch sparse between entries; adding _more_ vocal returns touches `workers/render.py` (dangerous) and is gated on the founder finding it too sparse by ear. **Live-listen the arc** is the open acceptance step.
- 2026-07-06 — **M5 Slice 1 built (live-control foundation).** Design + plan in `docs/superpowers/specs|plans/2026-07-06-m5-slice1-*`. The first "steer with words" slice: the browser plays Song 1's stems (drums/bass/other) live and in sync via **raw Web Audio** (no Tone.js — not installed, not needed), and "take the bass out"/"bring it back" lands **on the next downbeat** with a smooth 1-bar gain ramp, replying in DJ language. New, non-dangerous pieces: `LiveOp` model (additive), `planner/live.py` (deterministic command parser + out-of-scope decline; LLM path deferred behind the same fallback pattern as `plan.py`), `routes/live.py` (`POST /live/command` → LiveOp, `GET /live/context/{id}` → bpm+downbeats), web `lib/liveSchedule.ts` (pure, unit-tested: next-bar time, bus reducer, ramp target), `lib/liveAudio.ts` (`LivePlayer` Web Audio engine, ear-verified), `components/Live/LiveMix.tsx` wired **additively** into `Uploader.tsx` alongside the M4 finished-mix player. Built via subagent-driven TDD (6 tasks). **Gotcha hit:** the web `*.test.ts`/`*.test.tsx` files are a **dangerous surface** (test-harness guard) — adding new tests needed founder confirm-and-apply approval (Python `test_*.py` are not guarded, only `conftest.py`/`pytest.ini`); LiveMix wraps `new AudioContext()` in try/catch so jsdom/no-Web-Audio degrades to "not ready" (also fixes real browsers without Web Audio). 136 backend + 18 web green. **Open acceptance: founder live-listen** (press play, "take the bass out" → bass drops on the beat → "bring it back"). **Deferred to later slices:** Song 2's arranged vocal layered into the live player (via a backend arranged-vocal bus, not a browser rebuild), the other moves (drop-everything-but-the-beat, remove vocals, beat up, fade away), and the AI-judgment layer (AI-decided mute depth + context-aware suggestion buttons). Live tempo/BPM change stays V2.
- 2026-07-06 — **M4 Slice D built (per-bar beat-lock — the vocal stops drifting).** Found via founder ear-check of Slice C: spreading the vocal into longer sections exposed a pre-existing drift (a single global `atempo` ratio can't hold a long vocal on the beat; real tempos wobble + BPM detection is imperfect). Measured on the real 122/125 BPM 7:52 pair: **+315 ms** drift over a 40 s vocal. Fix (`/zuko:fix`, heavy path): `fence.warp_map` maps each Song-2 bar onto the matching Song-1 bar (`out_secs` = Song-1 bar length → boundaries lock to downbeats; drift resets each bar); `Placement.warp` (additive) carries it; `_vocal_take_warped` (engine, dangerous) renders per-bar with equal-power crossfades; referee (dangerous) gains **R7** (per-bar ratios in band + boundaries on grid) and makes R1 warp-aware; `ENGINE_VERSION m4c.1→m4d.1`. Same placement count. **Heavy-path ceremony:** confirm-and-apply on the two dangerous files (founder approval recorded/cleared); independent test-author wrote 21 hollow-test-hardened acceptance tests; a 3-lens quorum reviewed — scalability **ship**, DSP **safe**, and the safety-guarantee lens **caught a real must-fix** (R7 falsely rejected ~29% of mid-bar-start plans by shifting the grid on a leading partial). **Fixed** (warp starts on Song-2's first downbeat; a glitch bar → `[]` legacy fallback) with 3 reproduce-then-close regression tests; **re-review confirmed safe** (29% → 0.2% loud declines, 0 overlaps). **Verified end-to-end (no cloud cost)** on the 7:52 pair: boundary drift **315/236/180 ms → 0/0/0 ms**, R7+R1+R6 pass. 127 backend + 10 web green. **Backlog (logged, non-blocking):** (a) ~0.2% _loud_ R7 decline on band-edge pairs (rounding mismatch between `warp_map`'s unrounded band-check and R7's rounded recompute on a trailing partial) — give R7 a rounding-aware tolerance next time in `validate.py`; (b) per-bar render spawns ~~60 FFmpeg processes on a 7-min mix (~~+10 s, invisible behind stem separation) — collapse to one per-placement filtergraph, **bundle with the mix-WAV eviction work**; (c) `_ai_arrange` doesn't snap the slice start to a Song-2 downbeat (drops up to ~1 bar of AI lead-in). **Open acceptance: founder live-listen** that the arc + beat-lock sound right together (no click through the crossfades).
- 2026-07-06 — **M4 Slice B independent reviews.** Scalability: **ship** (additive, render cost bounded, boundaries clean). Adversarial safety: **SAFE** — the two-lead-voices guarantee could not be broken (straddles, sub-unity stretch, unsorted plans, evil-AI injection all defended by the referee, which is the backstop regardless of how the plan was built); M4a back-compat byte-identical; the sweep can't clip end-to-end; degraded inputs fail safe. Applied its **quality** polish (none safety-blocking): eased the sweep's leading edge to kill a possible faint click; corrected a misleading `_sweep_bed` comment; a typo-guard in the referee so an unknown `fx` fails loudly rather than silently no-op; contrast margin widened to 2 s so it clears the next duck/sweep bar. **CLAIM to re-verify next session:** a human listen to one real contrast+sweep mix (Slice B's audible quality — clicks, contrast balance — is the one thing CI can't judge). **Slice-B backlog notes (logged, not blocking):** `bpm_confidence is None` defaults to confident (only reachable on legacy analyses; real pipeline always sets it — consider defaulting unknown→shaky); if `s1_vocal_regions` ever grows a per-region field, promote the tuple to a named model in the same change. **The mix-WAV cache-eviction sweep remains the top item to land before the ~50-user test.**
- 2026-07-06 — **M5 Slice 2 built (the full mix, live, all parts controllable).** Design + plan in `docs/superpowers/specs|plans/2026-07-06-m5-slice2-*`. The live player now plays the **whole** mix and every part is steerable on the beat. **Key design (decided last session, held):** do NOT rebuild the M4 arrangement in the browser — a new `workers/live_stems.py` renders an **arranged-vocal bus** (Song 2's placed/warped/edge-faded vocal + Song 1's contrast regions) onto silence by **importing** `render.py`'s helpers (no engine edit, no duplicated DSP); vocal at **ratio 1.0, not normalized** so the browser's live sum matches the Download's balance. Served by `GET /live/vocal-bus/{mix_id}` (async/cached, derived from the cached `MixPlan`). `LiveOp` gained `targets` (multi-part ops); the parser grew to remove/bring-back vocals/drums/melody + the combos "drop everything but the beat" / "bring it all back". Web: `liveSchedule` gained a `vocals` bus + multi-target `applyOp`/`busesOf`; `liveAudio` loads the vocal bus as a 4th sync-started bus and ramps every named bus on one shared bar; `LiveMix` shows four **tappable** friendly-labelled parts (Beat/Bass/Melody/Vocals), reloads the vocal bus when the take changes (Regenerate follows), and disables Vocals until a mix exists; `MixMaker` emits `onMixReady(mix_id)`, `Uploader` lifts `mixId`. The live player is a steerable **approximation** (no sweep/beat-breath — those stay Download-only), founder-approved. Built via subagent-driven TDD (6 tasks + docs); each task independently reviewed clean. **`render.py`/`validate.py`/`storage.py`/`songs.py`/config untouched;** only the web `*.test.ts`/`*.test.tsx` files (test-harness guard) went through confirm-and-apply. **149 backend + 22 web green (171).** **Open acceptance: founder live-listen** (press play → hear the full mix → tap Vocals off / "drop everything but the beat" → parts fade on the beat). **Backlog (logged, non-blocking):** (a) vocal-bus WAVs join the mix-WAV **cache-eviction** debt (do before the ~50-user test, in `storage.py`); (b) the async start/poll/serve job pattern is now in a **4th** route (`/live/vocal-bus`) — extract a shared `async_job` helper next time it's touched; (c) `applyOp` now returns a fresh object on the no-op path (identity change; no behavior/test impact); (d) Slice-1's stateless-parser quirk persists (a redundant "bring it back" still replies) — the Slice-3 AI layer gives it live state.
- 2026-07-06 — **M5 Slice 3 built (AI smart-suggestion chips + "fade away").** Design + plan in `docs/superpowers/specs|plans/2026-07-06-m5-slice3-*`. As the mix plays, the live player shows 1–3 **context-aware chips** that change with Song 1's sections; tapping applies the move on the beat. **Key design:** a new `planner/suggest.py` points the arrangement-style brain at the section timeline and returns, per section, 1–3 moves from a **closed vocabulary** (Bring/Take the vocal, Take the bass out, Drop to just the beat, Bring it all back, Fade it out) — the AI can only pick moves the engine already executes; unknown picks dropped; any AI failure → a deterministic label→chips fallback. One call per mix, **cached** by `GET /live/suggestions/{mix_id}` (sync, fallback-safe — deliberately not a 5th async-job copy). Added the `"fade"` op (parser "fade away"; a 4-bar all-bus ramp in the player). Web: `applyOp` handles fade, `currentChips` picks the playhead's section, `LiveMix` polls the playhead (~250 ms while playing) and renders a **swappable chip row** (for the later UI design pass). **User stays in control** — chips only suggest; taps/typed commands unchanged. Scope decisions (founder): AI shows up only as suggestions (not depth on direct commands); **"beat up" deferred** (define its sound first). Built lean/inline (founder machine load) with per-task TDD; each task committed green. **`render.py`/`validate.py`/`live_stems.py`/`storage.py`/`songs.py`/config untouched;** only `liveSchedule.test.ts` + `api.test.ts` (test-harness guard) via confirm-and-apply. **157 backend + 26 web green (183).** **Open acceptance: founder live-listen** (chips show, change per section, land on the beat when tapped; "fade it out" fades the mix away). **Backlog (logged, non-blocking):** (a) suggestions JSON joins the mix-WAV + vocal-bus **cache-eviction** sweep (before the ~50-user test, in `storage.py`); (b) **"beat up"** energy move still to define + build; (c) chips are per-section, not live-mute-state-aware (tapping an already-active move is a gentle no-op) — smarter awareness is a later enhancement; (d) the async start/poll/serve pattern remains a 4th copy (suggestions is sync, so it didn't add a 5th) — extract a shared `async_job` helper when next touched.
- 2026-07-07 — **Bug fix: "Father Ocean × With You" (and any pair whose beat-lock warp ends in a tiny trailing partial bar) crashed mix-building with "Couldn't build this mix."** Root cause (`/zuko:fix`, heavy path, `superpowers:systematic-debugging`): in `_vocal_take_warped` (`workers/render.py`), FFmpeg's seek/atempo rounding hands back each stretched bar a little LONGER than its ideal hop+crossfade window (confirmed on the real pair: 200–700+ samples per bar). The output buffer only ever reserved one crossfade's worth of slack at the very end, sized from the last bar's actual length — fine normally, but when the warp's last bar is a near-zero trailing partial (the vocal's own tail, as with "With You"), that slack nearly vanishes and the accumulated overshoot from earlier bars runs a later write past the buffer (`ValueError: could not broadcast`). **Fix:** clamp every non-final bar's decoded audio to its exact allotted `hop+xf` window (pad if ever short) before assembly — this also fixes the crossfade alignment, since any overshoot beyond that window was never meant to be kept. Reproduced first with a hermetic test using the exact measured warp + overshoot values from the real pair (`test_vocal_take_warped_survives_ffmpeg_length_overshoot`), confirmed the same crash, then fixed to green. **Heavy-path ceremony:** confirm-and-apply on `workers/render.py` (founder approval recorded/cleared). **Verified live end-to-end** through the running server: the exact mix_id that crashed earlier in the session now returns `status: "ready"` for all 7 real AI-arranged regenerate takes on the real pair. 177 backend + 39 web green.
- 2026-07-07 — **Bug fix: Regenerate wasn't producing genuinely unique mixes — every take replayed the same short vocal excerpt, only the timing moved.** Root cause (`/zuko:fix`, light path — no dangerous surface touched): `fence.vocal_slices()` only ever built candidates from `TrackAnalysis.vocal_regions`, and fell back to a single slice (`best_vocal_slice`'s first chorus) whenever that came up empty. Checked the real catalog: **3 of the 4 vocal tracks** ("Der Lagi Lekin", "Tujhe Bhula Diya", "With You") have **zero** detected vocal_regions (a real MIR-analysis gap), even though all three have clear verse/chorus/bridge sections. So every placement in every arrangement — and every regenerate take — reused the exact same ~16s chorus, because there was nothing else to choose from; `_default_arrangement`'s slice pick (`slices[i % len(slices)]`) also never varied by `take`, only by placement position. **Fix:** (1) `vocal_slices()` now falls back to the song's own labelled sections (verse/chorus/bridge/solo — never intro/outro/instrumental) when vocal-region detection is empty, offering multiple real candidates instead of one (e.g. "With You" went from 1 slice to 4); (2) `_default_arrangement` now rotates WHICH slice fills a placement by `take` too (`slices[(i + take - 1) % len(slices)]`), not just by position, so regenerate pulls different vocal content, not just a different timestamp of the same line. This also directly answers "can vocals play longer": longer sections (up to `MAX_VOCAL_SECS` = 40s) are now real candidates instead of being invisible behind the single-slice fallback. TDD: reproduced with `test_vocal_slices_falls_back_to_sections_when_regions_missing` (fence) and `test_regenerate_varies_vocal_content_not_just_anchor` (plan) — both failed against the real bug, pass after the fix. **Verified against the real Father Ocean × With You pair:** vocal slice candidates went 1 → 4; 4 of 5 real regenerate takes now pull genuinely different vocal content (the 5th collided by coincidental modular-arithmetic overlap between the anchor and slice rotation periods — logged as a minor follow-up, not blocking). 179 backend + 39 web green, typecheck clean. **Scope note:** the request also asked for a general "remove unnecessary code" sweep and a broad "hygiene check" on the mixing pipeline — deliberately NOT bundled into this fix (unbounded, no concrete bug to reproduce against; better suited to a dedicated `/zuko:gate` pass if wanted).
- 2026-07-07 — **Shareable-link deployment prep + private tunnel (`/zuko:build`; founder chose private reach + tunnel-now/cloud-later).** Made the app deployable behind ONE origin: (1) `apps/web/src/lib/api.ts` — `API_BASE` is now configurable: `VITE_API_BASE` override, else `http://localhost:8000` in dev, else **same-origin `""`** in a production build (so the built UI calls the same host it's served from — no CORS, keys never cross-origin). (2) `services/api/app/main.py` — mounts the built web app (`apps/web/dist`, gitignored) at `/` via `StaticFiles(html=True)` **after** the API routers, so one host (port 8000) serves both the UI and the API; a no-op in local dev (Vite serves the UI, `config.py`/CORS untouched). Neither file is a dangerous surface. Built the frontend (`npm -w apps/web run build`) and **verified the single-origin backend** serves `/` (app), `/library` (API), `/assets/*` (code) all 200. **Live private link via ngrok** (agent auto-updated 3.3.1→3.39.9; `ngrok http 8000`): the whole app works end-to-end through the tunnel — page + JS/CSS + a 19 MB stream all 200. **Known limits of the tunnel (founder-accepted, inherent to "quick tunnel from my machine"):** the link only lives while the machine + backend + tunnel run; ngrok-free gives a NEW random URL each restart; ngrok-free shows a one-time "Visit Site" interstitial on first open (the app's XHR calls with `Accept: */*` bypass it); every mix still spends the founder's Anthropic + Replicate credits (private reach keeps that controlled); one machine does all the audio work. **Next step when validated → permanent cloud host** (a small always-on Linux VM / managed platform with FFmpeg + the keys as secrets + a persistent disk for the catalog/mixes, then lock CORS in `config.py` to the real domain — that CORS edit is the one dangerous-surface touch, gated). No test changes; 39 web + 181 backend still green; typecheck clean.
- 2026-07-07 — **Responsive UI for phone + tablet (`/zuko:build`, light path — frontend only, no dangerous surface).** The app was a fixed 1280×800 console/stage canvas scaled to fit via `transform: scale()`, so on a phone it shrank to ~29% and never reflowed (unusable). Guided by the UI/UX design skill. Added a `@media (max-width: 900px)` block in `index.css` that switches OFF the scaled canvas (`transform: none`, real pixels, `position: static`), lets the page scroll (`100dvh`, `overscroll-behavior-y: contain`, `touch-action: manipulation` to kill tap delay), and stacks the console over the stage full-width; a `@media (max-width: 480px)` hides the passive 4-step nav on narrow phones. Per-screen mobile blocks: Setup (fluid preview tiles, neutralized the fixed-height `margin:auto` centering), Play (the complex one — hide the duplicate console title, reorder so the stage/title/transport lead and the chat feed drops last via flex `order`, inline the LIVE badge, 54px play button, full-width 44px scrubber with an always-visible knob, 44px Part/chip tap targets, 16px command input to stop iOS zoom-on-focus), Generating + Export (neutralized `margin:auto`, bigger tap rows). `Frame.tsx` gained `data-screen` on `.body` so Play's reorder is scoped. **Verified live via computed-style inspection** (screenshots were flaky on the live-audio Play screen): at 375px and 768px — zero horizontal overflow, stacked full-width, stage-first on Play, all touch targets ≥44px, input 16px, feed last; at 1440px the framed 1280×800 scaled desktop design is byte-for-byte intact (row layout, `overflow: hidden`, scale 1). 39 web tests green, typecheck clean. No backend change.
- 2026-07-07 — **Bug fix: Play button dead after Regenerate (live-vocal race condition).** Symptom (founder): first mix played; after "another take", the Play button never became clickable. Root cause (`/zuko:fix`): `_run_vocal_bus` (`routes/live.py`) rendered the arranged-vocal bus **straight to its served path** (`{mix_id}.livearr.wav`), and `GET /live/vocal-bus` served the file as soon as it _existed_ on disk — but the render is still writing it, so a poll that arrived mid-render got a `FileResponse` whose `Content-Length` was the partial size while the body kept growing → `h11 LocalProtocolError: Too much data for declared Content-Length` → the browser's `fetchVocalBus`/`decodeAudioData` failed → `LivePlayer.load()` rejected → `PlayScreen` `setReady(false)` → Play stayed disabled forever. Only bit a **freshly-rendered** bus, so the first (cached) take played and the regenerate (new `mix_id`) didn't — exactly the founder's report. **Fix:** render to a temp file and **atomically publish** with `os.replace`, so the served path only ever appears fully written (also survives a mid-render restart). **Gotcha caught by end-to-end verify, not the unit test:** `soundfile` writes by file _extension_, so the temp must keep `.wav` (`{mix}.livearr.tmp.wav`, not `.wav.tmp`) or the real renderer can't write it — the unit test uses a fake renderer and would have hidden this, so the test now also asserts `temp.suffix == ".wav"`. TDD: `test_vocal_bus_is_published_atomically` (fails on the pre-fix direct-write, passes after). **Verified live** through the running server: a fresh regenerate take served `409→202→200` with a complete 72 MB WAV and **no** Content-Length error (was a hard `LocalProtocolError` before). `routes/live.py` is not a dangerous surface. 181 backend green. **Follow-up (logged):** the mix WAV and suggestions JSON are gated differently (plan-written-last / sync render) so they don't hit this, but if either ever moves to background rendering, give it the same atomic-publish.
- 2026-07-07 — **Mixing-pipeline hygiene sweep (`/zuko:gate` read-only audit → `/zuko:fix`).** Four independent reviewers audited the whole mixing pipeline (planner, render engine, referee, routes, docs). **Verdict: engine is sound — no way found to ship a two-voice/clipping/silent mix; scalable at validation scale; the earlier per-bar clamp fix re-confirmed correct.** Acted on the findings that mattered: **(1) Behavior — shaky-song arc:** `_apply_flourishes` trimmed a 3-placement arc to the FIRST two on a low-confidence song, leaving the whole final third silent; now keeps the arc's ENDS (first + last) so a long shaky song still gets an early AND a late entry. **(2) Behavior — regenerate variety on thin analysis:** `_default_arrangement` now slides the vocal SUB-WINDOW within a region by `take` (up to 3 positions when the region has ≥8s spare), so regenerate plays a genuinely different part of the same section even when a song offers only one vocal region (the hardest "every mix unique" case); the fully-degenerate 1-anchor/1-full-region mix is genuinely single and left as-is (honest). Both TDD (`test_shaky_song_still_spans_the_whole_song`, `test_regenerate_varies_vocal_window_from_a_single_region` — failed first, pass now). **(3) Dead code removed:** `fence.section_at` (zero callers) and `workers/live_stems.render_full_vocal` (superseded by `render_vocal_bus`, only its own test called it) + its test + the now-unused `_decode` import. **(4) Duplication removed:** `best_vocal_slice` now reuses the shared `_snap_and_cap` (fixes a subtle cap-before-snap order divergence); the 4-line `_hold` helper hoisted to module scope in `render.py` and imported by `live_stems.py` (was a byte-identical copy). **(5) Docs swept to match code** (rule 8): tech-spec R4/R5 "validator enforces" corrected (they're shaped upstream, not referee-enforced — validator does R1/R3/B3/R6/R7); ±8%→±11% in 3 spec spots + the `best_stretch` docstring; beat-breath "silences"→"ducks to 35%" (render.py header + tech-spec); vocal-bus cache `.vocalbus.wav`→`.livearr.wav`; functional-spec "uploads only" rows/prose marked superseded by the catalog pivot. **Dangerous surface:** only `render.py` (docstring + `_hold` hoist, no behavior change) via confirm-and-apply (founder approval recorded/cleared); `validate.py` untouched. **Deliberately NOT done (out of scope / architecturally correct as-is):** the async-job scaffold + `_HEX_ID` dedup across 4 routes (logged refactor backlog, not dead code); `_placements_of` "duplication" across render/validate is correct-by-design (render.py must stay decoupled from `app/`, so it can't share a fence helper — the scalability reviewer confirmed). **Logged follow-ups (non-blocking):** fold a `LIVE_VERSION` into `.livearr.wav`/`.suggestions.json` cache names when the live bus/suggestion vocab next changes (same stale-cache class `ENGINE_VERSION` already closes on the mix side); extract the shared `async_job` helper BEFORE the multi-worker migration. **180 backend + 39 web green, typecheck clean.**
- 2026-07-07 — **Catalog narrowed to a single-beat "Father Ocean shelf" + clean-blend band widened ±8%→±11%.** After ear-tests showed tempo-matched ≠ good (key/vibe mismatch is what kills a mashup, and the app can't pitch-shift), the founder curated hard: **one beat (Father Ocean, 122 BPM / Camelot 10B)** + vocals chosen for **key compatibility** (10B family) not just tempo. Parallel web research (cited) found the melodic-house key clusters are small and that Bollywood mashups over Father Ocean almost all pitch-shift (which we don't do). Real-audio analysis of the founder's candidates: **Don't Start Now** (10B, +2.4% — clean anchor), **Der Lagi Lekin** (10B, +9.9%), **Tujhe Bhula Diya** (10B, −8.3%), **With You / AP Dhillon** (11A near-clash, tempo-clean — ear-test), **Suniyan Suniyan** (4B hard clash — dropped). To fit the two key-perfect-but-tempo-borderline Bollywood vocals (Der Lagi, Tujhe), **`fence.SAFE_STRETCH` widened 0.92/1.08 → 0.89/1.11** (±8%→±11%) — this satisfies both B3 (plan) and R7 (per-bar render), which share the constant. **Trade-off (founder-accepted):** more audible stretch is now allowed on wide pairs across ALL mixes — the two borderline vocals must be ear-tested; if warbly, drop them and tighten back. `fence.py` isn't a dangerous-surface file; tests use the constant symbolically (only `test_plan.py` hardcode updated). `validate.py`/`render.py` untouched. **176 backend + 39 web green.** Catalog manifest + audio live in gitignored `data/` (local-only). **Bigger strategic note recorded:** a Bollywood-_heavy_ catalog genuinely needs the pitch-shift engine the founder set aside (V2). **Open acceptance: founder ear-test** — Father Ocean × each vocal; confirm Der Lagi/Tujhe don't warble and With You's key doesn't clash.
- 2026-07-06 — **MVP pivot: curated song catalog replaces user uploads (founder decision).** After a repeat "too far apart" decline, researched real BPM data (two parallel web researchers, sources fetched + cited): the aggregate finding is that **the real Tere Bina is ~144 BPM documented (a ~72-BPM ballad counted double) — it genuinely cannot blend with a 122 beat in V1**; the 125-BPM vocal in the founder's successful mixes was almost certainly **Don't Start Now** (mixname cache "Don't Start Father Now" + 125≈124 documented + 10B≈10A corroborate). So misreads AND genuinely-incompatible files were both biting; the founder pivoted: **users pick from preloaded, hand-verified songs — no uploads in the MVP** — so every pairing blends by construction. Built: `routes/library.py` (`GET /library`, manifest-driven, read-only, entries reference already-ingested song ids → all caches reused, no re-analysis), manifest seeded with the two verified songs (Father Ocean 122 beat; Don't Start Now ~125 vocals — founder to confirm the name by ear); `SetupScreen` rewritten as two dropdown slots (duplicate pick disabled; empty/error states; **no file inputs** — regression-tested); `App` skips upload (`pick1/pick2` → straight to study); study stage relabelled "Loading your two songs". `songs.py`/`storage.py` untouched; web `*.test.*` via confirm-and-apply. **176 backend + 37 web green (213).** Browser-verified against the live backend (catalog lists, picks fill slots, Mix-it enables). **Supersedes the "uploads only" V1 decision for the MVP** (functional spec updated; CLAUDE.md profile refresh queued). **Next:** founder picks the final 5 beat + 5 vocal songs from the research shortlist (all within ~5% tempo spread, all 25 pairings safe); founder supplies the audio files; each gets ingested + hand-verified once and appears in the dropdowns. Licensing note logged: preloaded copyrighted songs are fine for the private ~50-person test, not for public distribution.
- 2026-07-06 — **Fix + feature: Play now matches the finished mix, and the transport is seekable.** Founder ear-test on the new Play screen: the vocals "played all over the place / continuously / not matched up." **Root cause (not a hidden bug — a design trade-off gone wrong):** the live Vocals part was Song 2's _continuous_ full vocal (`render_full_vocal`, one global stretch from t=0), so it wandered off-beat and never rested — a different, rougher arrangement than the "perfect" Download. **Fix (founder chose "match the finished mix"):** the live route now renders the **arranged, per-bar beat-locked vocal bus** (`render_vocal_bus` — the SAME vocal as the Download, placements + warp + Song-1 contrast) instead of the continuous one; cache renamed `.livevocal`→`.livearr` to drop stale continuous caches. So the Play screen now sounds like the finished mix; this also **closes the deferred "live-vocal beat-lock" item** (the live vocal is now beat-locked). Trade-off accepted: "bring the vocal in" during a beat-only stretch has no vocal to add there (the arrangement doesn't place one) — honest. `render_full_vocal` now unused by the app (kept + tested). **Feature:** the transport scrub is now **click/drag-seekable** (+ arrow-key ±5s), and pause/resume now **resumes from position** instead of restarting at 0 — `liveAudio.LivePlayer` gained `offset`/`seek()`/`startSources()` and a position-aware `songTime()`; `PlayScreen` wires a draggable knob with a keyboard slider role. TDD: `test_live_route.py` (+the live bus renders via `render_vocal_bus`, not the continuous one; cache-name updated). **173 backend + 30 web green (203).** No dangerous surface (live route/live_stems/liveAudio are not on the danger list); no test-harness edits needed this time. **Open acceptance: founder ear/hands-test** — regenerate a take (fresh `.livearr` renders on first play), confirm the vocal now enters arranged/on-beat, and drag the transport to scrub.
- 2026-07-06 — **UI design pass built — the "Electric Violet" redesign + AI mix names.** Applied the founder's approved design (from a Cloud-design handoff + screenshots; mockup approved as an Artifact before building). The single long page is now **four state-driven screens** sharing one shell (a 1280×800 frame scaled to fit; top bar; 360px console / stage split): **Setup** (two upload cards + prompt + "Mix it") → **Generating** (our real study checklist + pulsing ring) → **Play & Steer** (command feed + the four part toggles + Beat up + suggestion chips + command input; stage = BEAT/VOX arrangement lanes + transport + LIVE + "another take" + export) → **Export** (download full mix now; 14s clip flagged M6). Two-hue palette (grey + one violet `#8F00FF`), Instrument Serif + Space Mono (Google Fonts). **New AI feature:** mixes get a playful **AI-generated name** (e.g. "Ocean Bina") — `app/planner/name.py` (LLM + deterministic "A × B" fallback) via `POST /mix/name` (cached); shown in place of "Untitled Mix". **Architecture:** `App.tsx` is the state machine; `components/shell/Frame.tsx` the shell; `components/screens/*` the four screens; tokens + shared layout in `index.css`. Old `Uploader`/`Mix`/`LiveMix` components retired (logic preserved in the screens over the unchanged `lib/` layer — api/study/liveAudio/liveSchedule untouched). **No engine/validator/storage/songs change;** only the web `*.test.*` files (test-harness guard) via confirm-and-apply. TDD (App flow + screen + name tests); browser-verified Setup renders in the exact design (violet `#8F00FF`, Instrument Serif) with zero console errors, and the live AI named "Father Ocean × Tere Bina" → "Ocean Bina". **172 backend + 30 web green (202).** **Open acceptance: founder click-through** of all four screens on a real pair. **Deferred (unchanged):** the 14s clip export + share links (M6); cache eviction now also covers `.mixname.txt`.
- 2026-07-06 — **"Beat up" built — the last V1 live command (live command set now complete).** Founder chose the sound: **"the beat takes over"** — melody + vocals duck so drums + bass drive (no tempo change; that's V2). New `beat_up` op: parser (`live.py`) maps "beat up"/"energy up"/"pump it up"/… → duck other+vocals; the browser (`liveAudio.schedule`) ramps every bus over one bar to fixed levels (drums/bass 1.0, melody/vocals 0.4 — gain-only, clip-safe); `applyOp` keeps all parts "on" (nothing muted, tops only duck); a **"Beat up"** button added to `LiveMix` (op built locally, no round-trip), typed command also works; `LiveOpDTO.op` widened. Reversible with "bring it all back". **No engine/validator/storage change** — only the two web `*.test.*` files (test-harness guard) via confirm-and-apply. TDD; verified the running backend parses "beat up" and the app loads clean. **168 backend + 33 web green (201).** **Open acceptance: founder ear-test** (play a mix → tap **Beat up** → melody/vocals duck so the beat drives, on the beat → "bring it all back"). **Follow-up (optional):** surface "beat up" as a contextual suggestion chip (add to `suggest.py`'s closed vocabulary).
- 2026-07-06 — **Fix: "Failed to fetch" when the app runs off port 5173.** Founder ran the new build on `localhost:5174` (their other window held 5173) and hit "Failed to fetch" on the studying screen. Root cause (not a feature bug — the studying screen correctly caught the failure and offered Start over): the backend CORS allowlist was hardcoded to `http://localhost:5173`, so the browser blocked every API call from `:5174`. Fix (`/zuko:fix`, dangerous surface — security config, founder-approved confirm-and-apply): added `config.allowed_cors_origin_regex = r"http://(localhost|127\.0\.0\.1):\d+"` and wired it into `main.py`'s `CORSMiddleware` — any local port is now trusted, remote sites still blocked. Reproducing tests first (`tests/test_cors.py`: standard origin allowed, `:5174` allowed [was the failing repro], `evil.example.com` still blocked). **165 backend + 32 web green.** Pre-public: lock CORS to the real deployed origin (added to the hardening list). Runtime note: the change is live only after the `:8000` backend restarts.
- 2026-07-06 — **One-click "Studying your songs" screen built (pre-user-test UX).** Removes the manual per-song **Split**/**Analyze** buttons: drop two songs → **Make my mix** → an honest studying checklist (splitting → finding the beat → planning) → the finished mix, hands-free. **Frontend-only orchestration, no backend/engine change, no dangerous code surface** (only the web `*.test.*` files, via confirm-and-apply). New `lib/study.ts` (`studyAndMix`: split both → analyze both → plan, with stage callbacks) — **split runs before analyze by design**, because `analysis.py` reads the split vocal stem to locate the singer (the old flow let you analyze-without-splitting → a weak read; now fixed). Extracted `splitSong`/`analyzeSong`/`makeMix` poll helpers into `lib/api.ts` (de-duplicated the three copy-pasted polling loops); `Uploader.tsx` rebuilt as idle → studying → mix (the M2 per-song stem players + insight cards dropped from the default casual-creator path); `MixMaker` gained an optional `initialMix` so the studying flow shows the result immediately (Regenerate unchanged). Mix prompt stays `""` so the cached demo pair is still instant. TDD; verified the new idle screen mounts clean in the browser (one **Make my mix** button, no Split/Analyze, zero console errors). **162 backend + 32 web green (194).** **Open acceptance: founder click-through** on a fresh pair (drop two → Make my mix → watch the checklist → land on the mix) and on the demo pair (near-instant).
- 2026-07-06 — **M5 Slice 3 fixes (founder ear-test, /zuko:fix).** Three issues found + fixed, each with a reproducing test. **(1) The AI brain was never actually running** — newer models return a _thinking_ block first, so `msg.content[0].text` raised AttributeError, was swallowed by the broad `except`, and BOTH the arrangement (`plan.py`) and live suggestions (`suggest.py`) silently fell back to the deterministic rules every time (`source="rules"` everywhere). Fix: new `app/planner/llm.py` (shared `MODEL` + `first_text` that skips thinking blocks + `extract_json`), model → `claude-sonnet-4-5`; **live-verified the AI now returns real per-section picks.** So "AI decides" is finally true (mixes + suggestions); rules stay the fallback; **regenerate a take to get an AI mix** (old cached mixes are rules-made). **(2) "Bring the vocal in" did nothing** between placements — the live Vocals part was the _sparse arranged_ vocal (silent outside its ~3 placement windows). Fix: `render_full_vocal` (Song 2's whole vocal, tempo-matched, from t=0) now feeds the live Vocals part, so you can bring vocals in/out **anywhere Song 2 sings** (its ~3-min length; the mix is ~8 min, so it can't cover the tail — a physical limit the founder accepted). Cache renamed `.vocalbus`→`.livevocal` to drop stale sparse caches. The Download keeps the per-bar-locked arc; `render_vocal_bus` is now unused by the app (kept + tested; remove/repurpose later). **(3) Suggestion chips felt static** — they change only at section transitions, but the analysis emitted many back-to-back same-chip sections and there are two independent playheads. Fix: `suggest_moves` merges consecutive same-chip sections; the live player shows a **"now playing: <part> · m:ss"** indicator so you can see which part the LIVE playhead is in and watch chips change at a real transition. **162 backend + 26 web green.** **Open: founder re-listen on a fresh take.** **Backlog:** remove/repurpose the now-unused `render_vocal_bus`; the continuous live vocal uses a single global stretch (may drift slightly over minutes — fine for the live approximation); the two-players UX is a design-pass item.
- 2026-07-08 — **Discovery + planning session (no code written).** Two things drove it: (a) the founder showed V0.1 to 3–4 real people who said it **"sucks / too simple"** — confirmed diagnosis: **the mix sounds basic** (not "too few songs" or "no control"); and (b) the founder pushed on whether an AI/transformer model should power the mashup. **Research (many cited web scouts) concluded, with the founder now aligned:** (1) **No open or closed model mashes up two REAL uploaded songs** — every "AI mashup" (Suno Mashup/Cover, MusicGen, Stable Audio, YuE, ACE-Step, DiffRhythm) **regenerates new audio** (real recordings gone); the one nuance is SingSong-style models that keep ONE real recording (a vocal) and generate the _other_ half — a different product, back-pocketed. So the app's **stem+DSP+LLM-plan architecture is confirmed correct**; a generative model also **kills live steering** (a baked track has no separable parts). (2) **Fine-tuning on 50–100 songs can't buy DJ judgment** — learned judgment models need thousands–tens-of-thousands of _labelled_ examples; the practical "judgment transformer" is **Claude planning over good MIR features** (already the design), tuned by **human ears** (founder → 50 testers → later a taste-scorer once thumbs-up/down data exists). (3) **Real open judgment-layer models DO exist and are worth adopting later — all perception, not generators:** **Beat This!** (MIT transformer, better beat/downbeat), Demucs-stem + energy/VAD to fix vocal detection, AutoMashUpper-style mashability scoring, Automix cue-points; **MERT is non-commercial (can't ship)**. (4) Genre lane **Techno/EDM × Bollywood locked** (proven scene; ~half of hit Bollywood is Western major/minor so it mixes without pitch-shift _if curated_). The **"both beats + both vocals" ask was reframed** (and founder agreed): not simultaneous (mud/clash) but a **DJ journey that trades/weaves both songs**. **Deliverable:** a full build plan written to **`docs/richer-mashup-proof-plan.md`** — a scoped **proof on ONE pair** (Father Ocean × Don't Start Now) of a **3-act mini DJ set** whose hero move is **Song 2 playing as itself** (its own beat+bass+vocal) via a clean bass-swap, plus a build to a peak; invariants held (one bassline / one lead vocal / on-beat / no clip). Maps of the current **ears** (`analysis.py`), **engine** (`render.py`), and **brain** (`fence/plan/validate/models/mix.py`) were taken to ground the plan; it touches the dangerous surfaces `render.py` + `validate.py` (gated). **Awaiting founder approval + 3 open decisions** (one switch vs several; add a "describe your mix" prompt now vs next — the AI driver already accepts a prompt, fed `""` today; confirm the pair). **No code changed; suite untouched (last green: 181 backend + 39 web).**
- 2026-07-08 (2nd, later) — **The judgment got real: founder-supplied reference mashups → the "House × Bollywood Recipe" (and a correction to the plan above). Still no code.** The founder gave **5 real house/techno × Bollywood mashups** (YouTube) with rich narration of what makes each good, and made a key domain point: **in this genre the house/electronic beat is the constant FOUNDATION — a DJ does NOT swap to the Bollywood beat.** This **supersedes the "Song 2 plays as itself / beat-swap" hero move** in `docs/richer-mashup-proof-plan.md` (banner added there). **The real recipe distilled from the 5 references:** (1) **Energy sync is the magic** — align the Bollywood vocal's emotion to the house track's structure: house _building up_ → Bollywood's _rising/soft_ part; house _drop/peak_ → Bollywood's _most powerful_ part (build-with-build, drop-with-peak; the founder's "perfectly synced" on both Anchor-Point mixes). (2) **On the drop use vocal CHOPS + HUMS, not full verses** ("dum da ra dum" — hook fragments/hums placed rhythmically). (3) Vocal rides **both highs and lows**. (4) Bollywood _music_ can **layer in as accents** at moments (Maula Mere), but the house beat stays the floor — this nuances the "never use Bollywood beats" line. (5) **Clean switches locked to the house build→drop.** **Two concrete new requirements surfaced:** (A) **Tempo "fast-track" fix** — the founder's #1 favourite, **Father Ocean × Tere Bina, is a pair the app currently REJECTS** for Tere Bina being too slow; we must **stretch slow songs UP** to the house tempo instead of declining (accept more warble on extreme pairs; touches `fence` decline logic, not a dangerous surface). (B) **Vocal chopping** — the engine must chop the vocal into hooks/hums and place fragments on drops (new `render.py` capability). **Founder also pushed that the app "has to listen to the music"** — resolved: the _app's_ perception (detect house builds/drops + vocal peaks) is the buildable "listening" that powers the sync; I (Claude) can't hear audio, but the founder's narration + running the references through the app's own analysis turns them into data. **Agreed roadmap:** _Phase 0 (no risky code)_ — optionally run the 5 references (+ source songs) through the app's analysis for hard build/drop/vocal-entry numbers, then write the **House × Bollywood Recipe** + a rewritten build plan for founder approval. _Phase 1 (heavy path, gated), in order:_ tempo fast-track fix → energy detection (builds/drops/peaks) → energy-sync arrangement → vocal chops/hums on drops → build/filter craft. _Phase 2:_ founder listens (ideally on Father Ocean × Tere Bina) + re-shows the 3–4 testers. **Open decision:** whether to pull the 5 YouTube references' audio for private analysis (fine for the 50-person validation, not public). **No code changed; suite untouched (181 backend + 39 web last green).**
- 2026-07-08 (3rd) — **Phase 0 written: the recipe + build plan are now real docs (still no code).** `/zuko:build`, docs-only. The founder supplied the **5 reference YouTube links** (Tere Bina, Der Lagi Lekin, Suniyan Suniyan, Maula Mere Maula, Ji Karda — all × Father Ocean) — now captured permanently in **[docs/house-bollywood-recipe.md](house-bollywood-recipe.md)** (the taste: energy-sync, chops/hums on drops, Bollywood accents, house beat as the floor; per-song rules drafted for founder correction) and **[docs/house-bollywood-build-plan.md](house-bollywood-build-plan.md)** (rewritten file-by-file plan). The old `richer-mashup-proof-plan.md` is now **FULLY superseded** (banner points to the two new docs). **Key new insight from the founder + web BPM research (this session):** the tempo fix is **NOT** "always speed the Bollywood song up to Father Ocean's 122" — it's a **movable master**: pick a shared tempo _between_ the two songs so neither warbles much (e.g. Father Ocean 122 × Tere Bina ~144 → meet at ~133 = ±8% each, in band; the app declines it today because it locks everything to 122 = −15% one-sided). This means Step 1 now also stretches the **house bed** (a `render.py` protected change), not just the vocal. **Grounded findings:** Father Ocean 122/10B; Der Lagi Lekin 111/10B (in catalog, cleanest); **Maula Mere Maula 120/C-maj≈8B — a near-perfect V1 match, not yet ingested**; Suniyan Suniyan 130 (tempo fine) but **C#-maj≈3B key clash → needs V2 pitch-shift**; Ji Karda ambiguous (120 vs 140 — needs founder ID). Plan steps: 1a target-tempo (`fence`, safe) → 1b stretch-bed (`render.py` 🔒) → energy detection (`analysis.py`/`models.py`, safe additive) → energy-sync arrangement (`fence`/`plan`, safe) → vocal chops on drops (`render.py` 🔒) → build/filter craft (`render.py` 🔒) → accents (`render.py`/`validate.py` 🔒). **Awaiting founder approval + decisions** (recipe correct? movable-master OK? run refs through the app for hard numbers? which Ji Karda? proving order?). A product-manager pressure-test of the plan was run this session. **No code changed; suite untouched (181 backend + 39 web last green).**
- 2026-07-08 (4th) — **Phase A BUILT (energy-sync, safe surfaces, TDD): the vocal now lands on the house DROP.** `/zuko:build`, branch `feat/house-bollywood-energy-sync`. Founder said "go ahead and build." Built the pressure-tested **Phase A** (energy-sync only — NO protected-engine edits, so no confirm-and-apply needed). **Two design refinements vs the plan doc, both safer:** (1) **energy detection is DERIVED, not persisted** — `fence.energy_drops` / `fence.vocal_peaks` compute builds/drops/peaks purely from the already-cached `energy_curve`+`downbeats`+`sections`, so **no `analysis.py` change, no re-analysis, works on the existing catalog immediately**; (2) **Step 1a (target-tempo math) is DEFERRED to travel with Step 1b** (it's inert dead code until the bed is actually stretched, a protected change). New `fence` functions (all pure, TDD'd): `energy_drops` (low→high onsets into sustained high), `vocal_peaks` (Song 2's loudest sung slices, loudest-first), `synced_anchors` (drop-preferring arc — one entry per third, prefers a real drop in that third, falls back to the loudest phrase-anchor so the whole-song span guarantee holds). `plan._default_arrangement` now uses `synced_anchors` + pairs Song 2's loudest peak onto the strongest anchor (a real drop beats a mere loud phrase), rotating the pairing by `take` for regenerate variety; the AI path payload/prompt gained `house_drops_seconds` + `vocal_peaks_seconds` + a synced `recommended_spread_anchors` so the AI arranges energy-synced too. **`render.py`/`validate.py`/`analysis.py`/`models.py`/`storage.py`/`songs.py`/config all untouched** (no dangerous surface, no test-harness edits). **10 new tests, all TDD (RED→GREEN): 191 backend + 39 web green, typecheck clean.** **Real-pair verification (no cloud cost):** on the actual Father Ocean analysis the app detects **4 real drops (63/236/378/441s)**; deterministic plan on Father Ocean × Der Lagi lands **every** placement ON a drop, spanning the whole 7-min song, with take 2 pulling different vocal content (Der Lagi has 0 detected vocal_regions → the sections fallback supplies the peaks). **Open acceptance: founder ear-test** on a real render (needs the engine running). **Deferred to Phase B (gated, protected):** movable-master tempo (1a+1b, unblocks Tere Bina), vocal chops on drops, build/filter craft, Bollywood accents — each with confirm-and-apply + adversarial quorum + founder ears.
- 2026-07-08 (5th) — **"The produced drop" built + a real foundational bug fixed; founder confirmed clean vocals.** `/zuko:build`, branch `feat/house-bollywood-energy-sync`. Two things landed: **(A) The produced drop (m5b.1, DANGEROUS `render.py`, confirm-and-apply + adversarial quorum).** Additive `Placement.build_bars`/`echo`; `render._build_bed` (multi-bar filter+volume BUILD into a drop, reuses `_sweep_bed`, clamped to never reach back over a prior vocal) + `render._echo` (decaying delay throw, `_guard_duration`-capped); `plan._produce_drops` marks drop placements with a build and echoes ONLY the final placement, and only when no Song-1 contrast vocal falls within a **tempo-exact** echo-tail window (R1 — a breach the referee can't see, since `placement_end` has no echo term). Gated behind `_confident`. **Referee (`validate.py`) UNCHANGED** — modeling build/echo as fields the referee ignores kept R6 (no clip / not silent) as the backstop and the only dangerous edit on `render.py`. The adversarial reviewer found **2 must-fixes** (echo tail vs Song-1 contrast R1; a 3-bar build muffling a prior vocal) — both fixed test-first and **re-reviewed → verdict `safe`**. **(B) Vocal-detection repair (m5c.1, SAFE — data).** Root cause of the founder's "vocals are short and flick mid-word": **3 of the catalog vocals (Der Lagi, Tujhe, With You) were analyzed BEFORE their vocal stem was split**, so `_vocal_regions` returned empty (`vocal_confidence=0.3`, the no-stem sentinel) and the planner fell back to crude section chunks. Recomputed `vocal_regions` for every catalog song from the now-present stems (local, no cloud): **Der Lagi 0→15 regions, longest 58s** (slices now 32–40s clean sung spans vs 13–18s chopped). `ENGINE_VERSION m5a.1→m5b.1→m5c.1`. **Founder live-confirmed the vocals now enter/leave smoothly.** **8 new tests, TDD; 199 backend + 39 web green.** **⚠️ Latent trap logged:** the analysis cache is "run once, ever" — a song analyzed pre-split keeps empty vocal_regions forever; catalog ingest MUST split-before-analyze, or add a self-heal guard (recompute when vocal_regions empty + stem present). The recomputed analyses live in gitignored `data/` (not reproducible from git). **With You only recomputed to 1 region (131s continuous) — thin variety, minor follow-up.**
- 2026-07-08 (6th) — **The House × Bollywood judgment path built through Step 2; founder ear-confirmed each move.** `/zuko:build`, branch `feat/house-bollywood-energy-sync`. Continued the same session. **STEP 1 — both vocals TRADE (DONE, founder-confirmed):** stopped always-stripping Song 1's (Father Ocean's) vocal. `fence.lead_sections` (replaces the old one-shot `contrast_windows`) finds where the house track sings a SUBSTANTIAL passage (adjacent regions merged across breaths; ≥10s kept, scraps dropped — the keep-or-drop judgment) in the gaps between Song 2's placements and lets it LEAD there; `fence.predrop_licks` KEEPS the house vocal's short LICK into each drop (the vocal-into-the-drop a DJ never cuts). One lead at a time held by the referee (R1). On the real pair Father Ocean now leads his own 37s passage at 2:41-3:18 (was a stripped 14s scrap) + licks into the 3:56 and 6:17 drops. `m5c.1→m5d.1→m5e.1`. **Then the natural HAND-OFF (founder taste calls, iterated live):** the outgoing vocal's OWN natural phrase-end decay does the blend (no engine-imposed fade — removed it), the incoming lead enters at FULL, and the outgoing vocal rings a **tunable bound** (`fence.LEAD_XFADE_SECS`, landed at 1.2s) past the entry so "…when we're alone…" decays under Der Lagi. **⚠️ DANGEROUS: this loosened the referee's R1** (`validate.py`, confirm-and-apply): it now ALLOWS a bounded hand-off overlap (Song 1 started before Song 2 AND ends ≤ the bound past the entry); more/other overlap is still rejected. `render.py` removed the imposed fade. `m5f.1→m5f.2`. **STEP 2 — echo THROWS on the drops (DONE, founder-confirmed):** `plan._produce_drops` throws an echo on EVERY drop where the echo tail is clear of the next lead (generalised the old climax-only R1 guard to each drop); then `render._echo` was reworked to echo only the LAST word(s) (not the whole slice), then further to split the vocal on its PAUSES (`render._phrase_ends`) and throw after EACH sung phrase into its own pause — "sing a line…echo…sing the next…echo." The echo tail past the vocal stays `delay*_ECHO_TAPS` (the R1 echo-tail guard bound), test-pinned. `m5g.1→m5g.2→m5g.3`. **Suite: 212 backend + 39 web green, typecheck clean.** All render/validate edits went through confirm-and-apply. **Verified end-to-end (no cloud cost) on the real Father Ocean × Der Lagi pair each iteration; founder listened to every render and approved.** **⚠️ OPEN SAFETY ITEM (re-verify before merge to main):** the R1 relaxation (bounded hand-off overlap without an engine-guaranteed fade — leans on the source's natural decay) has NOT had a clean adversarial verdict — the fade-version review found no exploit, but the founder directed the final no-fade model and stopped the re-verify. A fresh adversarial pass on the bounded-no-fade R1 relaxation MUST run before this branch merges to main. **NEXT: Step 3 — the app performs the four stems (auto drop-to-just-the-beat, pull/return the bass, beat-up, breakdown) at musical moments — the live-steering moves, baked into the arrangement.**
- 2026-07-08 (7th) — **Movable-master tempo built (unblocks far-apart pairs like Tere Bina); reordered BEFORE Step 3 at founder's request.** `/zuko:build`, same branch. **Roadmap change:** Step 3 (stem performance) is designed + committed ([spec](superpowers/specs/2026-07-08-step3-stem-dynamics-design.md)) but PAUSED; the founder chose to build the tempo fix first so his #1 pair plays. **Two founder design calls (recorded):** (a) **only when needed** — the movable master engages ONLY when the one-sided lock is out of band; every current catalog pair stays native (`bed_stretch == 1.0`, byte-identical). (b) **PROTECT THE HOUSE, not a 50/50 split** — an even geometric-mean meet was WRONG for the genre (slowing techno kills its drive); instead the house moves the MINIMUM to bring the vocal into band, bounded by `HOUSE_SLOW_MAX=0.04`/`HOUSE_SPEED_MAX=0.08` (tunable by ear), and the guest vocal absorbs the rest (sits at its band edge; a little warble accepted). For Tere Bina (144 on the grid vs 122) the house nudges UP ~5%, never slowed. **Build:** additive `MixPlan.bed_stretch`; `fence.tempo_plan` (house-protective target) + `fence.retimed_analysis` (the single grid-rescale both planner and referee call); `plan`/`fence` plan on the retimed grid; **engine (`render.py` 🔒)** pre-stretches Song 1's stems by `bed_stretch`; **referee (`validate.py` 🔒)** rescales the grid + guards the house band and master-tempo consistency; `ENGINE_VERSION m5g.3→m5h.1`. Spec/plan: [movable-master design](superpowers/specs/2026-07-08-movable-master-tempo-design.md) · [plan](superpowers/plans/2026-07-08-movable-master-tempo.md). **Heavy-path ceremony (both protected files):** independent test-author wrote the failing tests first; a **2-lens adversarial quorum** both found the SAME real must-fix — **F1:** the engine gate (`1e-3`) disagreed with the planner/referee gate (`1e-6`), so a razor-edge pair (bed_stretch in `[1e-4,1e-3)`) could ship an off-beat mix the referee couldn't see (measured drift ≤210ms) — fixed to a shared `1e-6` gate; **F2:** the referee now independently checks `master_bpm ≈ a1.bpm·bed_stretch` (was trusting the plan it judged) — added. **Re-verify → both CLOSED, no new issues.** Founder confirm-and-apply recorded/cleared. **Then a follow-on bug (`superpowers:systematic-debugging`, SAFE surface):** the forced-demo pair was DECLINED — `fence.predrop_licks` rounded the Song-1 lick end to 3 dp, which on a retimed (non-round) anchor rounds ~5e-4 PAST `a+LEAD_XFADE_SECS`, beyond the referee's 1e-6 crossfade tolerance → wrongly flagged R1 (two vocals overlap). Root-caused with instrumentation (NOT the "reach-back" hypothesis), fixed by clamping the lick end to the crossfade boundary; movable pairs now validate clean WITH the both-vocals-trade flourish. **Suite: 234 backend + 39 web green, typecheck clean.** **Machinery proof (free/cached):** forced a +5% house nudge on Father Ocean × Tujhe Bhula Diya → valid plan (bed_stretch 1.05, master 128.1, referee clean) → rendered to the Desktop for the founder's ear-test (does the stretched house beat sound clean?). **Open acceptance:** (1) founder ear-test the stretched-bed demo; (2) **Tere Bina not yet in the app** — founder to drop the audio in `song-dropbox`, then ingest (split+analyze, small cloud cost) → render the REAL pair. **Deferred/unchanged:** Step 3 (next, after this); the R1-relaxation adversarial re-verify still owed before merge to main; "always meet in the middle" for cleaner close-pairs (revisitable).
- 2026-07-08 (8th) — **Beat-lock drift fixed — the vocal now stays on the beat near stretch edges (founder ear-test on the real Tere Bina mix).** `/zuko:build`, same branch. Founder heard Tere Bina's vocal **drift off Father Ocean's beat ~4:18** in a long placement. `superpowers:systematic-debugging` root-caused it: the per-bar beat-lock (`fence.warp_map` / referee **R7**) checked each bar's stretch ratio against the **global** `SAFE_STRETCH` band [0.89,1.11]; when a pair's overall stretch sits near an edge (measured: **Tere Bina bars 0.874–0.894 at global 0.89 → 36/37 out of band; Der Lagi 1.086–1.112 at 1.099 → 2/27 out**), `warp_map` bailed to `[]` → a single global stretch that **drifts**. Diagnosis (before the fix): beat-locked placements were **Tere Bina 0/3, Der Lagi 1/3, Tujhe 2/3** — the closer to the edge, the worse (With You mid-band = 3/3, clean). **Fix:** a **wider per-bar grip band** `fence.WARP_LO=0.85`/`WARP_HI=1.15` (a per-bar re-lock is a _transient_ single-bar correction, not the sustained stretch the global band governs — which is **unchanged**, so which pairs mix / warble is unchanged), used by BOTH `warp_map` (safe) and **R7** (`validate.py` 🔒). **Heavy-path ceremony:** independent test-author wrote 5 R7 tests (2 RED); a fresh adversarial reviewer returned **`safe`** — every attack failed structurally (R7 governs only per-bar re-lock quality; the boundary-on-grid anti-drift check and R1/R2/R3/R6 are untouched; `out_secs` = Song-1 bar lengths drive the lock, independent of the ratio band; back-compat clean). Founder confirm-and-apply recorded/cleared. `ENGINE_VERSION m5h.1→m5i.1` (the rendered vocal changes). **Result: Tere Bina 0/3→3/3, Der Lagi 1/3→3/3, Tujhe 2/3→3/3 beat-locked, all validate CLEAN.** **240 backend + 39 web green, typecheck clean.** Re-rendered Father Ocean × {Tere Bina, Der Lagi} to the Desktop for the founder's ear-test. **Residual (ear-gated):** an occasional edge bar now stretches up to ~15% for ~1.7s (transient) to hold the beat — far better than sustained drift, but a human listen owns it. **Founder's clarified taste (recorded):** a long continuous vocal is fine IF it stays on the beat; the earlier "shorten/spread the vocal" idea was NOT the fix (drift was) — but non-linear/shorter placement remains a possible future taste option.
- 2026-07-08 (9th) — **Step 3 Wave 1 BUILT — the app auto-performs Song 1's beat (the bass pull-and-slam).** `/zuko:build`, same branch. The mix engine gained a "mixing board": it can ride one of Song 1's bed stems (drums/bass/other) up/down over an on-beat window. Wave 1 ships the highest-impact move — the **bass pull-and-slam**: the bass fades to silent across a produced drop's BUILD window and slams back at the anchor with the vocal (recipe §2.6 "PRODUCE, don't assemble"). Design: [step3 spec](superpowers/specs/2026-07-08-step3-stem-dynamics-design.md). **Additive by construction:** new `StemMove` model + `MixPlan.stem_moves` (defaults `[]`); `fence.stem_moves_for_drops` emits one `bass` pull (`1.0→0.0`) per produced drop, window = the real Song-1 downbeats `build_bars` before the anchor (on-beat by construction), gated on `_confident`; `plan` attaches it after `_produce_drops`; `ENGINE_VERSION m5i.1→m5j.0`. **Engine (`render.py` 🔒):** each bed stem gets a per-sample gain envelope (`_stem_envelope`, linear ramp + a ~8ms declick so the `0→1` slam never clicks); the three enveloped stems are **summed first**, then the ORIGINAL pipeline runs verbatim on the bed — so a no-move plan is **byte-identical** to the pre-Step-3 engine. **Referee (`validate.py` 🔒):** every `StemMove` must ride a real stem, land on downbeats (R3), keep gains in `[0,1]` (no boosts → can't clip), and never leave all three bed stems muted at once (an **exact** interval-intersection guard); overlapping same-stem moves are rejected; a moves-without-grid plan is flagged. **Heavy-path ceremony (both protected files):** independent test-author wrote the tests first (incl. a golden byte-identity check — removed after, since the repo keeps no `*.wav` in git — its one-time result is recorded here); a **3-lens adversarial quorum** found **2 must-fixes** — **(1)** an ordering change broke old-mix identity for multi-placement plans (deferred vocal placement skipped a later breath/sweep duck on a prior vocal's tail) → fixed by summing the enveloped stems first + restoring the original in-loop placement (re-review confirmed **byte-identical, max abs diff 0.000** across multi-placement cases); **(2)** the never-all-muted guard used a coarse 50ms sampler that missed crossing-ramp holes → replaced with exact interval math. A **focused re-review** then found **(3)** a MEDIUM latent flaw — the guard modelled a stem's gain as `min` of overlapping moves but the engine `multiplies` them, so overlapping _partial_ ducks could hide a hole (NOT reachable by Wave 1's single bass pull; a future-wave/AI-plan hazard) → fixed by rejecting overlapping same-stem moves, making the min-model exact. Founder confirm-and-apply recorded/cleared each time. **Suite: 269 backend + 39 web green, typecheck clean** (was 240 backend). **Verified end-to-end (no cloud cost) on the real Father Ocean × Der Lagi pair:** deterministic plan → 3 produced drops (1:03/3:56/6:18), 3 bass pulls, `validate_plan`+`validate_render` CLEAN, peak 0.891 (no clip), the bass slams back ~350× on each drop; rendered to the Desktop for the founder's ear-test. **Founder ear-test heads-up (from the quorum):** on real songs the source bass is already quiet in the pre-drop build, so the _pull_ is subtle — the **slam** carries the effect. **Open acceptance: founder ear-test.** **Deferred to Wave 2 (fast follow, same primitive, gated):** cut-to-just-drums, beat-up, breakdown (each a fence helper on the proven engine — no new engine surgery). The R1-relaxation adversarial re-verify is still owed before merge to main (unchanged by this step).
- 2026-07-08 (10th) — **Step 3 Wave 2 (first move) — "drop to just the beat," and Wave 1's bass move fixed to actually feel like a hit.** `/zuko:build`, same branch. Founder ear-tested the Wave-1 real render and reported it read as "only fade in and fade out." Investigated by MEASURING the real audio (not guessing): the bass genuinely did dip and slam (confirmed numerically), but the pre-existing multi-bar filter+volume BUILD (drums+other climbing quiet→loud, from the earlier "produced drop" work) is a smooth swell over the same ~6-7 seconds, so the bass's sudden snap had nothing to contrast against and got swallowed into the overall rise. **Fix — reworked `fence.stem_moves_for_drops` into a 3-stage tension arc, using the SAME `StemMove` primitive Wave 1 already built (no new engine capability):** (1) an `other` StemMove mutes the melody for `_CUT_BARS=2` bars right before the build — drums drive alone, a real "drop to just the beat" moment (the Wave-2 move the founder asked to see next); (2) the `bass` StemMove now spans the WHOLE cut+build window HELD FLAT SILENT (`gain_from=gain_to=0.0`, not a fading ramp) — guaranteed inaudible regardless of the song's own bass level, instead of depending on the source material happening to be quiet; (3) both snap back together at the anchor via the engine's existing declick. Both windows clamp against the previous placement's real vocal end, mirroring the guard the engine's own build clamp already uses. **`workers/render.py` and `services/api/app/planner/validate.py` were NOT touched** — the engine's per-stem envelope and the referee's checks (on-beat/gain-range/never-all-muted/no-same-stem-overlap) were already fully general from Wave 1's review; this is new _values_, not new code, on already-reviewed dangerous-surface machinery, so no confirm-and-apply/adversarial-quorum ceremony was needed this time. TDD: updated the Wave-1-era fence/plan tests to the new held-silent shape (they asserted the old fading-ramp behavior) + added a clamp test for a close previous vocal. `ENGINE_VERSION m5j.0→m5k.0` (bumped per the "fence rules change" convention even though the two protected files didn't move). **Verified on the real Father Ocean × Der Lagi pair:** `validate_plan`/`validate_render` both CLEAN, peak 0.891 (no clip); fine-grained (0.1s window) bass measurement shows near-silent (~0.0003-0.0008) energy through the ENTIRE 9.8s cut+build, then a jump to 0.24 in the very next 0.1s window at the anchor — a genuine unambiguous slam this time, not a gradual rise. **270 backend + 39 web green, typecheck clean.** Rendered fresh to the Desktop (`Der Lagi x Father Ocean - drop-to-beat.wav`) for the founder's ear-test. **Open acceptance: founder ear-test** — does the drop now read as a real hit? **Deferred (unchanged):** beat-up + breakdown (Wave 2's remaining two moves); the R1-relaxation adversarial re-verify still owed before merge to main.
- 2026-07-08 (11th) — **Bug fix: the "drop to just the beat" move was hollowing out the backing under Father Ocean's OWN vocal — a real defect, not a taste note.** `/zuko:build`, same branch. Founder ear-tested the 10th-entry render and reported it sounded broken ("some stem has broken"), strongly, not just "doesn't land." Root-caused by MEASURING the actual plan data (not re-explaining the design): 2 of the 3 drops (3:56, 6:17) have Father Ocean's own vocal — a lead or a predrop lick, "the vocal-into-the-drop a DJ never cuts" — running right up to the anchor (`plan.s1_vocal_regions`). The new cut+bass-hold windows (10th entry) were computed with NO awareness of `s1_vocal_regions` at all — only clamped against the previous Song-2 placement's vocal end — so on those 2 drops, the backing was stripped to silence (no bass, no melody) WHILE Father Ocean was singing, an audible hole under an active vocal line that reads exactly like a broken stem, not a produced moment. **This same gap existed in the ORIGINAL Wave 1 bass-only pull too** (it never checked `s1_vocal_regions` either) — just far less audible there, since it was a gradual ramp and never touched the melody; Wave 2's fuller, harder mute made the pre-existing gap severe. **Fix (`fence.stem_moves_for_drops`, safe surface, no render.py/validate.py touch):** takes `s1_regions` and, for any produced drop whose cut+build span overlaps ANY of Father Ocean's own vocal moments, **skips the move entirely for that drop** (no bass, no melody touch) — when his vocal is already leading into the drop, that IS the produced moment; the backing plays under it exactly as it did before Step 3. `plan.py` passes `s1_regions` (already computed by `_apply_flourishes`, just wasn't threaded through). TDD: 3 new tests (`test_stem_moves_skip_a_drop_where_song1s_own_vocal_leads_in`, `..._fire_normally_without_an_s1_overlap`, and the plan-level `test_stem_moves_never_overlap_song1s_own_vocal` — reproduces the exact founder scenario: a drop with Father Ocean singing into it, asserts no stem move ever overlaps any s1 vocal region). **Verified on the real pair:** drop 1 (no vocal nearby, 1:03) still gets the full cut+bass-hold+slam; drops 2 and 3 (3:56, 6:17 — Father Ocean singing into them) now correctly get NO stem move at all — zero overlap confirmed programmatically; `validate_plan`/`validate_render` both CLEAN, no clip. **273 backend + 39 web green, typecheck clean.** No `ENGINE_VERSION` bump needed this pass (folded into the same `m5k.0` the prior entry already bumped, not yet shipped/heard as final). Re-rendered to the Desktop (`Der Lagi x Father Ocean - drop-to-beat-FIXED.wav`) for the founder's ear-test. **Lesson recorded:** before adding any new auto-performed move, check it against BOTH vocals' real timing (`Placement`s AND `s1_vocal_regions`), not just the one list that happens to be in scope — an oversight here, now a standing check for Wave 2's remaining moves (beat-up, breakdown).
- 2026-07-08 (12th) — **Bug fix + a new standing rule: a continuous element must be LOWERED, never CUT.** `/zuko:build`, same branch. Founder ear-tested the 11th-entry fix and reported (strongly) that the music "suddenly stopped" — not vocals, the beat/melody — describing it as odd, "cannot suddenly stop... you can lower it." Measured the real audio at the exact point they were hearing (the melody cut boundary, ~0:53): confirmed a genuine defect — the melody fell from full (0.041) to near-silent (0.005) in **about half a second**, because the "cut" and "held silent" moves (built in the 9th/10th entries) both started at `gain_from=0.0`, meaning the ONLY transition into the mute was the engine's ~8ms declick — an all-but-instant snap that reads as a stem cutting out, not music fading. (A separate timestamp the founder cited, ~1:45, traced to a stable, unmodified stretch of the mix — likely the same event estimated loosely on an 8-minute track; the underlying defect was real regardless.) **Fix (`fence.stem_moves_for_drops`, safe surface, no render.py/validate.py touch):** both the `bass` and `other` moves now start at `gain_from=1.0` instead of `0.0` — a genuine linear ramp DOWN across the whole window, so the ear tracks a real decline, not a jump. The hard SLAM at the anchor (the drop itself) is unchanged and stays instant — that abruptness is the deliberate hit; the founder's rule is specifically that a hard stop is only legitimate at a real transition point the song earns, which the drop is. **New standing design rule, recorded in `fence.py`'s Step-3 header comment for Wave 2's remaining moves (beat-up, breakdown) to inherit:** every move's decline into a duck/mute must be a real ramp (`gain_from=1.0`), never a flat hold reached via the engine's declick; a hard cut is reserved for genuine musical transition points only. TDD: updated the two existing gain-value assertions (`test_stem_moves_cut_then_bass_ramps_down_on_a_produced_drop` renamed from `..._held_silent...`; `test_produced_drop_gets_a_bass_pull`) to the new `gain_from=1.0` contract — render.py's existing declick logic already supports arbitrary ramps generally (this is closer to Wave 1's ORIGINAL shape, just reused for both stems and a longer bass window), so no new render-level test was needed. **Verified on the real pair:** the melody band now declines gradually over ~4s (0.041 → ~0.01) instead of snapping in ~0.5s; the bass slam is untouched (0.0008 → 0.24 within 0.1s, still a hard instant hit); `validate_plan`/`validate_render` both CLEAN, peak 0.891, no clip. **273 backend + 39 web green, typecheck clean.** Re-rendered to the Desktop (`Der Lagi x Father Ocean - drop-to-beat-v3.wav`). **Open acceptance: founder ear-test** — does the lead-in now feel like a real fade, with the drop itself still hitting hard?
- 2026-07-08 (13th) — **Two more real silence bugs found by ear-test and fixed — one pre-dating Step 3 entirely.** `/zuko:build`, same branch. Founder ear-tested the 12th-entry fix and reported TWO spots (0:57, 3:50) going "completely silent for one second," contrasted against 6:11 which "sounds nice" (lowered, not stopped) — and was clear the fix from the 12th entry hadn't actually landed everywhere. Investigated both by measuring the real audio (not re-explaining), and found they were **two genuinely different bugs**:
  - **3:50 (drop 2's build window) — a PRE-EXISTING defect, unrelated to any Step 3 work this session.** Traced to the raw, untouched Father Ocean stems: even the DRUMS (never touched by any StemMove) are naturally near-silent (energy ~0.047-0.049) in the middle of that window — a genuine breakdown in the source recording. The **pre-existing** "produced drop" build (`_build_bed`, from an earlier session, M5b.1) applies its filter+volume climb blindly across a FIXED bar count with no awareness of whether the source has anything to build FROM — tipping an already-near-silent moment into full silence (measured: 0.001-0.002 for 1.5+ seconds). Drop 3 (6:11) doesn't have this because its own window never dips below 0.13. **Fix (`plan._produce_drops`+new `plan._safe_build_bars`, safe surface, no render.py/validate.py touch):** walks backward bar-by-bar from the anchor and stops the moment a bar's energy is at/under a new floor (`_BUILD_ENERGY_FLOOR=0.10`, chosen with margin between the measured breakdown ~0.047-0.049 and drop 3's healthy floor ~0.13) — the build SHRINKS to only the bars that actually have real energy (drop 2: 3 bars → 1), never 0 bars of real music, and if even the bar right before the anchor is too quiet, falls back to no build at all. Drops 1 and 3 are numerically confirmed unaffected (their windows never dip below the floor).
  - **0:57 (drop 1's cut→build boundary) — a real but SMALLER gap from the 12th entry's own fix.** The melody's ramp-down (12th entry) ended EXACTLY at build_start, and the engine's own ~8ms recovery declick landed at the SAME instant the pre-existing build's own quiet+muffled opening bar (`render._BUILD_GAIN_LO=0.55` + a heavy low-pass) also kicks in — two independently-mild quiet moments stacking into one more noticeable dip (measured: a real ~0.006 dip, smaller than 3:50's near-total silence but real). **Fix (`fence.stem_moves_for_drops`, safe surface):** new `fence._CUT_RECOVERY_BARS=1` — the melody's ramp now ends a full bar BEFORE build_start, giving it time to be genuinely back to full before the build's own quiet start takes over, so the two quiet moments no longer coincide. The cut steps back one bar further to preserve the full `_CUT_BARS` of genuine "just drums" time.
    TDD: `_safe_build_bars` unit-tested directly (shrinks across a breakdown, returns 0 with no runway at all, unchanged when healthy, defaults to max without data) + an integration test reproducing the exact founder scenario via `_produce_drops`; the 3 existing fence-level stem-move tests updated for the new recovery-bar offset. **Verified on the real pair, both spots:** 3:50's level now stays a healthy, continuous 0.011-0.019 (was crashing to 0.001-0.002) with drop 2's build correctly shrunk from 3 bars to 1; 0:57's dip is gone entirely (was 0.0061, now flat 0.03-0.06 through the whole region); `validate_plan`/`validate_render` both CLEAN, peak 0.891, no clip. **A full-track sweep (0.5s windows, whole ~8-minute mix) found no other near-total-silence stretch anywhere** except the last ~4 seconds — the song's own natural outro fade, expected. **278 backend + 39 web green, typecheck clean.** Re-rendered to the Desktop (`Der Lagi x Father Ocean - v5.wav`). **Open acceptance: founder ear-test** — do 0:57 and 3:50 now sound like 6:11 (lowered, not stopped)? **Lesson recorded:** given the pattern of ear-test findings this session, a full-track numeric sweep (not just the specific spot reported) is now standard practice before delivering a render for judgment, to catch sibling issues before the founder has to find them one at a time.
- 2026-07-09 (14th) — **Step 3 Wave 2's 2nd move built and founder-confirmed: "beat-up."** `/zuko:build`, same branch. Founder confirmed the 13th-entry fixes ("Cool, this is fine") and asked to move straight to beat-up. New `fence.beat_up_moves`: places ONE beat-up moment per mix — slides a `_BEAT_UP_BARS=4`-bar window across every beat-only gap (a new shared `fence._beat_only_gaps` helper, extracted from `lead_sections`'s own gap logic so both reuse one source of truth — no behavior change to `lead_sections`, its 3 existing tests re-verified unchanged) and keeps the single highest-average-energy window across the whole song that clears `_BEAT_UP_ENERGY_FLOOR=0.3` (a real intensification of something already driving, never a random dip in a quiet stretch). The `other` (melody) StemMove ramps `gain_from=1.0→gain_to=_BEAT_UP_TARGET=0.4` — matching the live "beat up" tap-button's own duck level, so the auto-placed version sounds the same as the manual one — a genuine decline, following the 12th entry's standing rule; the engine's declick carries it back to full afterward (a moderate duck's return is a small, everyday transition, not the near-total-silence case that demanded the drop-to-the-beat move's transition guards, so no gap-boundary anchoring was needed here). Never touches `bass` or `drums`; checked directly against `s1_regions` and every stem move already placed so it can never overlap Father Ocean's own vocal or a produced drop's own cut/build/bass. **`render.py`/`validate.py` again UNCHANGED** (confirmed via `git diff --stat` on both — empty) — same general machinery from Wave 1/2, only new values. `ENGINE_VERSION m5k.0→m5l.0`. **A real design bug caught during TDD, before it ever reached the founder:** the first draft anchored the window's END to the gap's own boundary (mirroring the drop move's transition-point reasoning); a test built to prove "picks the more energetic gap" technically passed but for the wrong reason (a tie-break, not real detection) — investigated, found the anchor-to-boundary design missed louder content anywhere but the tail of a gap, and reworked to a proper sliding search across the whole gap before shipping. TDD: `beat_up_moves` unit-tested directly (basic placement, energy-floor rejection, finds the loudest window mid-gap — not just at the edges, clamped away from s1 vocal / existing moves with both full-block and partial-shift cases, empty without a grid) + a plan-level integration test (`test_beat_up_move_wired_into_the_plan`). **Verified on the real Father Ocean × Der Lagi pair:** one beat-up fires at 5:13–5:21 (clear of every vocal and drop); `validate_plan`/`validate_render` both CLEAN, no clip; measured the real audio — a gentle, musical dip (mix level ~0.18→0.16, not silence) that recovers naturally afterward. **286 backend + 39 web green, typecheck clean.** **Founder confirmed ("Cool, this is fine") and asked what's next — same rhythm continues.** **Deferred (unchanged):** breakdown (Wave 2's last move); the R1-relaxation adversarial re-verify still owed before merge to main.
- 2026-07-09 (15th) — **Step 3 Wave 2's 3rd/last move built: "breakdown" — the four beat moves are COMPLETE.** `/zuko:build`, same branch. Founder confirmed beat-up ("This is cool") and asked for breakdown next. New `fence.breakdown_moves`: one breakdown per mix in the next-best energetic beat-only stretch — `drums` + `bass` both RAMP DOWN from 1.0 to `_BREAKDOWN_FLOOR=0.12` over `_BREAKDOWN_BARS=8` bars (a genuine gradual fade per the 12th-entry standing rule), leaving `other` (melody/pad) fully audible as the exposed simmer, then both return to full at the window end (the beat kicks back in on the downbeat — a return the song earns, like the bass slam). **Refactor first:** extracted `fence._best_energy_window` (the gap-slide + energy-pick + clash-avoid logic) out of `beat_up_moves` so breakdown reuses it exactly — `beat_up_moves` rewritten onto it, its 7 tests re-verified UNCHANGED (behavior-preserving); also removed a stray dead `return moves` line left in `beat_up_moves` from an earlier edit. **A real DSP-design decision, reasoned through before coding:** a "proper" breakdown wants a sustained low section (down-ramp → hold → up-ramp), but ANY two adjacent same-stem moves create a ~16ms `floor²` notch at their junction (the engine's per-edge declicks both fire and multiply) — exactly the kind of artifact the founder has been sensitive to. Chose instead **one down-ramp per stem with a hard return at the end** (the beat kicks back in — a transition the song earns, no seam), which is artifact-free by construction; a sustained-hold version is a future option only if the founder wants deeper space. `other` is never touched, so never-all-muted holds by construction. Distinct from beat-up: ducks the BEAT not the melody, deeper (0.12 vs 0.4), longer (8 bars vs 4); passed the running `stem_moves` so it lands clear of the beat-up and every produced drop. **`render.py`/`validate.py` UNCHANGED** (confirmed empty `git diff --stat`). `ENGINE_VERSION m5l.0→m5m.0`. TDD: `breakdown_moves` unit-tested (drums+bass together / melody untouched / gradual fade, energy-floor rejection, avoids existing moves + s1 vocal, never-all-muted through the REAL referee) + a plan-level integration test running the full plan through `validate.assert_plan`. **Verified on the real Father Ocean × Der Lagi pair:** breakdown at 5:42–5:58 (measured bass fading 0.14→0.023 over the window then returning; the melody holds the mix up throughout — never silent); the beat's return has NO click (boundary jump 0.72× the mix's typical sample-to-sample motion); no clip; a full-track sweep found no near-silence anywhere except the song's natural 7:52 ending. **291 backend + 39 web green, typecheck clean.** Rendered to the Desktop (`Der Lagi x Father Ocean - breakdown.wav`). **Open acceptance: founder ear-test.** **Step 3 = all four beat moves DONE** (bass pull-and-slam, drop to just the beat, beat-up, breakdown). **Next up:** vocal chops on the drops, then the AI taste layer. **Deferred (unchanged):** the R1-relaxation adversarial re-verify still owed before merge to main.
- 2026-07-09 (16th) — **⚠️ IN FLIGHT, NOT FULLY VERIFIED: Step 4 "vocal chops on the drop" built, but its independent safety review did NOT return this session.** `/zuko:build`, same branch. Founder consented to the careful path ("Yes, build it") — this is the first move that needed a NEW engine ability (a `render.py` 🔒 change), unlike the four beat moves which reused existing machinery. Built: additive `Placement.chop: bool` (models, safe); `render._chop_pattern(voc, out_len, bpm)` (🔒) re-fires the vocal's first `_CHOP_SECS=0.22s` onset every `_CHOP_INTERVAL_BEATS=0.5` beat (8th notes) to fill exactly `out_len` samples; in `render_mix` the placement loop does `if p.chop: k=min(bar,len(voc)); voc[:k]=_chop_pattern(voc[:k],k,master_bpm)` — **replaces the vocal's FIRST BAR only, so the placement length is unchanged and `fence.placement_end` / the referee's R1 overlap math are untouched → `validate.py` NOT edited** (dangerous footprint is render.py alone). `plan._flag_chop_on_biggest_drop` sets `chop=True` on the one produced drop sitting on Song 1's loudest bar (a single showcase moment; recipe R2). `ENGINE_VERSION m5m.0→m5n.0`. **What IS verified (mine, not independent):** the whole suite is green WITH the change — **293 backend + 39 web, typecheck clean**; a synthetic smoke test showed a no-chop plan renders BYTE-IDENTICAL (0.0 diff) to the field-absent plan, the chopped bar has 8 rhythmic onsets (the 8th-note stutter), no clip; the REAL Father Ocean × Der Lagi pair rendered CLEAN (`validate_plan`/`validate_render` both CLEAN, peak 0.891, no clip), chop landing on the biggest drop at **3:56**, copied to the Desktop (`Der Lagi x Father Ocean - vocal-chops.wav`). **What is NOT done (why this is IN FLIGHT):** (1) the **independent test-author** (writing `test_render.py` chop tests) and the **adversarial safety reviewer** were dispatched and were STILL RUNNING when the session was closed — neither verdict came back, so the dangerous-surface change has NOT had its independent review; (2) the founder has NOT ear-tested it. **This work is committed to the branch WITH this in-flight flag purely to preserve it cleanly — it must NOT be trusted or merged until the review completes.** The `render.py` approval was recorded + cleared. **Do-first next session: finish the vocal-chops safety review** (re-dispatch the test-author + adversarial reviewer, fold in `test_render.py`, address any findings), THEN founder ear-test. **Deferred (unchanged):** the AI taste layer (Step 5); the R1-relaxation adversarial re-verify still owed before merge to main.
- 2026-07-09 (17th) — **Vocal-chops (Step 4) independent safety review COMPLETED — the dangerous risks are DISPROVEN; two audible-quality findings recorded; safety tests landed. `render.py` NOT touched.** `/zuko:build`, same branch. Re-ran the in-flight review as a fresh-agent quorum: an independent test-author + a **3-lens adversarial review** (correctness/length-invariant, audio-safety/clipping, edge-inputs/crashes). **CLEARED (proven, not assumed):** (1) **cannot clip** — the final peak-normalize in `render_mix` runs unconditionally AFTER the chop and before write, so any stacked chop transient physically cannot clip (`validate_render` R6 reads the real WAV); (2) **cannot break the length/R1 overlap math** — the chop replaces only bar 1 and `_chop_pattern` returns exactly `out_len`, so `len(voc)`/`placement_end` are unchanged; R1 computes overlap from plan fields only, structurally independent of `chop`; (3) **byte-identical no-op** — a plan without the flag skips the block entirely; (4) **cannot crash as wired** — every degenerate input (empty/1-sample/mono voc, bpm≤0, out_len≤0) was executed; the two isolated crashes found (negative `out_len` → `np.zeros` before the guard; mono voc → `_edge_fade` broadcast) are UNREACHABLE via the only call site (`k=min(bar,len(voc))` gated `if k>0`; voc always stereo via `always_2d=True`). **TESTS LANDED (safe surface — `services/api/tests/test_render.py`, NOT a dangerous glob):** 7 new tests (length-exactly-`out_len`, render-with-chop==render-without same length, chop-absent byte-identical to chop=False @0.0 diff, multiple rhythmic onsets in the chopped bar, no-clip, edge-case gracefulness), mutation-verified (a deliberately broken `_chop_pattern` is caught). **300 backend + 39 web green, typecheck clean** (was 293 backend). **TWO QUALITY FINDINGS surfaced by the fresh eyes — NOT caught by any automatic check (`validate.py` sees only clip + total-silence, never loudness or a dead section), so recorded here for the founder's ear-test to judge:** **(A) loudness-gutting above ~136 BPM** — chop fragments (`_CHOP_SECS·SR`=9702 samples) only overlap/stack when the 8th-note `step` < fragment, i.e. `master_bpm > 136.4`; then the stacked transient dominates the peak and the whole mix is normalized ~6 dB quieter. **The curated catalog is 121–128 BPM so this does NOT bite today, but nothing in CODE bounds it** — it leans on the hand-picked list; a double-time tempo misread (e.g. 72→144) would trip it. **(B) dead chop** — the fragment is always the vocal take's first 0.22s; if that slice is a lead-in breath/near-silence, the drop's first bar sounds empty while validation still passes. **Also two cheap latent-hardening notes (not live bugs today, gated by caller discipline):** `_chop_pattern`'s no-op branch returns `voc[:out_len]` which is < `out_len` if a future caller passes `out_len>len(voc)` with bpm≤0 (make it return exactly `out_len` unconditionally); move `np.zeros((out_len,2))` below the `out_len<=0` guard. **Verdict aggregation (default-unsafe): the change is structurally safe + now test-covered, but the audio-safety lens returned `not-proven-safe` on the two quality paths → surfaced to the founder.** **Founder decision: "I listen first, then decide"** — NO engine edit made this pass; the tests are landed; founder to ear-test the existing render (`Der Lagi x Father Ocean - vocal-chops.wav`, chop at **3:56**, current since `render.py` is unchanged) for (A) overall loudness and (B) a dead/empty first bar on the drop, then decide whether the small graceful-fallback hardening is worth doing. **Deferred (unchanged):** the AI taste layer (Step 5); the R1-relaxation adversarial re-verify still owed before merge to main.
- 2026-07-09 (18th) — **Vocal chops PARKED (founder decision) — Step 4 set aside; moving to Step 5.** Founder ear-tested the 3:56 chop and it landed exactly on finding (B): the chop's source fragment was a breath, so the drop went DEAD for ~2-3s. Founder's call: park the chop (it's a flourish, not core) and move to Step 5 (the AI taste layer), which matters more. **Parked cleanly, safe surface only** (`plan.py`, no `render.py`/`validate.py` touch): the `_flag_chop_on_biggest_drop(placements, a1g)` call in `build_mix_plan` is commented out, so no plan ever flags a chop and no mix can go dead on a breath. **All the machinery stays in the tree, dormant + tested** — `render._chop_pattern` (+ its 7 `test_render.py` tests from the 17th entry), `Placement.chop`, and `_flag_chop_on_biggest_drop` — so reviving is a one-line re-enable. The plan-level test was repurposed (`test_vocal_chop_is_parked_but_the_function_still_works`): it asserts the plan flags NO chop, then calls the helper directly to prove it still works for a future revival. **Revival condition (recorded):** only bring the chop back once it grabs a _punchy syllable_ (skip breaths/near-silence, find the strong onset in the first bar) instead of the raw first 0.22s slice — otherwise the dead-chop returns. **300 backend + 39 web green, typecheck clean.** **Next: Step 5 — the AI taste layer.** **Deferred (unchanged):** the R1-relaxation adversarial re-verify still owed before merge to main.
- 2026-07-09 (19th) — **`/zuko:goodnight` batch: 9 of 10 requested mashups rendered on the deterministic engine (no code change).** Founder supplied 10 beat×vocal pairs (6 new catalog songs added to `song-dropbox`) and asked for a full unattended render run using everything built EXCEPT the AI judgment layer. Ran via a standalone orchestrator (`scratchpad/goodnight_batch.py` + `goodnight_fix.py`) that mirrors `routes/mix.py` offline: per song `normalize → store → separate_stems → analyze_track` (all cache-aware, cloud via Replicate), then per pair `build_mix_plan → assert_plan → render_mix → assert_render` with **`ANTHROPIC_API_KEY` popped** so the arrangement takes the deterministic rules path (founder's explicit choice; no Claude cost). Output → `Desktop/DJAI SONGS`. **Result: 9 rendered** (01 FO×Der Lagi 122, 02 FO×Suniyan 122, 03 FO×Tere Bina 127 [movable master engaged], 04 FO×Tujhe 122, 05 Anchor Point×Dil Ye Bekarar 122, 06 Innerbloom×Dooriyan 122, 08 Adore You×Tujhe 120, 09 Anchor Point×Jee Karda 125, 10 Anchor Point×Maula Mere 122); **1 declined** (07 Tumhi Ho Bandhu×CHANEL — ~17% tempo gap, beyond the house-protective stretch bound — a genuine incompatibility, correctly refused). **Corrective pass folded in:** pairs 05 and 08 first hit transient cloud errors (a read-timeout and a 502) → retried, both succeeded; and Suniyan + Anchor Point had **stale analyses (analyzed before their stems existed → empty vocal_regions, the m5c.1 trap)** → deleted + recomputed with the vocal stem present, affected pairs re-rendered. **No dangerous surface touched** (no `render.py`/`validate.py`/etc. edits — a pure production run using the shipped engine `m5n.0`); the vocal chop stayed parked. New songs' stems+analyses are now cached (gitignored `services/api/data/`), so re-renders are free. Morning report at [.zuko/goodnight/report.md](../.zuko/goodnight/report.md). **Open acceptance: founder ear-test the 9 mashups** — the 4 Father Ocean pairs are the safe reference; the 5 new-beat pairs (05/06/08/09/10) are first-time-through-the-app and unproven (analysis quality unknown), so their quality is the founder's call.
- 2026-07-10 (20th) — **PHRASING built — every change now lands on a musical phrase line (safe surface).** `/zuko:build` (full heavy path: brainstorm → spec → plan → TDD). Founder's DJ-phrasing rule (from the Lawrence "mix any genre" video + the Anchor Point diagnosis): changes must land on the "turn of phrase," never mid-phrase (bar 5/6) which sounds abrupt. **Design (founder-approved):** big changes on the **8-bar** grid, small on the **4-bar** grid; **phrase wins** over a slightly-off detected drop. Spec + plan: [phrasing-alignment design](superpowers/specs/2026-07-10-phrasing-alignment-design.md) · [plan](superpowers/plans/2026-07-10-phrasing-alignment.md). **Built:** new `fence.snap_to_phrase(t, downbeats, bars)` ruler (nearest of `downbeats[::bars]`; ties round UP to the upcoming turn, never back to the song start; empty-grid → no-op). Applied: (1) **`fence.energy_drops`** pins each detected drop to the nearest 8-bar phrase line (+dedupe) → propagates through `synced_anchors`/`_default_arrangement`, and `_default_arrangement` re-snaps anchors as a belt; (2) **`fence._best_energy_window`** only considers windows whose start is on the 4-bar grid → beat-up (4 bars) and breakdown (8 bars) both land, duck AND return, on 4-bar lines. **Deliberately NOT snapped (a refinement from the code reading, spec-updated):** the produced-drop **build** ramp AND the "drop to just the beat" **cut** ramps are gradual fades into the already-8-bar-aligned slam — not abrupt, and snapping a 2-bar cut to the 4-bar grid collapses it for no audible gain; leaving them also keeps the change off `render.py`. **Safe surface only** — `fence.py`/`plan.py` + `routes/mix.py` (version); **`render.py`/`validate.py` UNTOUCHED** (phrase lines are downbeats, already accepted by R3, confirmed by an upfront code-map investigation). `ENGINE_VERSION m5n.0→m5o.0`. **TDD, 5 tasks, each committed:** the ruler; 8-bar entries/drops; 4-bar beat-up/breakdown; (cut = no-op after the ramp refinement); integration guard. **Two pre-existing test interactions handled honestly (recomputed, never weakened):** a drop midway between phrase lines now rounds up (tie-break added); regenerate variety now shows in vocal content when two takes share phrase lines. **307 backend + 39 web green, typecheck clean** (was 306). **Verified on the REAL Anchor Point pairs (local/cached, no cloud cost):** 05/09/10 all — every vocal entry on the 8-bar grid, beat moves on the 4-bar grid, `validate_plan`/`validate_render` CLEAN (incl. #09 Jee Karda on the movable-master retimed grid). Re-rendered 05/09/10 to `Desktop/DJAI SONGS`. **Open acceptance: founder ear-test the 3 Anchor Point mixes** — are the abrupt changes gone? **Next in the DJ-judgment cluster:** hook-on-drop, then strip-beat-vocal.
- 2026-07-10 (21st) — **PHRASING REVERTED (founder decision, same day) — it pulled the vocal OFF the real drop.** Founder ear-tested the phrasing renders and heard the vocal entering BEFORE the drop. `superpowers:systematic-debugging` → **measured + confirmed the root cause:** the 8-bar phrase grid was counted from bar 0 (`downbeats[::8]`), but a song's real phrase structure is OFFSET by its intro. Anchor Point's real drops sit at bars **43/61/97/121/139** (none a multiple of 8), so snapping each drop to the nearest fixed-origin 8-bar line pulled every vocal entry **1–3 bars off the real drop** (the first drop **84.0s → 78.1s, 3 bars early** = the vocal before the drop the founder heard). The snap-to-a-fixed-origin-grid approach was simply wrong. **Founder chose to remove phrasing entirely** and put the effort into hook-on-drop instead. **Reverted** `fence.py` / `plan.py` / `routes/mix.py` + the three test files to the pre-phrasing state (`115ed1f`) — `snap_to_phrase` and every phrasing edit gone, `ENGINE_VERSION` back to `m5n.0`, **300 backend + 39 web green**. The design/plan docs ([spec](superpowers/specs/2026-07-10-phrasing-alignment-design.md) · [plan](superpowers/plans/2026-07-10-phrasing-alignment.md)) are kept as a historical record. **LESSON (if phrasing is ever revisited): the phrase grid must be ANCHORED TO THE MUSIC — to a detected drop or the first strong downbeat — never counted from bar 0.** **Next: hook-on-drop** — land each song's signature hook (_"Jee karda", "Dil ye bekaraar kyun hai", "Aankhein teri kitni haseen"_) ON the real drop.
- 2026-07-10 (22nd) — **HOOK-ON-DROP built (deterministic path) — the drop now plays the signature line, not the loudest blob.** `superpowers:systematic-debugging` had shown the app picked the drop vocal by loudness (`fence.vocal_peaks`), which grabbed the wrong bit; and the cached `vocal_regions` are too coarse (huge blobs) to isolate the hook — but the **section map** (labelled verse/chorus) locates it. New `app/planner/hooks.py`: a per-song-id `HOOKS` dict marking the signature slice (read off the section map + song knowledge, confirmed by ear), with `hook_for(song_id)`. `build_mix_plan` sets `opts["hook"]`; **`plan._default_arrangement`** lands the hook on the STRONGEST anchor (the drop) and gives the other entries the SETUP (other vocal parts, never the hook again, rotated by `take` for regenerate variety). **Additive + SAFE surface** (`plan.py` + new `hooks.py`; no `render.py`/`validate.py`/`fence.py` touch); a song with no marker falls back to the old loudest-peak pick. Seeded 3 hooks (best guesses from the section maps): Dil Ye Bekarar `(35.5,56.9)` "Dil ye bekaraar kyun hai" (1st chorus); Jee Karda `(55.0,68.7)` "Jee karda" (1st chorus); Maula Mere `(28.9,51.9)` "Aankhein teri kitni haseen" (**best guess — verse after the opening refrain — needs ear-confirm**). **302 backend + 39 web green.** Verified on the real pairs: the drop placement's `vocal_src` now starts exactly on the marked hook (05→35.5, 09→55.0, 10→28.9); `validate_plan`/`validate_render` CLEAN. Rendered 05/09/10 to `Desktop/DJAI SONGS` as **"(hook on drop)"** (plain loudest versions kept for A/B). **Open acceptance: founder ear-test — is the right line on the drop?** (esp. Maula Mere). **Then:** refine the setup-entry selection (verses, not coarse loudest blobs) and strip-beat-vocal.
- 2026-07-10 (23rd) — **Hook fine-tuning + master recipe doc (founder-directed).** (a) **Dil Ye Bekarar hook corrected:** the 35.5s chorus-boundary mark landed mid-phrase ("…kyun ye…"); founder gave the real start of "Dil ye bekaraar kyun hai" = **42.0s** → `hooks.py` updated, re-rendered. (b) **Anchor Point × Jee Karda pairing REMOVED** (founder: doesn't work together) — files deleted; Jee Karda's hook kept for other pairings. (c) **Strip-beat-vocal REJECTED** (see 22nd/memory) — the natural fade-underneath hand-off is correct, not clutter. (d) **New master recipe doc: [docs/mix-recipe.md](mix-recipe.md)** — the single plain-English rulebook for how a mix is made (tempo → vocal placement → hook-on-drop → beat-lock → hand-off → drop production → guardrails), plus the OPEN items (vocal "play"/production to fix the "too plain" feel; cleaner setup parts; which-beat-suits-which-vocal) and the lessons (chop parked, phrasing reverted, hook-on-drop + hand-off confirmed). Founder's stated **#1 open quality gap: the mixes feel "too plain" — the vocal needs "play"/production and more energy outside the drop** (the real YouTube mixes process the vocal, e.g. Maula Mere's soft voice made harsher). **Next build: TBD** (founder chose the recipe doc this round; the production/"less plain" work is the likely next, needs scoping vs. the light-touch non-goals).
- 2026-07-09 — **GOOD-PARTS WINDOW BUILT (founder ear-confirmed on real Anchor Point × Maula Mere) — mixes are now built on the beat's best ~90s, not the whole track.** `/zuko:build` (full heavy path: brainstorm → spec → plan → TDD). Design + plan: [good-parts-window design](superpowers/specs/2026-07-09-good-parts-window-design.md) · [plan](superpowers/plans/2026-07-09-good-parts-window.md). Directly targets the founder's #1 open note from the 23rd entry ("mixes feel too plain," long songs feel empty): the arrangement engine built across the WHOLE beat song, so a 7–9 min beat made a 7–9 min mix with long dead beat-only stretches (measured: Anchor Point 5.8 min → 1.9-min gaps; Innerbloom 9.2 min → 3.0-min gaps). **Windowed-canvas approach:** new `app/planner/window.py` (`choose_window` picks a ~90s, 60–120s-flexible window anchored on the beat's MAIN/loudest drop, starting on a low-density CUE POINT — a phrase boundary at a breakdown/quiet spot, never mid-chorus; `window_analysis` crops+rebases the analysis grid to the window, 0-based; `windowed_options` re-derives the arrangement menu on that windowed grid) — `build_mix_plan` (`plan.py`) re-grids onto the window when confident, and the ENTIRE existing arrangement engine (energy-sync, hook-on-drop, produced drops, flourishes, stem moves, beat-lock) then runs UNCHANGED on the windowed grid; no new arrangement logic was needed. **Fallback-to-full-track invariant:** no confident main drop (empty/low-confidence, no legal cue with run-up, track too short) → `choose_window` returns `None` and the mix renders exactly as it does today — never worse than before, held by construction and locked by a regression test. **Dangerous-surface edits (both went through the heavy path — independent test-author + adversarial-safety-reviewer quorum + founder confirm-and-apply):** `workers/render.py` crops the (already `bed_stretch`-applied) decoded bed to `[win_start, win_end]` before the stem-envelope sum, with a **fail-loud guard** — a malformed/reversed window raises `RenderError` instead of silently shipping an empty, beat-less bed (the exact silent-bad-mix gap the adversarial review flagged, closed before merge); `plan.window is None` stays byte-for-behaviour identical to today. `services/api/app/planner/validate.py` — the referee is handed the FULL Song-1 grid (as the real pipeline does), but a windowed plan's anchors/warp are window-relative, so `validate_plan` now re-derives the SAME windowed grid the planner used (via `window_analysis`) before judging on-beat/warp/moves; without this, a real, tempo-drifting grid falsely rejects every windowed mix as off-beat. This was the last piece of the feature to land, closed by a hermetic drifting-grid regression test (`test_validate_plan_clean_on_a_windowed_plan`). `MixPlan.window: tuple[float,float]|None = None` is additive (old cached plans still parse). **Founder ear-fixes (same session, `plan.py`, safe surface):** the hook was landing on an arbitrary drop within the window rather than the loudest one — `_drop_energy` now picks the MAIN/loudest drop for the hook, matching the window's own anchor; the beat now extends `_OUTRO_SECS=12s` past the last vocal (clamped to the real track end) so a windowed mix winds DOWN into an outro instead of stopping cold on the hook or ending abruptly the instant the last placement finishes. **Verified end-to-end (no cloud cost)** on real cached pairs; **founder ear-confirmed on Anchor Point × Maula Mere** (tight ~90s good part, real build-up, hook on the main drop, eases out instead of cutting cold). **Logged, separately flagged (not fixed this pass):** the key-compatibility check is too strict — it scored **Anchor Point × Maula Mere as incompatible**, even though the founder confirms by ear that the pair works; the check needs loosening/revisiting as its own follow-up, not blocking this feature. **Living docs updated same change:** functional-spec (good-parts window folded into the "TODAY" narrative; open assumption #3 marked superseded), technical-spec (the `window.py` module + windowed-canvas architecture), mix-recipe (the good-window rule).
- 2026-07-09 (24th) — **MULTI-SONG SETS (engine) + VARIATION + drop-detection fix — the founder's two headline features landed to near-complete; app-wiring is what's left.** `/zuko:discover` → design → `/zuko:build`. (a) **SETS engine:** new `workers/set_render.py::assemble_set(mix_wavs, out, xfade_secs=4.0)` — equal-power (sin/cos) crossfade join of ANY number of finished mashups into one continuous set (peak-normalized + clip-guarded like the master; single-mix passthrough; empty→`SetRenderError`). Transition style = **wind-down → cue-in** (founder decision; no beatmatched overlap — forgiving of tempo/key mismatch, avoids two-lead-vocal clash; DJ-research-backed). 7 TDD tests (length, seam-continuity, **equal-power non-tautology-verified** against a linear-fade fake, no-clip, passthrough, 3-mix order-by-FFT, empty-raises). **Unlimited chaining PROVEN** (a real 3-mashup set rendered — the design's "~2-4" was only a testing suggestion, no code cap). New file, NOT a dangerous glob; `render.py`/`validate.py` untouched. Design: [multi-song-sets design](superpowers/specs/2026-07-09-multi-song-sets-design.md). **LEFT:** `/set` API + set-builder screen (queue mashups, "Make my set", play, export). (b) **VARIATION (founder: "a DJ never plays it 100% the same"):** `window.choose_window(a1, drops, take)` now rotates the window across the genuinely-strong drops (≥60% of the hardest hit) by `take`, so every take lands on a DIFFERENT real drop; vocal-slice/cue variety already keyed off `take`, so the whole mix varies. Founder-confirmed on Father Ocean × Der Lagi (take 1→6:17, **take 2→3:56 = "the perfect one"**, take 3→1:02). **Decision: always-fresh by default + a "keep/lock this one" for keepers — LEFT (app-wiring):** the app still calls `take=1` on first play (regenerate already varies); default-fresh + the lock/persistence is the next small build. NOTE: variation changed the DEFAULT take — the earlier founder-loved Anchor Point window (3:55 drop) is now `take=2`. (c) **DROP-INTENSITY FIX (founder ear-note: Father Ocean's 3:56 main drop was being skipped):** new `window._drop_intensity` ranks a drop by POST-ONSET energy (the bars AT/after the hit), not the phrase-aligned average that buried dramatic drops under their own near-silent breakdown run-up (Father Ocean 3:56 scored **0.37 → 0.885**; the flatter 7:20 that was wrongly picked drops to last). `_phrase_energy_at` kept for CUE selection. Safe surface (`window.py`/`plan.py`); 2 new tests. **CORRECTION to the good-parts entry's key-compat note:** the key-compatibility check is **NOT a blocker and never auto-rejected pairs** — the app declines only on TEMPO; `key_fit`/`camelot_fit` is informational (fed to the AI as context, "Informational in M3"). The earlier "it's why it rejected the pair" was a mistaken read (that was a demo-picker script's own preference). `camelot_fit` ±2 flagging Anchor Point 8A × Maula Mere 5A is cosmetically misleading but harmless; optional future tweak, not a bug. **332 backend + 39 web green, typecheck clean** (was 302 backend). Founder chose **pause + handoff** here; resume order in [zuko-handoff.md](zuko-handoff.md): variation-in-app → set-builder → mastering/clip-export → drive the running app end-to-end → the ~50-creator test.
- 2026-07-09 (25th) — **"Final test" set for 4 pairs — a production/diagnosis session; NO app code changed (git clean), one important root cause found.** `/zuko:start` → build. Founder asked for a continuous 4-mashup set of **Father Ocean×Tere Bina, I Adore You×Tujhe Bhula Diya, Innerbloom×Dooriyan, Rapture×How Deep Is Your Love** ("the final test"). All work done in **session-scratch scripts** (`scratchpad/build_set.py`, `set_pro.py`) mirroring `routes/mix.py` offline — **the shipped engine (`m5o.0`) was never edited; `git status` is clean.** (a) **2 new songs processed + cached** (gitignored `services/api/data/`, via Replicate, free from here): Rapture `7f0b66c9`, How Deep Is Your Love `4e246293`. Their 44.1k WAVs are large (Rapture 82 MB); the whole-file Replicate upload stalled/disconnected → **fix (scratch only): upload a compact 192 k MP3 of the same normalized audio to the cloud models, keep the full WAV for local key/energy/vocals** — identical content/timing, ~10 MB transit. Worth porting into `app/audio/stems.py`+`analysis.py` if uploads keep failing on long tracks. (b) **Explored the good-parts window vs the set:** founder's direction moved from a windowed set → to **full-length individual mashups** ("good parts not working; do the full song, better judgment"). A set-assembly prototype (`set_pro.py`: tempo-lock all to one BPM + beat-align each seam by onset-energy correlation + volume/filter intro build) was written to fix "beats clash at the transitions" and "opener bursts in" — it works numerically (opener ramps 0.003→full, seams beat-trimmed) **but was NOT adopted** (founder pivoted to individual mashups) and is NOT in the product. `workers/set_render.py` is still just the plain 4 s crossfade (no beatmatch) — the real set engine, if pursued, needs the tempo-lock/beat-align logic. (c) **⭐ ROOT CAUSE (verified, `superpowers:systematic-debugging`): the founder's "great this morning, bad now" regression was NOT a code change — it was the AI vs rules arrangement path.** The mixes were rendering via the **AI/LLM path** (`_ai_arrange`, active whenever `ANTHROPIC_API_KEY` is set), while every loved render (incl. this morning's `08 - Adore You x Tujhe`) was built with **`ANTHROPIC_API_KEY` popped → the deterministic RULES engine**. Proof by sample-correlation on I Adore You×Tujhe: **rules-path render == the morning file (corr 1.000)**; AI-path == a different arrangement (**corr 0.919**); two AI runs identical (1.000, so AI is deterministic, not random — it just arranges differently and the founder likes it less). Every post-morning commit was independently cleared (phrasing fully reverted; hook-on-drop a no-op for no-hook pairs; drop-rank/vary window-only). **Fix = build on the rules path** (pop the key), which reproduces the loved version exactly. **4 full-length mashups (rules path, both vocals) delivered** to `Desktop/DJAI SONGS` as `FINAL TEST 1–4 …(full).wav`; freshly rendered (timestamps + the new Rapture pair, which has no morning counterpart, prove it's not a paste). **⭐ OPEN PRODUCT DECISION (founder's call, flagged): the app should probably DEFAULT to the rules path** — the AI arrangement is the one the founder consistently dislikes, yet the app uses it whenever the key is present. **332 backend + 39 web green, typecheck clean** (re-run fresh at handoff; unchanged — no code moved).
- 2026-07-10 (27th) — **PHASE 0 (Vocal Chain) STARTED — Slices 1 + 2a BUILT (the safe win + the invisible groundwork): the app now defaults to the loved RULES arrangement, key-fit is logged, the cache is tuning-ready, and the declarative vocal-processing timeline + pitch-repair math are laid down OFF. Safe surface only; render.py/validate.py UNTOUCHED.** `/zuko:build`, same branch. Working from the founder's detailed Phase-0 brief (`phase0_prd_and_architecture.md` + the "Merry Go" refinement). Full plan: [phase0-vocal-chain-plan.md](phase0-vocal-chain-plan.md). **Two mandated verifications ran FIRST (both confirmed the brief's warnings):** (a) ffmpeg's `rubberband` filter IS present AND has the `formant` option — but its default is `shifted` (the chipmunk mode); Phase-0 pitch will pass `formant=preserved` + `pitchq=quality` explicitly, never the default (so "we have rubberband → no chipmunk" is only true when we say so). (b) ffmpeg has **no native reverb** — only `aecho` (an echo) and `afir` (convolution, needs an IR); stage 8 will use a deterministic numpy-convolution reverb (golden-file-friendly, no binary asset), not a silent echo substitute. **Built (Slice 1, all NON-dangerous files — `plan.py`/`fence.py`/`models.py`/`routes/mix.py`; `render.py`/`validate.py` UNTOUCHED):** (1.1) **`plan.USE_AI_ARRANGEMENT` flag, default False** — `_ai_arrange` no longer runs on mere `ANTHROPIC_API_KEY` presence; every mix now uses the deterministic `_default_arrangement` the founder's loved renders came from. `_ai_arrange` kept in-tree as the future menu-selection seat (not deleted). This resolves the standing #1 open decision (25th/26th entries: "the app should DEFAULT to rules"). (1.2) **`camelot_fit` attached + logged on every mix** (`models.CamelotFit`, `fence.camelot_detail`, logged in `_run_mix`) — informational, **never gates** (a clashing pair still builds); lets us see how many "good" pairs quietly key-clash before Slice 2d turns the bouncer into a repairman. (1.3) **`ENGINE_VERSION m5o.0 → m6.0`** so cached AI-path mixes re-plan on the rules path. (1.4) **`chain_config_hash` folded into `mix_id_for`** (`models.VocalChainConfig` + `chain_config_hash`, both shipped now so the hash is stable from the start) — during the tuning week each dial change will yield a fresh render instead of a stale cache (a day-of-non-bug-debugging trap, closed up front). **`VocalChainConfig` lives in `models.py`, deliberately NOT the dangerous `config.py`.** **Zero cloud cost confirmed:** stems + analysis are keyed by `song_id` and never read `ENGINE_VERSION` (grep-verified in `app/audio/`), so the bump triggers **zero Replicate calls** — only a one-time CPU re-render of cached mixes. **TDD:** 4 existing AI-path tests updated to opt the flag on (not weakened — the AI path is now opt-in); +9 new tests (off-by-default guarantee incl. `_ai_arrange` never invoked; camelot attached/compatible/clash-not-gated; chain-hash stable+dial-sensitive+additive; mix-id folds the hash; route carries camelot_fit). **341 backend green** (was 332), web untouched (39). **Slice 1 classified LIGHT** (single reversible flag + additive fields). **Then Slice 2a (declarative scaffolding, still LIGHT, zero audible change) BUILT same session:** `models.VocalProcessMove` (the G5 timeline instruction — the `StemMove` pattern extended to vocal DSP: `placement_id` + `start_bar/end_bar` + per-stage dials + a `reason` field for later cross-adaptive processing) and `models.DuckMove` (stage 9, bed-side, keyed by the placed vocal); additive `MixPlan.vocal_moves`/`duck_moves` (default `[]` → today's plain vocal, byte-identical); and `fence.compute_pitch_repair(bed, voc, cfg) -> PitchRepair | Decline` — **written, NOT called** (Camelot pitch-class arithmetic: shortest legal semitone shift into key-compatibility, `low_confidence` at ±3, decline beyond the cfg cap; mode is fixed by pitch-shifting so it can only reach same-letter compatibility, never the relative — carries the load-bearing "arithmetic, not taste" docstring). +8 new tests (pitch-repair no-shift/small-fix/cross-letter-decline/no-key/pc-roundtrip; move round-trips + no-op dial defaults; plan emits no moves while disabled). **349 backend green** (was 341). **NEXT (HEAVY path, founder-gated):** Slice 2b (the nine-stage chain in `render.py` 🔒, golden-file byte-identical-when-off) and 2c (referee P1–P5 in `validate.py` 🔒) — each with the full test-author + adversarial-quorum + confirm-and-apply ceremony — then the founder's tuning week (pitch ON, acceptance-policy Slice 2d OFF), then the flip.
- 2026-07-10 (28th) — **PHASE 0 Slices 2b + 2c BUILT — the nine-stage vocal chain + referee rules P1–P5, SHIPPED DISABLED. Golden-file byte-identity holds; independent adversarial review = SAFE for the disabled ship. NOT enabled; awaiting founder approval.** `/zuko:build` (heavy path: golden-test-first → build → referee → independent adversarial quorum → confirm-and-apply). Built to the founder's detailed 2b/2c brief. **Golden gate written FIRST against main** (`test_render.py::test_golden_enabled_false_is_byte_identical_to_m6_0`, hash `0483b03e…`, + a determinism test), so "disabled == m6.0" is a real gate, not a rubber stamp. **The chain (`workers/render.py` 🔒):** stages 1,2,5,7 (de-ess/high-pass/compress/presence) in ONE FFmpeg filtergraph; 3 pitch (rubberband, **`formant=preserved`+`pitchq=quality` explicit** — never the chipmunk default); 4 time-stretch UNTOUCHED (the 3→4 double-resample cost is commented, not fixed); 6 saturate (numpy tanh, wet hard-capped 0.5); 8 reverb (deterministic seeded numpy convolution — FFmpeg has no native reverb; NOT `aecho`); 9 duck (bed-side, keyed by the placed vocal, only ducks). **Length-preserving by construction** (every stage trimmed/padded to input length), so `placement_end`/R1 are untouched. **Referee (`validate.py` 🔒):** P1 pitch band (±3), P3 dial bounds, P4 duck sanity (only ducks / ≤6 dB / never the vocal stem / keys a real placement), P5 timeline anchoring — plan checks; **P2 (pre-master ceiling) is render-side** (it needs the intermediate audio). **Emission (`plan.py`):** `_emit_vocal_chain` turns the config into per-placement `VocalProcessMove`+`DuckMove` (the G5 seam); a disabled stage emits its neutral dial; `pitch_semitones` pinned 0 (repair is 2d). `build_mix_plan` gained a `chain=VocalChainConfig()` param (default OFF). **Two deviations, both flagged in-code (rule 6):** (a) **P2 is a RELATIVE peak-gain guard (+3 dB), not the brief's literal −3 dBFS absolute ceiling** — real separated vocal stems peak ~1.14, so a literal ceiling rejects EVERY mix; the true saturation-mush detector (crest-factor) is deferred to the tuning week; (b) **stage 9 is a numpy envelope ducker, not FFmpeg `sidechaincompress`** — exact depth, provably only-attenuating, golden-deterministic (same latitude as the reverb). **A REAL BUG caught during build (probe on the real Der Lagi vocal):** the reverb inflated the vocal peak +7 dB (convolution sums many IR taps) → fixed by normalizing the wet to the dry peak (reverb adds ambience, not level). **Verified end-to-end on the real Father Ocean × Der Lagi pair:** DISABLED render **byte-identical to the m6.0 baseline** (same bytes); ENABLED render validates clean (P1–P5), peak 0.891 (no clip), same length, pitch 0. **Independent adversarial-safety review (fresh agent, 8 attack vectors):** all 8 HELD; verdict **SAFE for the disabled ship** (golden byte-identity confirmed empirically + by inspection). **TWO must-fixes BEFORE `enabled=True`/Slice 2d (do NOT block the disabled commit):** ⭐ **(1) GPL licensing — the `rubberband` filter is GPL `librubberband`**, which contradicts CLAUDE.md's LGPL-only stack rule; inert in Phase 0 (pitch pinned 0, never called at mix time), but the code+tests depend on a GPL build (the pitch tests now `skipif` no rubberband, so CI won't hard-require GPL). **Founder licensing decision owed before pitch turns on.** **(2)** sub-peak saturation mush is unguarded (P2 can't catch it; crest-factor floor deferred to tuning). **372 backend green** (was 351; +21 chain/referee/emission tests, each fail-without-the-rule). `render.py`/`validate.py` edits went through record-approval → apply → clear. **STOPPED at the gate: not committed pending founder review, `enabled` stays False, Slice 2d off.**
- 2026-07-10 (29th) — **PHASE 0 close-out: off-state COMMITTED + three pre-enable refinements. Still disabled, golden green, 379 tests.** `/zuko:build` (founder close-out brief). **(1) Off-state committed** (`ce67358`) — the reviewed 2b/2c, `enabled=False`. **(2) Reverb IR fix — `reverb_wet` is now song-independent.** The first fix (normalize the WET to the dry peak per render) was signal-dependent — a spiky vocal got less reverb than a squashed one at the same dial, which would wreck the tuning week. Fix: **L2-normalize the IR once at generation** (a fixed property: measured −0.9 dB peak gain, energy-preserving/audible), so `reverb_wet` means the same on every song; no per-render normalization. **(3) 🔴 Crest-factor MUSH guard built (before enable, not deferred to tuning).** P2 (peak-gain) is structurally blind to saturation mush (tanh LOWERS the peak), so a crest-factor (peak/RMS) delta guard was added: the chain may not collapse crest below `CREST_MIN_RATIO × input`. **Threshold found empirically (real Der Lagi vocal):** clean input crest 5.52; LEGAL processing (all dials in P3 range) keeps ratio **0.83–0.95**; MUSH (distortion past the caps) collapses it to **0.26–0.37** (tanh drive 8→0.37, drive 16→0.31, hard-clip→0.26). Wide cliff (0.5–0.83) → threshold **0.60**, centered. (Within the P3 caps it can't fire; defense-in-depth for a future dial/bug.) **(4) P2 + crest moved to a standalone module** `workers/chain_guards.py` — a real referee re-derives independently, not an assertion inside the code it polices; **one-way dependency** (render imports it, never the reverse); tested against synthetic signals with known answers (sine crest √2, square→1, silence→∞, spiky→mush). **(5) Duck envelope confirmed smoothed** (single-pole `_smooth_env`, not a hard step) + a discontinuity test (a step would click, and clicks survive the master). **(6) 🔴 GPL write-up delivered to founder, UNDECIDED.** Confirmed this dev FFmpeg is `--enable-gpl --enable-librubberband` (a GPL build). Contradicts CLAUDE.md's LGPL-only rule. **Inert today** (pitch pinned 0), **blocking for Slice 2d.** Business/licensing decision — NOT made by the agent; CI skip retained. **Golden byte-identity re-verified on real audio** after every change (disabled = m6.0, same bytes; enabled = clean, no clip, pitch 0). **379 backend green** (was 372). `render.py` edits went through record-approval → apply → clear. **STOP: `enabled` False, Slice 2d parked pending the GPL decision.**
- 2026-07-10 (30th) — **PHASE 0 MVP prep: GPL decision MADE (keep it), FFmpeg pinned, tuning harness ready. Still disabled; 381 green.** `/zuko:build` (founder MVP-mode brief). **⭐ Founder decision: STAY on the GPL FFmpeg build, keep pitch, do NOT rebuild LGPL** — Merry Go is server-side only (50–100 users), GPL triggers on _distribution_ not use, users receive audio not the binary → no exposure at this stage; and pitch is Phase 0's highest-value feature (the whole point of Slice 2d). **(1) FFmpeg pinned:** `workers/ffmpeg_pin.py` (`PINNED_FFMPEG_VERSION=8.1.1`) + `test_ffmpeg_is_the_pinned_version` fails loudly on drift — the golden-file byte-identity rests on a KNOWN binary; build string recorded in [docs/ffmpeg.md](ffmpeg.md). **(2) Licensing escape-hatch documented** ([docs/ffmpeg.md](ffmpeg.md)): current position (safe, server-side), the trigger (any on-device/shipped binary = distribution), and the small known exit (only stage 3/pitch is GPL; swap to Signalsmith Stretch (MIT) + LGPL rebuild; stages 1,2,5,7 are core LGPL). CI skip retained; question PARKED. **(3) Crest guard relabeled a BACKSTOP** (`chain_guards.py`) with a "these rules are LAYERED, NOT redundant — do not loosen P3's cap assuming this catches it" warning — the empirical finding was that inside P3's cap there is no crest cliff, so P3 prevents mush by construction and this only fires if a dial/bug escapes P3. **(4) Reverb IR normalization PINNED by test** (`test_reverb_gain_is_input_independent`): proves `_reverb` is LINEAR (additive) → input-independent gain; a reintroduced per-render normalization (the song-dependence bug) breaks additivity and fails immediately. **(5) Tuning harness built:** `scripts/tune_chain.py` renders a pair OFF (m6.0 baseline) + ON with CLI dial overrides, stamps the dials into the filename + a sidecar `.txt`, prints crest before/after + peak + key-fit; guide at [docs/tuning-guide.md](tuning-guide.md) (the 7 key-compatible tuning pairs; the **3 clashing pairs EXCLUDED** — FO×Suniyan, Anchor×Maula Mere, Innerbloom×Dooriyan — and reserved as the Slice 2d before/after evidence set; tuning order; bisect-don't-guess). Smoke-tested on FO×Der Lagi (OFF/ON both peak 0.891, crest 4.86→4.71). **Golden gate green after every step; 381 backend green** (was 379). **STOP: `enabled` False, Slice 2d untouched, nothing pushed. Next: the founder enables in the sandbox and tunes by ear → the winning dials become `bollywood_vocal_over_house` v1 → then Slice 2d (pitch repair).**
- 2026-07-10 (31st) — **"Fix the ears" Step 0 + Step 1 DIAGNOSTIC — two theories REFUTED, the real problem located. Analysis cache split (zero cloud). render.py/validate.py untouched; 382 green.** `/zuko:build` (founder "fix the ears" brief), done in parallel with the founder's tuning week (different files, no collision). **Step 0.1 — analysis cache SPLIT:** the cloud half (bpm/beats/downbeats/sections) is cached in `{song_id}.structure.json` keyed by content hash, NEVER invalidated; the local half (key/energy/vocal_regions) is keyed by `song_id` + `LOCAL_ANALYSIS_VERSION` (`la1`), so improving the local analyzer re-derives it for free (the cloud structure is reused, or seeded from a legacy combined analysis — zero Replicate). Route + `analyze_track` are version-aware (a stale local half re-derives on next access). **Zero-cloud proven** (`test_local_reanalysis_costs_zero_cloud`: rig the cloud to crash, re-analyze, seeded from the legacy analysis). **Step 0.2 — golden-gate framing:** the golden test keys on a FIXED synthetic PLAN (Gate A — render determinism), NOT on a song pair, so analyzer changes can't break it; a Gate B (plan-determinism) is deferred to Step 2 (re-baseline deliberately then). **Step 1 — READ-ONLY diagnostic across 18 catalog songs / 56 legal pairs (`scratchpad/diag_ears.py`, cloud guarded):** ⭐ **(1.1) BLOB THEORY REFUTED — only 2/18 songs (11%) are one region covering >90%** (both are INSTRUMENTAL beats with sparse vocals — Rapture, I Adore You); vocal songs are multi-region (median 4 regions, median 84% coverage). ⭐ **(1.2) "lead_sections never fires" REFUTED — it fires 56/56 (100%), predrop_licks 54/56.** The "both vocals trade" feature runs on EVERY legal pair (beats have low vocal coverage / many gaps, and even a blob beat sings in the gaps). **(1.3) Loudest-slice hook fallback is WORTHLESS — 0/3 vs the hand-marked hooks** (off by 28s / 14s / 124s) → hand-marking is mandatory, confirmed. ⭐ **(1.4) THE REAL PROBLEM: the section map is close to noise — only 23% of labelled drops sit on a real energy jump, and 54% of real energy jumps have NO label** (worse than the brief's 40% "noise" threshold). **Conclusion:** Step 2 (the big per-beat/hysteresis/HPF segmentation overhaul for blobs) is largely UNNECESSARY — the features aren't starving. Step 3 (section second-opinion via energy cross-check) is the high-value fix, and Step 4 (confidence throttles ambition — esp. the 0.6 section confidence) follows. **REPORTED, STOPPED per the brief — steps 2–4 gated on founder + reviewer reading these numbers.** 0.1 committed; nothing pushed; `enabled` False; Slice 2d untouched.
- 2026-07-10 (32nd) — **"Energy first" Step A DIAGNOSTIC — the section map is close to noise AND its error is random; energy-first is a DROP detector, not a HOOK detector. Read-only; no code changed.** `/zuko:build` (founder "energy first" brief). **TWO DEAD THEORIES (recorded plainly, per the brief):** (i) `vocal_regions` is blobby — DEAD (measured 11%, both instrumental beats); (ii) `lead_sections` never fires — DEAD (measured 100% of legal pairs). Do NOT touch `vocal_regions` or `lead_sections`. **Step A.1 — is the section map's error random or structured?** Per-song section-label precision vs energy clarity across 18 songs: `r(precision, n_jumps)=+0.55` but that is a METRIC ARTIFACT (more jumps → more chances a label coincides); the honest clarity measure `r(precision, max_rise)=+0.27` (weak), `r(precision, e_std)=+0.13` (none); clean-vs-messy-energy split **0.20 vs 0.25** (negligible). **Killer datapoint: Father Ocean has the SHARPEST drop (max_rise 0.80) and the WORST label precision (0.00)** — clean energy, wrong labels = the random signature. **Conclusion: the section map's error is essentially RANDOM w.r.t. energy clarity → per the brief's rule, DROP the section map from the decision path (energy leads alone), not merely demote it.** (18-song sample; founder/reviewer confirm.) **Step A.2 — energy-first hook candidate vs the 3 hand-marked hooks:** energy-first picks 14s/175s/285s → off by **28/106/233s**, WORSE than loudest-slice's 28/14/124 on 2 of 3. Because a HOOK is sung-vocal content and the energy curve tracks overall/instrumental loudness (the loudest jump is often a late instrumental climax). **So energy-first is a DROP detector (beat song, where to place vocals), NOT a HOOK detector — hooks stay HAND-MARKED (1.3 + A.2 both confirm).** This does NOT undermine promoting `energy_drops` to the primary DROP detector (Step B.1); drops≠hooks. **REPORTED, STOPPED — Step B gated on founder + reviewer.** `sections_confidence=0.6` is exposed as ~3× optimistic (real precision ~0.23). `render.py`/`validate.py`/`vocal_regions`/`lead_sections` untouched; `enabled` False; Slice 2d untouched; nothing pushed.
- 2026-07-10 (33rd) — **"Energy first" A.3 experiment + Step B BUILT — section map DROPPED from the decision path (it was already inert); energy leads. 384 green; render.py/validate.py/vocal_regions/lead_sections untouched.** `/zuko:build` (founder "energy first" brief). **A.3 (read-only) — the vocal-stem + repetition hook experiment (the signal A.2 never tested):** scored 8-bar phrases by vocal-stem chroma self-similarity (a hook is the line that comes BACK) + vocal-stem energy. Off by **73/27/164s** vs the 3 hand-marked hooks — does NOT beat loudest-slice (28/14/124). BUT the true hook surfaces as a HIGH-repetition candidate (Dil Ye Bekarar's marked phrase scored repetition 0.97, top-3) — the signal is real and now on the RIGHT track (vocal stem), a naive "highest repetition" pick just can't isolate the hook from repeated verses. **Hooks stay HAND-MARKED for the MVP (now confirmed by THREE methods: loudest-slice, bed-energy, vocal-stem+repetition — all miss); vocal-stem repetition is a genuine post-MVP direction.** **Step B (build):** **B.1 — `energy_drops` is ALREADY the primary drop detector** (drops are energy-first everywhere; nothing computed them from sections) — documented, no code change. **B.2 — section map REMOVED from the active decision path (`fence.py`):** deleted the section-map fallbacks in `best_vocal_slice`/`vocal_slices`/`vocal_peaks` (+ the now-dead `_SUNG_LABELS`); ⭐ **KEY FINDING: this changed ZERO catalog mixes** — the section map already influenced no arrangement decision (drops energy-first, hooks hand-marked, cropping off; the removed fallbacks fire only on EMPTY `vocal_regions`, and every catalog song has them populated). The only remaining `sections` reads are the DISABLED AI arranger and the live-chip suggester (neither a drop/hook/crop decision). **Breakage list (reported):** 3 fence tests pinned the removed fallback → updated to assert the new track-middle fallback (not weakened). **B.3 — confidence throttles ambition:** satisfied BY CONSTRUCTION — ambition now tracks MEASUREMENT (energy drops/windows + bpm confidence), never the noisy section map; a no-drops song gets a plain arc (produced drops need drops; cropping is off). **B.4 — GATE B added** (`test_gate_b_plan_determinism_on_a_fixed_analysis`, sig `b55f68d2…`): plan-determinism given a fixed analysis, complementing Gate A (render determinism); **deliberately baselined** — verified via `test_section_labels_do_not_affect_a_populated_regions_plan` (garbage section labels produce an IDENTICAL plan) that the plan comes from energy + vocal_regions. **B.5 — bumped `ENGINE_VERSION m6.0→m6.1`** (a PLANNER change → the mix cache is the right lever; ⚠️ NOT `LOCAL_ANALYSIS_VERSION` — the analysis output is UNCHANGED, flagged per rule #4). Catalog mixes re-render byte-identical (the fallback never fires); **zero Replicate** (stems/analysis keyed by song_id; diag built 56 plans with the cloud rigged to crash). **FOUR sessions, FOUR theories killed, still zero features mis-built.** `render.py`/`validate.py`/`vocal_regions`/`lead_sections` untouched; `enabled` False; Slice 2d untouched; nothing pushed.
- 2026-07-10 (34th) — **Task 1 — the loudest-slice HOOK fallback REMOVED. A no-hook song no longer guesses; it uses its vocal regions as-is. Safe surface only; 384 green.** `/zuko:build` (founder "validate the drops" brief, Task 1). Applying B.3's own principle to hooks: a **hand-marked** hook = high confidence (unchanged — still lands on the loudest drop, recipe R1); **no hand-marked** hook = low confidence → **do NOT guess** which slice is memorable. Diagnostic 1.3/A.2 measured the loudest-slice guess **28/14/124s off** as a hook detector — a confident wrong answer is worse than an admitted unknown. **Change (`plan._default_arrangement`, safe surface — `plan.py` + `hooks.py` docstring; `render.py`/`validate.py`/`fence.py` UNTOUCHED):** the three no-hook sites that fell back to Song 2's **loudest** vocal peak (`vocal_peaks[0]`) — the single-anchor return, the multi-anchor `else` branch (loudest→drop), and the nothing-fit safety net — now use the vocal regions **as-is in the song's own time order** (`asis = sorted(vocal_slices, by start)`), laid across the anchors in time order, so **no slice is privileged onto the drop**. `vocal_peaks` is still used to order the SETUP entries when a hook IS present (not a hook claim). **⚠️ CORRECTION to the first summary of this change (caught by the founder): the catalog is NOT unaffected.** All FIVE shipped catalog vocal donors (Don't Start Now, Der Lagi, Tujhe Bhula Diya, With You, Tere Bina) are **hookless** — the 3 marks in `hooks.py` are older Anchor Point songs, none in the shipped manifest — so **every catalog vocal mix changed.** **VERIFIED on the founder's tuning baseline Father Ocean × Der Lagi (take 1):** entries 2 & 3 swap which Der Lagi section they sing (216.6–249.1s ↔ 134.5–174.5s); the drop entry (43.6–83.6s @ 63.0s) is unchanged; tempo/stretch/builds identical. **`ENGINE_VERSION m6.1→m6.2`** is therefore warranted by REAL movement (not caution) — without it the app serves stale pre-Task-1 audio (PLANNER change → mix cache; zero Replicate, stems/analysis keyed by song_id). **Follow-up (Task 2): mark each donor's hook so the stable with-hook path runs — Der Lagi FIRST to un-move the tuning baseline.** **TDD:** the old `test_loudest_vocal_peak_lands_on_the_biggest_drop` (which pinned the now-removed guess) replaced by `test_no_hook_lays_vocals_in_song_order_not_loudest` (earliest anchor → earliest region; loudest region NOT forced onto the drop); the with-hook `test_hook_lands_on_the_loudest_drop` stays green untouched. **Gate B re-baselined deliberately** (`b55f68d2…→e57725c7…`): eyeballed the new no-hook plan — vocals now in time order (anchor 0.0→region 0-16, 32→24-44, drop wraps to region 1), drop keeps its energy-driven produced build; still energy + vocal_regions, not section labels. **Golden gate green** (the golden plan is hand-built, doesn't touch `_default_arrangement`). **384 backend green** (unchanged count: −1 test +1 test). **Zero cloud** (pure planner logic; no analysis/stems touched). Classified **LIGHT** (single reversible planner surface, no dangerous glob, clear check). `enabled` False; Slice 2d untouched; nothing pushed.
- 2026-07-10 (35th) — **Task 2 tool + Task 3.1 BUILT; Task 1's real impact measured. 386 green; render.py/validate.py untouched.** `/zuko:build` (founder "validate the drops, start the seams" brief). **⚠️ Task 1 impact corrected (founder caught the contradiction):** ALL FIVE shipped catalog vocal donors are hookless (the 3 hooks.py marks are older Anchor Point songs, none shipped), so **every catalog vocal mix changed** — VERIFIED by diffing old vs new plans for the tuning baseline **Father Ocean × Der Lagi** (take 1): entries 2 & 3 swap which Der Lagi section they sing (216.6–249.1s ↔ 134.5–174.5s); the drop entry is unchanged. So `ENGINE_VERSION m6.1→m6.2` is warranted by real movement, not caution. **Fix owed: mark Der Lagi's hook (Task 2 tool) to restore the stable with-hook baseline — needs the founder's ear, flagged.** **Task 2 (the instrument, not the report):** `scripts/mark_drops.html` — a self-contained, zero-install browser marker (folder-pick → play → tap **D** at drops, **H**/**H** to bracket the hook → CSV export, autosave; the 6 catalog songs named, others by hash); `scripts/measure_drops.py` — reads the marks CSV + cached analyses, runs the SHIPPED `fence.energy_drops` (reused, not copied), reports **precision/recall/offset-in-bars**, greedy nearest match within ±1 bar, **zero cloud**. Scoring self-verified (perfect marks→100/100 @0 bars; drop one + add a fake→86/86). `scripts/ground_truth/{drops_hooks.csv template, README}`. **The precision/recall REPORT is blocked on the founder's ~1hr marking session → Task 4 stays gated (correct).** **Task 3.1 (persist the plan's beat grid):** the `MixPlan` was already written next to the WAV (`routes/mix.py:_run_mix`) — 3.1 **enriches** it with the mix's OWN output-time grid (`MixPlan.out_downbeats`/`out_phrase_starts`/`mix_duration`, additive) so joining sets is arithmetic, never a re-analysis of the output. New `window.output_grid` (the referee's own retime→window derivation, canonical copy since `validate.py` is untouchable); `routes/mix.py:_attach_set_grid` stamps it after render (length from the WAV header, metadata not analysis). **`render.py`/`validate.py` UNTOUCHED** (3.1 = `mix.py`+`window.py`+`models.py`). TDD: `output_grid` == referee derivation (native + retimed+windowed); the real render test asserts the persisted grid is non-empty, duration>0, phrase⊆downbeats. **386 backend green** (was 384). **NEXT: 3.2 (one global master tempo across a set) → 3.3 (beat-aligned phrase-boundary seams in set_render.py, reading these grids).** `enabled` False; Slice 2d untouched; nothing pushed.
- 2026-07-10 (36th) — **Set transitions 3.2 + 3.3 + 3.4 BUILT — the set engine now beat-matches its seams. 395 green; render.py/validate.py untouched.** `/zuko:build` (continuing the "start the seams" brief). **3.2 — one global master tempo (`workers/set_render.py`, pure planning arithmetic):** `global_master_tempo` = geometric midpoint of the feasible interval `[max_bpm·lo, min_bpm·hi]`; `set_tempo_plan` greedily drops the worst BPM outlier until the kept set is feasible (`max/min ≤ hi/lo ≈ 1.25`), then centres on it — **never loosens the band** (`SAFE_STRETCH 0.89/1.11`, pinned equal to `fence`'s by a drift test). **Catalog report:** the full 6 can't share one tempo (111–143 BPM = 1.29×), so **Tere Bina (143) is declined** (stretch 0.845, and it doesn't blend with the FO beat individually anyway); the **realistic 5-song set fits at ~120.8 BPM** (0.91–1.09). The greedy drop removes ONLY the true outlier — a naive geo-mean+one-shot would have wrongly dropped Der Lagi (111) too. **3.3 — beat-aligned, phrase-boundary seams:** `assemble_beatmatched_set(mixes, phrases=1)` reads each mix's persisted `out_phrase_starts` (3.1); `_phrase_seam` mixes A OUT on its last phrase boundary (crossfade = A's last `phrases`×8 bars) and B IN on its first — both begin on a phrase boundary, so at the one shared tempo every beat in the overlap coincides **with no onset detection** ("never cut mid-sentence"). Reuses the existing equal-power `_crossfade_join`; too-thin grids fall back to a plain crossfade (never worse than `assemble_set`); the tail grid is carried into output time across chained seams. **3.4 — peak-safe:** both assemblers share a new `_finalize` (peak-normalize −1 dBFS + brickwall clip); a hot-input test proves no clip. **TDD (9 new set tests):** band-matches-fence; tempo centres in band; close set all-fit; catalog declines exactly Tere Bina; `_phrase_seam` picks last/first boundaries + None fallback; phrase-boundary trim length; **downbeat alignment via impulse trains (one bar-spaced train in the overlap, not two interleaved)**; fallback length; hot-input no-clip. **395 backend green** (was 386). **LEFT — the set-builder wiring:** a `/set` API + screen that renders each chosen mix at the one global tempo then calls `assemble_beatmatched_set`; the engine (3.1–3.4) is done. `render.py`/`validate.py` untouched; `enabled` False; Slice 2d untouched; nothing pushed.
- 2026-07-09 (26th) — **GOOD-PARTS WINDOW DISABLED — mixes are FULL-SONG again (founder decision, prototyped in the sandbox first, then applied to main).** After ear-testing full-song vs windowed mixes, the founder chose full-song. Workflow: first disabled + rendered the 4 full-song mashups in the experiment sandbox (`C:\DJ-AI-Experiment`) for the founder to hear, confirmed, then ported the one change to main. **Change (safe surface — `plan.py` only; `render.py`/`validate.py` UNTOUCHED):** new module flag `_GOOD_PARTS_WINDOW_ENABLED = False`; `build_mix_plan` now computes `window_span = choose_window(...) if (_GOOD_PARTS_WINDOW_ENABLED and _confident(a1g)) else None`, so `window_span` is always `None` → the engine's own "no window → full track" path runs and `render.py`/`validate.py` (which already treat `window=None` as full track) need no edit. **The window machinery is kept DORMANT, not deleted** (`window.py` + the render/validate window-handling), matching the parked-vocal-chop convention — a one-line re-enable. **Tests updated (safe surface):** `test_build_mix_plan_windows_a_long_song` → `..._uses_the_full_song` (asserts `window is None`); `test_long_song_vocal_spans_its_good_part_window` → `..._spans_the_full_song` (anti-clustering now on the full track); `test_validate_plan_clean_on_a_windowed_plan` now flips the flag ON via monkeypatch so the referee's window path stays covered. **332 backend + 39 web green, typecheck clean.** Verified on the real Father Ocean × Tere Bina pair: `build_mix_plan` returns `window=None`, rules path, 3 placements across the full 472 s track. Living docs updated same change (functional-spec, technical-spec, mix-recipe, backend-explained). **Founder-confirmed by ear** on the 4 sandbox full-song renders before the main-folder change. Note: the good-parts window was the prior session's headline feature (24th entry / the standalone good-parts entry) — this reverts its _effect_ while preserving the code.
- 2026-07-11 (37th) — **⚠️ BASELINE-MOVE NOTICE (read before "fixing" any Father Ocean × Der Lagi render): the corrected render is MORE correct, NOT a regression — do not revert it.** `/zuko:build` (founder "hear the seam, start the screen" brief). The old Father Ocean × Der Lagi tuning baseline was built on the **loudest-slice hook guess**, which diagnostics 1.3 / A.2 measured **~28 s off** the song's real memorable line — i.e. the "baseline" everyone was tuning against was itself landing the wrong slice on the drop. Task 1 (34th entry) removed that guess; the founder is now hand-marking Der Lagi's real hook (Task 2 marking session, `scripts/mark_drops.html`). **When that hook lands, this pair's render CHANGES again — and that change is the guess being replaced by the marked truth, a correction, not a break.** So: if a future session (or reviewer) sees the Der Lagi mix "move" and is tempted to restore the old audio to match a saved reference, STOP — the old audio was the ~28 s-off guess. The marked-hook render is the new, correct baseline; re-tune against it, don't revert to the wrong one. (No engine change in this entry — this is the paper trail the founder asked to leave so the correction isn't mistaken for a bug. `render.py`/`validate.py`/`vocal_regions`/`lead_sections`/`enabled`/Slice 2d untouched; nothing pushed.)
- 2026-07-11 (38th) — **Task 2 MEASUREMENT reported + five catalog hooks WIRED (Der Lagi's correction now APPLIED). Crop (Task 4) HELD on the numbers. 396 green; render.py/validate.py untouched.** `/zuko:build` ("numbers before features" brief). **The founder's ~1hr marking session is done** (`scripts/ground_truth/drops_hooks.csv` = the real marks, was a header-only template): **21 drops across 13 songs, 25 hooks across 25 songs** — an adequate sample for a pooled read (not the "one or two songs" thin-data case), though per-song counts (1–3 drops) make per-song figures directional, pooled trustworthy. **Task 2 result — `energy_drops` vs the ear (`scripts/measure_drops.py`, 12 songs scored, 1 skipped for no cached analysis, zero cloud): pooled PRECISION 19%, RECALL 55%, median offset 0.11 bars (mean 0.24).** Read: the detector **over-fires** (58 found vs 20 real → ~3× too many candidates) and **misses ~half** the real drops, BUT **when it hits it is essentially dead-on the beat** (0.11 bar). Failure is **song-dependent, not uniform**: on **Father Ocean — the only shipped beat — recall is 100%** (both real drops found) at 50% precision (2 real + 2 false); several other beats fail hard (2471e/7f0b6/Tujhe/fa79/2294a at 0% recall, some finding 8 candidates matching 0). ⚠️ **Caveat recorded:** this measures the RAW candidate list; the arranger uses the single strongest drop, so raw precision overstates the arrangement's real exposure — the decision-relevant number is "does the TOP-ranked drop match a mark", not yet measured. **Task 4 (the crop) NOT started — HELD by construction on this result** per the founder's own logic (a crop anchored on drops must not be built on a detector that doesn't match the ear): the pooled numbers say the raw detector isn't reliable enough for a _general_ crop; for the current single-beat catalog (FO, 100% recall) a tightly confidence-gated crop _might_ be safe, but that's a founder+reviewer call, deferred until the top-drop measurement is seen. **Hooks WIRED (`hooks.py`, planner/safe surface):** all five shipped vocal donors (Der Lagi 18.7–51.1, Don't Start Now 51.5–66.2, Tujhe Bhula Diya 58.7–81.5, With You 53.3–84.3, Tere Bina 63.5–90.1) from the founder's marks; **`ENGINE_VERSION m6.2→m6.3`** (with-hook R1 path now lands each donor's real hook on the drop → every catalog vocal mix re-renders). **Der Lagi's hook landing IS the correction the 37th entry predicted — the tuning baseline is un-moved, not broken.** Guard test added (`test_every_catalog_vocal_donor_has_a_marked_hook` — a donor added to the manifest without a hook now fails loudly); the version-pin test updated m6.2→m6.3 (a deliberate bump, not a weakening). **396 backend green** (was 395: +1 hook guard). Golden gate re-verified byte-identical. Classified **LIGHT** (planner data, reversible, clear check). `render.py`/`validate.py`/`vocal_regions`/`lead_sections`/`enabled`/Slice 2d untouched; nothing pushed.
- 2026-07-11 (39th) — **⚠️ FRAMING NOTE — the 2026-07-09 good-parts-window rejection does NOT settle the SET-crop question; they are different tests. Do not cite the old result as if it did.** `/zuko:build` ("A/B the cropped set" brief). On **2026-07-09** the founder compared full-song vs the ~90s good-parts window on a **SINGLE mix** and preferred full (a 90s crop of one song loses body) → the window was disabled (`_GOOD_PARTS_WINDOW_ENABLED=False`, still off). **That was the wrong question for sets.** The open question now is entirely different: **does a tight ~3-minute SET beat a bloated ~15-minute one** (a two-song set of full-length mixes is a 15:00 sit; the crop-length check (38th-adjacent) showed FO crops 7:38→1:34, so the set collapses **15:00→~2:53, a 5.2× cut**). Losing "body" on one song is a real cost; sitting through 15 minutes of a two-song set is a bigger one — the calculus flips for sets. **Resolution mechanism:** a read-only A/B — both sets rendered at the SAME engine (m6.3, hooks wired), SAME two mixes (FO×Don't Start Now, FO×Der Lagi), SAME beat-matched seam engine, the ONLY variable being full vs hand-marked-crop (FO cropped on its hand-marked drop, not the auto-detector the crop-readiness report killed). Founder decides by ear; a reviewer confirms. **If the tight set wins → scope a MINIMAL FO-only crop (fires only on hand-marked drops, like hooks; not a general crop system). If full wins → crop parked for good, full-length is the MVP.** No product code changed for the A/B (window forced ON only inside a throwaway render script; cropped mixes written OUTSIDE the cache so the app never serves them). `render.py`/`validate.py`/`vocal_regions`/`lead_sections`/`enabled`/Slice 2d untouched; nothing pushed.
- 2026-07-11 (40th) — **🔴 CROP PARKED FOR GOOD (do not re-litigate) — the hand-marked crop makes an INVALID mix on the real catalog: R1 vocal overlap. Full-length sets are the MVP.** `/zuko:build` ("A/B the cropped set" outcome). The A/B render (39th) settled it by construction: **set A (full, m6.3+hooks) rendered fine (15:00), but set B (hand-marked crop) FAILED the quality validator — R1 "Song 1 and Song 2 vocals overlap beyond a crossfade."** In the tight ~94s window (FO 2:55→4:30, anchored on the hand-marked 3:55 drop) the "both vocals trade" feature (Song 1's own vocal via `lead_sections`/`predrop_licks`) collides with Song 2's hook — in a full song there's room to hand off, in 94s there isn't. **The validator was NOT overridden** (guardrail working as designed). **Why this parks it, not just delays it:** the only clean fixes are (a) rework the windowed arranger, or (b) suppress Song 1's vocal in the window — which touches `lead_sections`, an explicitly-untouchable surface. So the "minimal FO-only crop" is actually a real build that fights an existing feature AND the validator, for **one** song (FO is the only catalog beat). Stacked with the 2026-07-09 fuller-sound preference (39th reframed that as a different question, but it still stands as a data point) → **not worth it.** **DECISION (founder): park the crop permanently; full-length sets are the MVP; do not rebuild the good-parts window.** The window machinery stays dormant behind `_GOOD_PARTS_WINDOW_ENABLED=False` (still tested, one-line re-enable if some future multi-beat catalog ever revisits it — but the crop as a V1 feature is closed). No rough-trim clip made. The `/set` UI is NOT being built either (founder tests sets by ear via a CLI tool, next entry). `render.py`/`validate.py`/`vocal_regions`/`lead_sections`/`enabled`/Slice 2d untouched; nothing pushed.
- 2026-07-11 (41st) — **BEAT-LOCK "Thread B" fixed — a glitch bar TRUNCATES the lock instead of discarding it, so a mid-bar hook's ENTRY snaps to the beat. Tujhe late-by-~2-beats bug fixed; ear-confirmed. 396 green; render.py/validate.py UNTOUCHED.** `/zuko:build` ("fix the vocal-timing bug" arc). **Diagnosis (isolated vocal bus, the tool that caught the earlier mistake):** Tujhe entered ~1.16s (≈2 beats) late; the beat grids were clean (±0.03s); root cause was the per-bar beat-lock BAILING to `[]` on a glitch bar (Tujhe's hook has two mis-detected short bars at its end), so the legacy global stretch played the un-snapped raw hand-mark — off-beat for a MID-BAR hook. (Der Lagi's "early" was proven NOT a bug on the isolated bus — it enters +0.05s, on-beat; the "early" was Father Ocean's OWN vocal. Der Lagi stays cold.) **Two fixes proven WRONG before commit (the "prove it, don't hope" discipline):** (1) per-bar global fallback — R7-REJECTED (shifts an interior boundary off Song 1's grid), verified on the real render; (2) re-lock/merge — Tujhe's glitch is UN-lockable (no bar count yields an in-band ratio: 1.60/0.80/0.53). **The fix that works — TRUNCATE (`fence.warp_map`):** lock every bar UP TO the glitch, then ride the rest (glitch + tail) on ONE trailing global segment — the LAST segment, whose end boundary R7 does NOT grid-check (line ~108), so no interior boundary drifts and no ratio warbles. Entry + body lock on-beat; only the un-lockable ~4s tail global-stretches (under the echo). **Verified:** isolated bus entry at 34.03s = the anchor (was ~1.16s late), syllables |mean| 0.107s off the beat; tail numerically ambiguous (echo contaminates), so **founder ear-confirmed the tail rides clean** before commit. **`ENGINE_VERSION m6.3→m6.4`** (planner change; every mix whose slice had a bailing glitch re-renders; clean-grid slices identical; zero Replicate). **BOTH bail-tests rewritten** (test_fence + test_beatlock) to the truncate behavior, each commenting why the F1-review bail was right for downbeat-aligned slices and incomplete for mid-bar hooks — so nobody silently re-bails. TDD (failing test first). Golden gate byte-identical; **Gate B green, no re-baseline needed** (its synthetic fixture has no glitch bar → plan unchanged; the fix is surgical). Classified planner-only, referee-safe (uses R7's last-segment exemption, does not fight it). `render.py`/`validate.py`/`vocal_regions`/`lead_sections`/`enabled`/Slice 2d untouched; nothing pushed. **NEXT: Der Lagi is confirmed NOT a bug (cold); the only open vocal-timing item is Thread A (align to the audible syllable vs the downbeat), which has NO proven bug demanding it — deferred.**
- 2026-07-11 (42nd) — **Session close (`/zuko:handoff`): catalog gained a 2nd beat (LOCAL-ONLY), vocal-chain tuning STARTED. 396 green; nothing pushed.** Two things not in the numbered engine entries above: **(1) "I Adore You" added as a second catalog BEAT** (120 BPM, key 10A; `data/library/manifest.json`, role_hint beat). ⚠️ **The manifest is GITIGNORED — this addition is LOCAL-ONLY, not in git, will not transfer to another machine/branch.** Founder heard `I Adore You × Tujhe` (that's how the Tujhe timing bug surfaced); after the m6.4 fix the mix is timing-clean, but the founder has NOT given a final "keep I Adore You in the catalog" — so it's provisional. Its Tujhe hook is marked (m6.3); as a beat, its OWN hook mark is unused (hooks.py is for vocal donors). **(2) Phase 0 vocal-chain TUNING started** — the chain still ships OFF (`enabled=False`, untouched); tuned per-render in a SANDBOX via `scripts/tune_chain.py` (single pairs, pre-existing) and the new `scripts/build_set.py --chain` (sets; chain renders → `data/tuning_renders/`, never the app cache). **Approved so far: `saturate_wet=0.3`** (dial 1 of 7; order: saturate_wet→presence_gain_db→reverb_wet→duck_depth_db→compress_ratio→highpass_hz→deess_intensity). Six knobs to go; when all are dialed, the winning config is recorded and flipping `enabled=True` is a separate deliberate founder call. **Confirmed:** enabling the chain needs NO `render.py` change (chain is built + gated on `plan.vocal_moves`, which the config emits). Golden gate green (chain-off byte-identical → honest A/B baseline). **AUDIT correction (founder memory):** pitch-repair / Slice 2d is a gated ENGINE with NO decision logic (`plan._emit_vocal_chain` pins `pitch_semitones=0`); nothing pitch-corrects today. The silent set-order AI-scoring was NEVER built (`build_set.py` has `--order` only). `render.py`/`validate.py`/`vocal_regions`/`lead_sections`/`enabled`/Slice 2d untouched; nothing pushed.
- 2026-07-14 (43rd) — **🟢 Phase 0 vocal chain ENABLED in the shipped app (Export + live Play); loading pill added; pre-merge milestone gate run. `render.py`/`validate.py` LOGIC untouched.** All 7 dials tuned by ear on a full 3-mix set A/B and founder-approved: **saturate_wet=0.3, presence_gain_db=4.0, reverb_wet=0.08, duck_depth_db=1.0, compress_ratio=2.0, highpass_hz=120, deess_intensity=0.4** (character: bright, forward, punchy, clean, natural). **Turned on via `SHIPPED_CHAIN` (routes/mix.py, `enabled=True`) passed into `build_mix_plan`; the `VocalChainConfig` model default stays `enabled=False`, so the disabled/golden path is still byte-identical to m6.0 — the sound is OPTED IN, not a default flip. `ENGINE_VERSION m6.4→m6.5`** (the chain-config hash folds into the mix cache id → every mix re-renders with the chain; **zero Replicate**, stems/analysis keyed by song_id). **Live Play/steer preview now MATCHES the Download:** `live_stems.render_vocal_bus` applies the same per-placement chain + chop + echo (proven **byte-identical** to Export's vocal to one PCM-16 step, all 10 catalog pairs render a clean bus); the bed-side stage-9 duck stays Download-only (the live bed plays as separate stems). **Pitch stays pinned 0** (Slice 2d parked; rubberband never called at mix time — re-verified). **Web:** a "Loading your mix…" buffering pill on the Play screen while the live audio buffers (`PlayScreen.tsx`; verified live — shows on the 202 poll, clears on 200). **Tests:** `test_shipped_chain_is_enabled_with_the_founder_approved_dials` locks the enabled state + all 7 dials (a silent revert to OFF would otherwise stay green — the golden gate only guards the DISABLED path); `test_vocal_bus_APPLIES_the_chain_matching_export_pass1` proves Play==Download vocal; a `PlayScreen.loading.test.tsx` guards the pill; version pin m6.4→m6.5. **397 backend + 40 web green; typecheck + lint clean; golden gate byte-identical.** **Pre-merge gate (this branch → main, 169 commits):** floor green; scale review = ship-now; two adversarial safety reviews; docs + coverage audits. **⚠️ KNOWN LIMITATION — founder ACCEPTED (2026-07-14, prelaunch): the R1 crossfade relaxation (`validate.py`, the intended "natural hand-off") bounds a Song-1-tail / Song-2-lead overlap to ≤~1.2s and guarantees Song 2 enters clean, but it does NOT verify the outgoing tail has actually decayed — so a future pair whose Song-1 vocal is loud mid-phrase at a drop could ship ~1.2s of two full leads. Bounded, fails toward the intended hand-off, and the hand-verified catalog sounds good → founder chose ACCEPT + NOTE.** **PRE-LAUNCH TODO (not merge-blocking, before the ~50-user test): (a) make the hand-off decay true by construction (render a fade-out ramp over the overlap) OR referee-measure the tail energy; (b) ear-check each NEW pair's hand-off; (c) the cache-eviction sweep in `storage.py` — this merge orphans another WAV generation (m6.5 + new chain hash), unbounded disk; (d) lock down the localhost-wide CORS (`config.py`) before any public exposure.** Two commits (`a989586` chain on, `2ada0f7` loading pill) on `feat/house-bollywood-energy-sync`. `render.py`/`validate.py`/`vocal_regions`/`lead_sections` LOGIC untouched; nothing pushed, not yet merged.
- 2026-07-14 (44th) — **Pre-merge hardening: R1 hand-off FIXED (render.py), CORS locked down (config.py), ~3.9 GB cache cleared. Both blind pairs heard. 402 backend green.** Founder pre-merge brief (three "must clear before I click merge" items). **(1) Two blind pairs vetted:** the tuned chain (presence 4.0, near max) rendered clean on **With You** (untested vocal) and **I Adore You** (untested beat) — no guard trips; measured With You's post-chain presence-band energy at **0.30×** the tuned-vocal average (a _darker_ vocal → less harsh, not more); founder ear-confirmed. **(2) R1 hand-off FIXED (`render.py`, `ENGINE_VERSION m6.5→m6.6`).** Diagnosis (complete catalog coverage — V1 is curated pick-only, so the whole user-reachable space is 2 beats × 5 vocals): the ≤1.2 s hand-off overlap was safe on all **Father Ocean** pairs (its sparse vocal decays, −4.8/−5.0 dB) but NOT on **I Adore You** pairs (its vocal is flat/rising, up to **+7.2 dB** vs the incoming — real two-full-leads). Founder chose _keep I Adore You + fix the engine_ (explicitly lifting the render.py-untouched rule for this one reviewed fix). Fix: the Song-1 contrast loop fades the outgoing tail out across the overlap (`linspace(1,0)`), so as Song 2 edge-fades in, Song 1 ramps out → a real crossfade, decay **true by construction** for every pair. TDD (fade + control tests); **golden gate still byte-identical** (the fade is overlap-gated; the synthetic fixture has no overlap — see the tech-spec golden-gate note); adversarially reviewed **SAFE** (no clip, no crash, no shape bug); founder ear-confirmed on the worst case (I Adore You × Don't Start Now). Residual: a few tens of ms at the very hand-off instant, down from ~1.2 s. **(3) CORS locked down (`config.py`, dangerous surface, confirm-and-apply):** removed the wide `http://(localhost|127.0.0.1):\d+` regex from the default build; origins now an explicit env-set allowlist (`PROMPTDJ_CORS_ORIGINS`, default `localhost:5173`); the dev regex re-enables only with `PROMPTDJ_DEV_CORS=1`; production is same-origin so it needs none. `test_cors.py` rewritten to assert the lockdown bites (other localhost ports now blocked by default). **Cache cleanup:** cleared ~3.9 GB of regenerable mix outputs (`*.mix.wav`/`.mixplan.json`/`.livearr.wav`/`.suggestions.json`); all 28 songs' stems + analyses kept (the cache-eviction _sweep_ in `storage.py` is still the durable pre-launch item). **Two adversarial safety reviews this session (chain-on live path earlier; the hand-off fade now) both SAFE.** `validate.py`/`vocal_regions`/`lead_sections`/Slice 2d untouched. To be committed as separate commits (hand-off, CORS, docs); **nothing merged — the merge click is the founder's.** _(That branch was subsequently merged to main by the founder — the vocal chain is live.)_
- 2026-07-14 (45th) — **BUILD 1 (MVP finish): Pitch repair (Slice 2d) WIRED, shipped OFF behind a flag. `render.py`/`validate.py` untouched; 405 backend green.** Founder MVP-finish brief (features 3 + 5, the last two). The pitch ENGINE existed but `pitch_semitones` was pinned 0, so clashing-key pairs were accepted as-is (subtly wrong). **Now wired:** `build_mix_plan` computes the key correction and emits it as a real `pitch_semitones` on every vocal move. **Gate (same pattern as the chain):** new `VocalChainConfig.pitch_repair_enabled` (default **False** → pitch stays 0, nothing pitch-corrects in the shipped app; the founder flips it on after the ear-test). When on: `fence.compute_pitch_repair(a1,a2,cfg)` → shortest camelot shift; a clash **beyond the ±cap DECLINES** (raises `MixDeclined` — never warps past the safe band). Referee **P1 (±3)** and `render.py:_pitch_shift` (**formant=preserved**, no chipmunk) were already enforced/set — UNTOUCHED. **Findings (read-only):** the 3 named clashing pairs are **dropbox-only**, not in the shipped catalog; on the shipped catalog no pair needs a _small_ shift (all already-compatible or clash >±3), so turning repair on would only make **Father Ocean × With You** (10B/11A) and **I Adore You × Tere Bina** (10A/11B) decline instead of mixing wrong. **Ear-test set (`scripts/tune_pitch.py`, zero cloud — reuses tune_chain's replicate-raises guard, all 3 pairs cached):** Father Ocean × Suniyan (10B/4B → −1 st) and Anchor Point × Maula Mere (8A/5A → −1 st) render BEFORE/AFTER; Innerbloom × Dooriyan (6B/7A) **DECLINES** on AFTER (>±3) — the refusal is the demonstration. TDD: off-by-default emits 0 / on emits the shift + passes P1 / >±3 declines. Golden gate byte-identical (405 green). **STOP for founder ear-test — turning `pitch_repair_enabled=True` is a separate deliberate call (ear + yes), like the chain enable.** `render.py`/`validate.py`/`vocal_regions`/`lead_sections` untouched; nothing pushed.
- 2026-07-14 (47th) — **🟢 MULTI-SET UI BUILT (Setup multi-set + `/set` route + stripped Play screen) — reverses the "two-songs-only / no set screen" non-goal AND retires live steering, both founder calls. Branch `feat/multiset-ui`; 415 backend + 49 web green; live two-set heard playing. `render.py`/`validate.py`/`set_render.py` UNTOUCHED (reuse only).** `/zuko:build`, three verified pieces, mockup-approved first. **This is a deliberate DRIFT from two frozen decisions:** (a) functional-spec non-goal "V1 is strictly two songs / no in-app /set screen" → up to **2** sets at the time (**raised to 5 on 2026-08-07 — the core rule is now `MAX_MIXES_PER_SET = 5`**), opt-in, with a screen; (b) V1 Feature 2 "live steering" → **removed from the Play screen** (the founder was told twice exactly what the prompt bar/parts/chips did, then chose to strip them). Both recorded in the specs as superseded, not deleted. **Piece 1 (Setup):** `SetupScreen` owns a 1–2-set line-up (`MAX_SETS=2`, `types.ts`), emits `onBuild(sets)`; console = picker, stage = running order (reorder/remove); single-set path unchanged. **Piece 2 (`services/api/app/routes/set.py`, NEW; `main.py` registers it):** async start-then-poll like `/mix`, cached by ordered pairs; `_run_set` is thin orchestration — `set_render.set_tempo_plan` (decline off-tempo outliers) → per kept pair `mix._run_mix` (real pipeline, cache-reused) → `set_render.assemble_beatmatched_set` (beat-matched join); returns a manifest of kept/dropped members (+ reason + `seam_at`) + duration; `/set/{id}/audio` serves the joined WAV. The **route sibling of `scripts/build_set.py`** — same engine calls, now with an endpoint. **Piece 3 (Play stripped):** removed stem-bus `LivePlayer` + all steering from `PlayScreen`; new `lib/trackAudio.ts` (`TrackPlayer`, thin HTMLAudio wrapper, mockable) plays the finished mix or set; left panel = read-only set line-up (now/next/dropped), scrubber shows seam markers; `App` normalizes both paths to `PlayMember[]`+`audioUrl`; `study.studyAndBuildSet` reuses the studying checklist then `makeSet`; `api.ts` adds `startSet/getSetStatus/makeSet`; `ExportScreen` now takes `audioPath` (sets export too). Backend live routes left in place but unused (smallest change). **NO live mid-playback append** (that needs gapless splicing — a separate future build; sets are Setup-only, with a note). **Heavy-path ceremony:** the four web `*.test.tsx` files (test-harness guard) went through confirm-and-apply (founder approved twice — Setup batch, then Play/App/Export batch); each keeps every prior check and adds multi-set coverage. **Verified:** typecheck clean; `npm test` **49 web**; `pytest -q` **415 backend** (incl. `test_set_route.py` — real 2-pair render+join, ≤2 cap, off-tempo decline, not-analyzed 409). **Live end-to-end (running app):** built Father Ocean × Der Lagi → Father Ocean × With You — one **15:00** continuous set, seam at **7:22**, **158 MB** `audio/wav`, confirmed **playing** in the stripped Play screen (clock advanced, one `/set/…/audio` request). **⚠️ FILE-SIZE / OPEN ITEM:** a two-set of long tracks ≈ **150 MB** — the two-set cap bounds it, but it belongs on the still-owed `storage.py` cache-eviction sweep (now orphans a new WAV kind, `*.set.wav`). **Open acceptance: founder ear-listen** — does the join between the two sets sound good? (the one thing CI can't judge). `render.py`/`validate.py`/`set_render.py`/`vocal_regions`/`lead_sections` untouched; **nothing merged — a PR is opened for the founder's review.**
- 2026-07-14 (46th) — **BUILD 2 (MVP finish): Silent set-order scoring (feature 5) BUILT — data collection only, changes nothing the user hears. 409 backend green.** When `build_set.py` renders a set it now ALSO computes the order the app WOULD recommend — harmonic (`camelot_fit` between adjacent mixes' beat keys) + an energy arc (build to a late peak, then resolve; a triangular-template MSE score) — ranks every ordering (brute force; a set is a handful of mixes) and LOGS **app-pick vs user-pick vs the delta** to `data/set_order_log.csv` (gitignored). **Silent + safe:** it runs AFTER the set WAV is written (never delays or changes it), is fully `try/except`-guarded (can never break a set), gates nothing, and is zero-cloud (cached analyses + arithmetic). New `workers/set_score.py` (`score_ordering` / `recommend_order` / `log_set_pick`); `build_set.py` gets the guarded background hook. **Demonstrated:** a 3-mix Father Ocean set at `--order 3,1,2` wrote the set in the USER's order and logged `user 2,0,1 vs app 0,2,1` (match=False, delta +0.0039), zero cloud. TDD: late-peak-then-resolve arc / key-adjacency preference / CSV appends both picks + delta / empty-safe. Golden gate green. **This completes all 5 MVP features being BUILT** (1, 2, 4 live; 3 pitch repair wired but shipped OFF pending the founder's ear-test + enable; 5 done). `render.py`/`validate.py`/`vocal_regions`/`lead_sections` untouched; nothing pushed.
- 2026-07-15 (48th) — **🟢 BEST-PARTS shipped to the live app and made the DEFAULT (toggle removed); multi-set merged; 3 catalog beats added (LOCAL-ONLY). `main` @ `ab10f88`; 416 backend + 49 web + typecheck green; `render.py`/`validate.py` UNTOUCHED.** `/zuko:start` → build/fix arc across a long session. **What best-parts is:** crop each FINISHED mix to its ~180 s highlight — edges snapped to VOCAL-SILENT phrase boundaries (measured on the reconstructed output vocal bus, BOTH songs, so no lyric is chopped) — with a low→high BUILD at the start and high→low WIND-DOWN at the end (the engine's own `_build_bed` filter-build, mirrored), then chained via the shipped beat-matched seam. **Post-render only:** `workers/best_parts.py` (`crop_and_arc`) imports `render.py` DSP helpers READ-ONLY and slices the already-rendered WAV; it NEVER re-arranges and is **distinct from `_GOOD_PARTS_WINDOW_ENABLED`** (the old crop-THEN-arrange window, still `False`, untouched — sandbox research this session proved crop-then-arrange STARVES the mix: an 84 s window keeps 0/5 beat-moves, whereas cropping the FINISHED mix at ~180 s keeps ~6/8, because the richness is already baked in). **Shipped in two founder-merged steps:** (a) gated behind a set-screen toggle, OFF by default (PR #3, byte-identical when off); then (b) made the DEFAULT for EVERY mix and set with the toggle removed (merged to `main` @ `ab10f88`). **Backend:** `mix.py` single mixes now serve a best-parts highlight while the FULL `mix.wav` stays canonical (Regenerate re-renders it, the set route crops it); the highlight is built BEFORE the plan is persisted so a 'ready' mix always already has it (fixes a read-before-crop RACE the test caught); a crop failure falls back to the full mix (playback never breaks). `set.py` always crops each member before the join (resilient per-member fallback), permanent `:bestparts` cache key. **Frontend:** the "Best-parts" set-screen toggle was added then removed; best-parts is now automatic; single-mix + Regenerate paths unchanged. **Golden gate:** the render engine is byte-identical (best-parts is a post-render crop, not a render change); the SERVED output is now the ~3-min highlight. A new `test_mix_serves_best_parts_highlight_keeping_full_render` locks it. **Multi-set UI (47th) also MERGED to `main` this session** (PR #2) — the drift it recorded (two-songs-only + live-steering reversals) is now live. **CATALOG — 3 BEATS added (⚠️ `data/library/manifest.json` is GITIGNORED → LOCAL-ONLY, not in git, will NOT transfer to another machine/clone):** Innerbloom (RÜFÜS DU SOL, 122 BPM — copied free from the sandbox cache), Rapture (Black Coffee, 120 BPM/9A) and Anchor Point (Ahmed Spins, 122 BPM/8A) (split + analyzed via **Replicate**, a small paid step the founder OK'd). All three land in the ~120–128 band → blendable; catalog is now **5 beats + 5 vocals**. **STILL OWED (pre-launch, from prior handoffs):** the `storage.py` cache-eviction sweep now also orphans `*.bestparts.wav` + `*.set.wav` (unbounded disk); the R1 hand-off pre-launch items (44th). `render.py`/`validate.py`/`vocal_regions`/`lead_sections`/`_GOOD_PARTS_WINDOW_ENABLED` untouched.

- 2026-07-15 (49th) — **🟢 CATALOG EXPANSION + a D&B "bridge" beat (Merrygo): 7 vocals + 1 beat added, hooks marked, two new planner mechanisms (instrumental-only beats, hand-marked main drops). `render.py`/`validate.py` UNTOUCHED; 420 backend + 49 web + web-typecheck green.** Session across `/zuko:start` → several build/fix loops. **(1) 7 Bollywood/Punjabi VOCALS ingested** (stems+analysis on Replicate, founder-OK'd): Nadan Parinde (133 BPM/11A), Uff Teri Ada (133/10B), Jugni Ji (95/12A), Wari Jawa (122/9A), Tere Bin (103/6B — note: a DIFFERENT song from the existing "Tere Bina"), Mera Yaar (94/11B), Khuda Jaane (80/6B). A reusable operator ingest tool was written: `scripts/ingest_catalog.py` (normalize→store→stems→analyze→manifest; idempotent; retry-safe on Replicate timeouts). **(2) HOOKS marked for all 7** (`app/planner/hooks.py`): 5 placed by section/vocal-region + confirmed by the founder as-is, 2 moved to the founder's ear-marks (Nadan Parinde 1:35–2:05, Jugni Ji 0:09–0:29). **(3) The tempo problem + the fix:** the 4 slowest new vocals (Khuda Jaane 80, Mera Yaar 94, Jugni Ji 95, Tere Bin 103) are too far from the house beats (~120–122) to blend inside the ±11% safe stretch band — declined by design. Founder chose "add slower beats." **A Drum & Bass remix of Khuda Jaane ("Merrygo beat", adiwav) was added as a BEAT** — measured 85 BPM (its half-time feel), which pairs cleanly with all four slow vocals (+6% to −11%). **(4) Two problems found by ear on Merrygo, both fixed with new per-song planner overrides (kept dormant for every other song):** (a) **Overlapping lyrics** — because Merrygo is really a full vocal song, the "both songs trade" flourish was weaving in ~45 s of its OWN Khuda Jaane vocal against Song 2. Fixed with a new **instrumental-only beats** marker (`app/planner/instrumental_beats.py`): a listed beat contributes its music only, never its own vocal (`plan._apply_flourishes` returns no `s1_vocal_regions`). Verified 0 s of Song-1 vocal on all four pairs. (b) **Piano intro bleed** — Merrygo's first 0:00–0:22.68 is Khuda Jaane's piano backing; it was trimmed off (cut on the downbeat nearest 22 s) and the beat re-ingested (final id `4fc82b59…`, ~112 s). (c) **Vocal not landing on the drop** — the D&B beat's energy is too flat for `energy_drops` to detect a drop, so the vocal spread evenly. Fixed with a new **hand-marked main-drop** system (`app/planner/main_drops.py`): the founder marked the drop at **0:40**; `fence.arrangement_options` now snaps that mark to the grid and uses it as THE drop, so Song 2's hook lands on it. Verified the hook anchors at 0:40 (0:37 for Tere Bin, whose grid is tempo-shifted — same musical spot). **(5) ENGINE_VERSION m6.6 → m6.7 (instrumental-only) → m6.8 (marked drops)** so stale cached mixes never replay the old behaviour. **(6) Disk hygiene:** C: hit 0 bytes free twice mid-session (render caches + 1.4 GB of stale pytest temp renders); with founder consent, only regenerable render outputs (`*.mix/bestparts/set/livevocal/vocalbus.wav`) + `pytest-of-Akshay` scratch were cleared (~2.5 GB total) — no stems, analyses, or source songs touched. **⚠️ All catalog additions are LOCAL-ONLY** (`data/library/manifest.json` + stems are gitignored) — they live only on the founder's machine. Code (planner + tests + ingest script) is committed; the catalog audio is not. `render.py`/`validate.py` were NOT edited (re-verify: `git log -1 -- workers/render.py services/api/app/planner/validate.py` predates 2026-07-15).
- 2026-07-16 (49b) — **Process drift (recorded, not hidden): the 49th batch was merged to `main` (@ `eb8098a`) with NO pull request.** `gh` is not installed on the founder's machine, so the agent could not open the PR itself; offered the options (drive their browser / install `gh` / skip the PR / walk them through it), the founder explicitly chose **"skip the PR — merge it directly."** Deviates from CLAUDE.md's "never commit to the protected branch directly — branch and open a PR." Mitigations actually run: the work was committed on a branch first (`feat/catalog-expansion-merrygo-beat`, `518b56b`), pushed, merged `--no-ff` (so the batch is still a distinct, revertable merge commit), and the **full suite was verified green both before AND after the merge (420 backend on merged `main`)**. **Not a new default** — offer the PR path again next session; the durable fix is installing `gh` (founder declined for now).
- 2026-07-16 (50th) — **🐛 SET BUG FIXED: the set tempo is reconciled on each MIX's real playing tempo, not the raw song BPMs. `routes/set.py` only; `render.py`/`validate.py`/`set_render.py` UNTOUCHED. 422 backend green (420 + 2 new).** Founder report: "Set A = Merrygo + a vocal, Set B = another — the sets aren't merged and no transition is created." **Diagnosis (measured, not guessed), two SEPARATE things.** **(1) NOT a bug:** Merrygo is 85 BPM, every other beat is 120–122 → a 1.41–1.44x spread. A set runs at ONE tempo and the safe band allows only 1.247x (`SAFE_STRETCH_LO/HI` 0.89–1.11), so `set_tempo_plan` correctly declines one member rather than warble it. Real and unbridgeable — the band is never loosened (a warbling vocal is unrecoverable; a shorter set is invisible). The only defect here is UX: the app renders first and then reports the drop as a small grey "skipped" card on the Play screen (`PlayScreen.tsx:189-206`), so it reads as "merge is broken". Founder chose NOT to fix the warning this round (offered; deferred). **(2) A REAL, reachable BUG (fixed):** `set.py` fed ALL FOUR songs — both beats AND both vocals — into `set_tempo_plan`, voting with each vocal's ORIGINAL bpm. But a set joins finished MIXES, and inside a mix the vocal is already stretched onto the beat's grid — its raw tempo no longer exists in the render. Proof on real catalog data: **Merrygo+Khuda Jaane (raw 80) + Merrygo+Tere Bin (raw 103)** — the SAME beat, so the mixes play at **85 and 91.67 (1.078x, trivially joinable)** — but the raw vocals look 1.288x apart, so a perfectly good set was **thrown away**. Latent until now: the original 5 vocals were curated near 120, so their raw tempo happened to match their beat; the slow Bollywood vocals (80–103) exposed it. **Fix:** reconcile on `fence.arrangement_options(a1,a2)["master_bpm"]` — ONE entry per PAIR, at the tempo it will really play. The fence is pure arithmetic (no AI, no audio, no render), so this stays cheap and pre-render. Pairs the fence rules unmixable are dropped with the fence's own reason and never vote (previously a 0-bpm entry silently vanished from `set_tempo_plan`'s `keep` AND its `declined`, so it would have been treated as KEPT — a latent second bug closed by the same change). **Caching:** added `SET_PLAN_VERSION = "s2"` to `set_id_for` so this invalidates SETS ONLY — bumping `mixroute.ENGINE_VERSION` would have needlessly re-rendered every (expensive, unaffected) mix. Stale `.set.wav`/`.setmanifest.json` cleared. **Tests (red/green proven):** `test_set_keeps_both_sets_on_one_beat_whatever_the_vocals_original_tempos` FAILS on the old code with the exact bug message ("too far from the set's tempo") and passes on the new; `test_set_declines_a_mix_whose_PLAYING_tempo_is_the_outlier` guards that the genuine 85-vs-122 decline still happens. **Flake noted, not caused by this change:** `test_mix_is_cached` fails with a JSONDecodeError if a live uvicorn server runs concurrently with the suite (it reads the real data dir mid-write); green with the server stopped — spawned as a separate task.
- 2026-07-16 (51st) — **🟢 PER-SEAM TRANSITIONS: a set is never refused for tempo again — a seam its tempos can't blend is CUT like a real DJ. Founder call, ear-tested. `set_render.py` + `routes/set.py`; `render.py`/`validate.py` UNTOUCHED. 427 backend green + lint clean.** **The correction that unlocked it:** the agent twice asserted (wrongly, without reading) that merging 85 with 122 would warble. **`set_render.py` has NO time-stretch at all** — no atempo/resample; it crossfades finished mixes at their NATIVE tempos, and `set_tempo_plan`'s computed master tempo was **never applied** (the route used only its `declined` list and discarded the tempo). So the ±11% band on the SET join was never protecting against a warble — it was inherited from the MIX stage (where the stretch is real) and was only guarding the seam. Measured: an 85+120 join is an **8-bar = 22.6s** overlap of two colliding grids (not "~4 bars" as first claimed). Founder A/B'd two real renders (`A_crossfade` = 22.6s collision vs `B_hardcut` = 15ms cut, same two mixes) and chose the cut. **What shipped:** `set_render.tempo_blendable(bpm_a,bpm_b)` + `CUT_RAMP_SECS=0.015`; `assemble_beatmatched_set` now takes a per-member `bpm` and picks the transition **PER SEAM** — the existing beat-matched phrase blend where the tempos agree (byte-identical behaviour, same 0.89–1.11 band), a **CUT** (no overlap, 15ms equal-power declick) where they don't. Deliberately a **tempo rule, not a Merrygo rule**: no song id is hardcoded, so any future slow beat works with no code change. The route no longer declines for tempo at all (`set_tempo_plan` dropped from its imports — still shipped + tested as engine API); a pair the FENCE can't mix is still dropped (a real mix-level stretch decline). `SET_PLAN_VERSION` s2→s3. **A second bug caught DURING this work, by checking the render instead of trusting the API:** `_seam_positions` is a SECOND copy of the engine's sample accounting and didn't learn about cuts — the audio was a correct 282.33s cut while the manifest claimed 259.75s and put the transition at 67.76s instead of 90.34s (the Play screen would have drawn it in the wrong place). Fixed + pinned by `test_seam_positions_match_the_rendered_set_across_a_cut`, which asserts the manifest against the REAL WAV so the two copies can't drift again. **Verified end-to-end on the live API with real songs:** Merrygo(85)+I Adore You(120) → **kept 2/2**, cut at 90.335s, manifest 282.325s == WAV 282.33s; Merrygo(85)+Merrygo(85) → kept 2/2, **blend** at 67.76s, manifest == WAV. Exactly the founder's rule: a far-off mix cuts only at ITS OWN seams and leaves every other seam on the normal blend (`test_a_far_off_mix_only_CUTS_at_its_own_seam`). **5 new tests** (tempo_blendable band, cut-vs-blend by measured duration, cut-only-at-its-own-seam, the manifest/WAV drift pin, both-kept).
- 2026-07-16 (53rd) — **🟢 HELD-OUT BEATS: a beat can be marked to play AS ITSELF until its own verse, then hand over to Song 2 — founder-designed, ear-approved on Innerbloom × Wari Jawa. New `vocal_windows.py` + `plan.py`/`fence.py`; `render.py`/`validate.py` UNTOUCHED. ENGINE_VERSION m6.8→m6.11. 433 backend green + lint clean.** **How we got here — the 52nd entry was WRONG and was reverted.** It hand-marked Innerbloom's drop at 5:54 on the theory that a melodic swell fooled the loudness detector. The founder rejected it ("de-upgrade that... we are fucked up somewhere") and supplied the real design: Innerbloom's buildup starts ~5:45 and **its own lyrics start at 6:17** — so Song 2 should enter THERE, riding in with Innerbloom's own singing, which then decays away underneath (the m5f.1 natural hand-off). **The vocal entry is not a DROP at all — it is where the BEAT starts singing.** The section map agrees exactly (5:46-6:02 break, 6:02-6:17 inst, 6:17 verse). **What shipped:** (1) **`app/planner/vocal_windows.py` (NEW)** — a per-beat vocal-ENTRY FLOOR, keyed by content id like the hooks/main-drops; Innerbloom `377.0` (6:17). `fence._vocal_entry_floor` snaps it to a downbeat and re-times it onto the grid (same native→grid index mapping as `_hand_marked_drops`); `arrangement_options` filters anchors+drops to >= the floor and exposes `vocal_entry_floor`; an impossible floor is IGNORED rather than stranding the mix into a vocal-less render. (2) **`fence.synced_anchors(..., start=)`** — the arc bands span floor→end instead of 0→end, else every early band comes back empty and the arrangement collapses. (3) **`plan._held_out_lead_in`** — the engine's bed is drums+bass+other with Song 1's vocal EXCLUDED, and `lead_sections` only fills gaps BETWEEN Song 2's placements (the head before the first entry has never been a gap, because that entry is normally early). So without this the held-out head would play as a 6-minute INSTRUMENTAL, not the record; the beat's own vocal is handed back across the whole head (86s on Innerbloom). (4) **After the floor the beat HANDS OVER** — leads/licks that start after the floor are dropped, so Song 2 leads and the beat's vocal stays gone ("it will automatically fade out... have nothing to do with the vocal of Innerbloom"). This also fixed a REAL R1 violation the referee caught: once the floor packed the entries tighter, an 8:39-8:44 Innerbloom lick landed **3.0s inside** an 8:08-8:42 Wari Jawa line — handing over is the right fix, not a wider overlap allowance; `validate.py` untouched. (5) **Founder follow-up ("longer, in more parts"):** `_placement_count` scales the entry count to the held-out window (`_HELD_OUT_ENTRY_EVERY_SECS=40`, capped by distinct slices and `_MAX_HELD_OUT_PLACEMENTS=6`) — 3 entries are sized to span a WHOLE song and left a **94s hole** (6:34→8:08) in a 177s window; and the held-out setup now draws **longest-first from `vocal_slices`** instead of loudest-first from `vocal_peaks`, which on Wari Jawa ranked an **8s scrap FIRST and omitted a 40s passage entirely**. Coverage of the 6:17→end window: Wari Jawa **34%→55%**, Nadan Parinde 45%→77%, Tere Bin(a) 47%→77%, Don't Start Now 45%→65%; all 9 tempo-compatible Innerbloom pairs pass the referee. (6) **Innerbloom's main drop marked 6:17** — with 4 anchors the hook drifted to 7:05 (energy ranks it above 6:17) and 6:17 got an 8s scrap; the mark pins the signature line to the hand-over, as specified. **Two agent process failures worth keeping:** (a) **A burned version string.** m6.9 was re-used for a DIFFERENT behaviour after the 5:54 experiment was reverted, so the reverted render came back as a **cache hit** and was nearly delivered as the fix — caught only by reading the render's own output. Version strings must be unique PER BEHAVIOUR; hence m6.10 then m6.11. (b) **A hand-typed id that matched no song.** Innerbloom's id was first typed from memory as `0759b66b…` — belonging to NO catalog song, so the mark would have failed SILENTLY. Both now guarded by `test_every_hand_marked_id_is_a_real_catalog_song`.

- 2026-07-21 — **Catalog +1 vocal (Dooriyan), no code change.** "Dooriyan" (Love Aaj Kal) was sitting in `song-dropbox/` but had never been ingested, so it was invisible in the app. Added via the existing `scripts/ingest_catalog.py` — 122 BPM, key 7A, 15 vocal regions, id `c4b28366…`; catalog is now **6 beats + 13 vocals = 19 entries**. No source file changed; the manifest, stems and analysis are all under gitignored `data/`, so **nothing about this addition is in git** and it exists only on the founder's machine. Verified in the running app (`/library` reports 19, Dooriyan appears in the Song-2 dropdown) and end-to-end by rendering I Adore You × Dooriyan (3:12) and a 2-set run with Innerbloom × Dooriyan. ⚠️ **Dooriyan has NO hand-marked hook** in `app/planner/hooks.py` — it is the only catalog vocal without one, so it falls back to song order and does not land its signature line on the drop. Owed: a founder ear-mark.
- 2026-07-21 — **The "8 uncommitted web edits" from the 2026-07-16 handoff were a FALSE ALARM — now resolved.** That handoff flagged modified `App.tsx`, `SetupScreen.tsx/.module.css`, `api.ts`, `api.test.ts`, `liveSchedule.test.ts`, `study.ts`, `study.test.ts` as unexplained in-flight work of "unknown intent", to be finished or discarded. They contain **zero changed lines**: `git diff` reports 0 content lines and `git diff --ignore-cr-at-eol --stat` is empty. The files differ only in CRLF/LF line endings (Git warns "LF will be replaced by CRLF"). There was never any in-flight web work. **No decision is needed and the item is closed** — do not re-raise it.
- 2026-07-21 — **DEFECT FOUND, NOT FIXED: "Download full mix" silently saves nothing (`ExportScreen.tsx`).** Reported by the founder as "the export button is disabled". It is **not** disabled — reproduced in the running app (rebuilt their exact 2-set line-up); every button on the Export screen is enabled, opacity 1, `pointer-events: auto`. Root cause found by instrumenting `URL.createObjectURL` / `HTMLAnchorElement.click` / `URL.revokeObjectURL` in the live page: the fetch **succeeds** (blob created, 64,357,820 bytes, `audio/wav`) and the anchor is clicked with the right filename, but (a) `URL.revokeObjectURL(href)` runs **~2 ms after `a.click()`**, cancelling the save before the browser reads a 64 MB blob, and (b) the anchor is **never appended to the document** (`inDom: false`), which some browsers require. A third gap: `res.ok` is never checked, so a failed fetch is silently indistinguishable. **Why the suite stayed green:** `ExportScreen.test.tsx` only asserts `fetch` was called — it never asserts the object URL outlives the click, so it passes on the broken code. **Fix is scoped but BLOCKED on founder sign-off**, because the durable fix edits `ExportScreen.test.tsx`, a dangerous-surface glob (`**/*.test.tsx`). Options put to the founder: fix download + test (recommended), fix download only, or defer.
- 2026-08-05 — **Effect pool built + shipped OFF (heavy path; `workers/render.py` + `services/api/app/planner/validate.py` 🔒).** New product direction: regenerate should give a genuinely different-sounding mix, so each take deterministically picks a different SPACE + WIDTH vocal-effect combo — SPACE {room/hall/plate/predelay (length-preserving reverbs), throw/freeze (final-placement tail-extenders)} × WIDTH {double, none} — seeded from pair+prompt+take (never wall-clock). Follows the founder's 36-variant echo/reverb harness ear-test (he picked long-hall, doubler, freeze, ping-pong; confirmed the o4 throw). **Part 1/2 of that work (harness-only, no dangerous surface):** fixed the two over-level effects (doubler +3.2 dB, freeze +4.3 dB) with an input-dependent joint level-comp → now +1.5 dB with margin; and confirmed via a mono fold-down check that the doubler does NOT collapse on phone speakers (chorus, not phase cancellation). **Design (founder-approved):** cumulative level safe by construction (joint wet-trim keeps a placement's pooled peak ≤ +1.5 dB over dry, whatever stacks); max 2 effects (one space + one width); tail-extenders final-placement only. **Ships OFF** (`plan._EFFECT_POOL_ENABLED=False`) — dormant + byte-identical to pre-pool; `mixroute.ENGINE_VERSION` is DERIVED from the flag (`m6.11` → `m6.11+m7pool`) so flipping it on after the ear-test auto-invalidates the pool-OFF mix + set caches (one flag, no stale cache). **Heavy-path ceremony:** founder approval recorded/cleared on both dangerous files; independent test-author wrote **34 acceptance tests** (all 7 criteria + a three-reader vocabulary cross-check); the adversarial reviewer **CONFIRMED the level guarantee unbreakable and CAUGHT a real HIGH must-fix — a throw/freeze tail ringing over Song 1's outro vocal (two voices, R1), invisible to the dry-length overlap check — FIXED** in the planner (substitutes a safe reverb) + referee (rejects the overlap), re-verified with the reviewer's exact attack. **467 backend tests green.** Ear-test takes (reference + 6 distinct pool takes) rendered to the founder's Desktop (`Prompt-DJ pool ear-test/`). **OPEN: founder ear-test → flip the flag on.** **Backlog (logged, non-blocking):** (a) distinctness-under-substitution not guaranteed on a pair with a strong Song-1 outro lead (a throw/freeze take can map onto another take's reverb → a rare repeat, never a bad mix); (b) some takes pre-decline on the pool-INDEPENDENT R1 arrangement overlap (takes 3 & 7 on I Adore You × Tujhe) — **Regenerate should handle a decline gracefully** (a pre-existing UX gap); (c) `rubberband` pitch throws `std::bad_alloc` on this ARM ffmpeg build, so the product's `_pitch_shift` / Slice 2d may be broken on this hardware — verify before enabling pitch. **⚠️ Disk hit zero THREE times this session under render-heavy work — the `storage.py` cache-eviction sweep is now genuinely urgent (still the standing pre-launch item).**
- 2026-08-05 — **Effect pool AUDIBILITY fix (founder ear-test: "I can't hear the echo OR reverb at all"). `render.py` 🔒.** Diagnosed read-only with hard numbers before any fix (per the diagnose-before-fix rule): the effects DID reach the output but were **inaudible by design**. Root cause proven with a clean same-take A/B (pool ON vs OFF, identical arrangement): the effect sat **−17.6 dB below the mix** (correlation 0.993). The 6 ear-test files had differed only by ARRANGEMENT (different takes), which masked that the effect layer was uniformly too quiet — comparing them was a trap. Contributing factors, all measured: (1) the pool reverb was ~4 dB weaker than the harness the founder validated (wet/dry 0.266 vs 0.416) because the pool kept the dry at unity + 0.28 wet; (2) the peak-neutral +1.5 dB headroom design (chosen for stacking margin) is fundamentally at odds with an audible effect; (3) masking by the dense bed. **NOT a wiring bug** (plan carried `.space/.width`, `_apply_pool` ran, chain_guards didn't reject). **Fix (founder chose "match the harness levels"):** reverb wet 0.28→0.55, joint headroom +1.5→+2.7 dB (near the P2 ceiling), and a bed duck (`_POOL_BED_DUCK ≈ −2.5 dB`) under pooled placements. Result **−17.6 → −9.7 dB below the mix** (clearly audible); stacking (hall+double) **+2.70 dB** peak (P2-safe by construction — the joint trim guarantees ≤ headroom). **Closed the real test gap** the founder identified: new `test_pool_effect_is_audibly_present_in_the_mix` asserts the effect is > −13 dB below the mix (would have failed the −17.6 dB version). 35 pool tests + golden byte-identical green; the render-path regression tests pass in isolation (the full-suite failures this session were all **disk-full**, confirmed — the failing tests pass with headroom). Fresh louder ear-test takes re-rendered to the founder's Desktop. **OPEN: founder ear-test the louder takes → tune by ear if needed → flip the pool flag on.** **⚠️ DISK is now the critical blocker — hit zero ~5× this session; a clean full-suite run needs headroom. The `storage.py` eviction sweep must be built before more render-heavy work.**
- 2026-08-05 — **Cache-eviction sweep BUILT (the standing pre-launch item; disk hit zero ~5× this session). `storage.py` 🔒.** `storage.sweep()` LRU-evicts ONLY regenerable render outputs (`.mix.wav`/`.bestparts.wav`/`.set.wav`/`.livearr.wav`) via a strict suffix ALLOWLIST — never sources/stems/analyses/manifest/approved-config, never subdirs (`listening/`, `tuning_renders/`, `library/`). Auto-hook `maybe_sweep()` at the top of every mix/set render job (only below the 2 GB floor); manual CLI `scripts/evict_cache.py` (dry-run by default, `--apply` to delete). Founder approved the policy (explicit keep-list) before build. **Dogfooded live: reclaimed 1.15 GB (41 stale best-parts renders); catalog verified intact afterward (39 sources, 144 stems, 36 analyses, `library/`, `APPROVED_CHAIN_CONFIG.txt` all untouched); disk 300 MB → 1.7 GB.** **Heavy-path ceremony:** founder approval recorded/cleared; independent test-author wrote 11 acceptance tests (all pass, mutation-verified non-hollow); adversarial reviewer ran 6 attacks — **the catalog-protection contract HELD under every one** (no catalog/stem/analysis/approved deletion, no data-dir escape, no over-eviction), and found two REGENERABLE-data robustness gaps: **F1** (a sweep can evict another job's in-flight/being-served render — TOCTOU in `/mix/{id}/audio` + the set-builder) → FIXED with a 5-min age grace (`_EVICT_MIN_AGE_SECS`) + honest docstrings; **F2** (unbounded evict chasing an unreachable target) → FIXED with a warning log. **Residual (low, logged):** a mix OLDER than the grace can be evicted mid-download by a concurrent sub-floor sweep — regenerable, narrow at validation scale; founder sign-off owed on the concurrency behaviour. **F3:** commit `test_cache_sweep.py` + `scripts/evict_cache.py` so CI pins the allowlist (currently working-tree-only). **Disk is now UNBLOCKED for the cut-ratio prototype.**
- 2026-08-05 — **Phase-throw echo + continuous reverb bed BUILT + shipped OFF (step 3; `render.py` + `validate.py` 🔒).** The founder-settled reverb/echo model: reverb is a continuous BED (always on, rings into gaps), echo is an EVENT firing on ~20-30% of a placement's 4-bar MOMENTS (punctuation, not grammar), 5 cut ratios chosen per moment energy-biased. Reached via a long, ear-driven design loop (cut-ratio prototype → reverb listening set → the finding that reverb-as-bed reads inaudible on a full-mix meter but the founder HEARS it → recalibrating the differs-check to the ISOLATED vocal → the phrase-throw dump that caught come-down monoculture + dead 4:4 before rendering). **Selection** `planner/throws.py` (deterministic, taste params in one block); **engine** `_throw_at_moments`/`_reverb_bed` are LENGTH-PRESERVING (carry + faded tail inside the placement; reverb truncated to vocal length) so placement_end/R1 are unchanged and the F1 overrun class is designed out; `_punchy_bar` re-fires only a VOICED downbeat-aligned bar (the Father Ocean 3:56 breath fix); one mechanism owns echo (clears `Placement.echo` when on); ships OFF, `ENGINE_VERSION` derives `+m8throw`. **Heavy-path ceremony:** founder approval recorded/cleared; test-author wrote 17 acceptance tests (incl. byte-identical-OFF-with-`echo`, throw-rate 24%, ratio-distinctness on a long placement, the breath-defect on REAL Der Lagi audio — all mutation-verified non-hollow); adversarial reviewer ran BOTH founder-ordered attacks — **tail containment (both kinds) + byte-identical-OFF held** (byte-identity proven vs `HEAD:render.py`), verdict `safe` for the OFF ship — and flagged two flip-on residuals, both FIXED: reverb bed additive P2 thin margin (joint-trimmed to ≤ +2.0 dB, verified on a hot vocal) + a flaky Windows temp/ffmpeg test (retry). **The differs-check** `scripts/audio_diff.py` recalibrated to the isolated vocal (spectral metric), validated to separate the bit-identical vs audible reverb pairs. Full affected suite green (golden gate intact). **OPEN: founder ear-test of the echo+reverb listening set → flip the flag on.** **Backlog (logged):** throws spread by count not musical opportunity (long placements may want more — ear-check); commit step-3 + step-2 files so CI pins them; the effect-pool selection layer is now dormant (superseded).
- 2026-08-05 (late) — **Rule 4 CUT BACK HARD — the throw/cut-ratio model was the WRONG feature (founder call). Ships OFF; NOT deleted yet — plan-first, no code until sign-off.** The re-fire (`_punchy_bar` re-firing a voiced bar across the carry bars) is **chop-and-repeat = Rule 3**, which leaked into Rule 4; the whole sing/carry cut-ratio model was built around it and goes with it. **The re-fire IS firing** (verified this session: 9/9 throws re-fire a voiced bar, +10.3 dB over the decayed tail in the 2nd carry bar) — it works, it's just the wrong rule. **THE SIMPLIFIED RULE 4 SPEC (the new truth):** take Rule 1's vocal EXACTLY as-is (do NOT cut/shorten/chop/re-fire the vocal), and add echo + reverb ON TOP — ONE variation, always both together (no echo-only / reverb-only / per-take selection). Echo fires **at the END of a vocal line, sized to fit the GAP before the next vocal** (longer gap → longer tail; no gap → no echo). Reverb runs continuously underneath (as built). **DELETE from Rule 4 (next session, after sign-off):** the 5 cut ratios, sing/carry gating, `_punchy_bar` + the re-fire, throw-moment selection by energy, the energy→ratio mapping, the three variations, and the effect-pool selection layer. **PRESERVE `_punchy_bar` + the breath fix retrievably** (it's genuinely useful for Rule 3 later — it's in git history + `workers/render.py` until the simplification). **KEEP:** the level fix, the joint wet-trim, `chain_guards`, the length-preserving tail containment, the recalibrated isolated-vocal differs-check (`scripts/audio_diff.py`), the reverb bed, and the byte-identical-OFF flag structure. **GAP-DETECTION FINDING (grounds the rebuild):** Song 2's vocal STEM energy has **9 clear troughs (line gaps) across 59 bars (std 0.278)** while `vocal_regions` (segmentation) is an **86% blob** — so vocal-stem RMS troughs (reliable, just loudness) are the way to find line-ends + gap lengths, NOT segmentation. **DO FIRST NEXT SESSION:** report whether vocal-stem energy troughs give reliable line-end points AND gap lengths (smallest reliable measure of "the gap before the next vocal", reliable signals only) → propose the simplified implementation (small) → founder sign-off → build. What this session built (Items 1-3 of the old model: length-weighted throws, three-variation render, re-fire verification) is superseded by the cut-back but its KEEP-list parts carry forward.
- 2026-08-05 (simplified Rule 4 BUILT — the cut-back done, ships OFF; `render.py` + `validate.py` 🔒) — **Gap-detection reported first (founder gate), then the simplified Rule 4 built + full ceremony.** **Gap-detection finding:** reproduced the trough measurement across ALL 36 catalog vocals with the reliable detector (fresh 40 ms RMS envelope on the isolated vocal stem + hysteresis onset 0.15 / offset the trusted voiced floor 0.08 + min-gap; **never `vocal_regions`**, which reads a median 78% blob): **median 28 line-ends and 15 echo-worthy gaps (≥0.8 s) per vocal; fires on 34/36 (94%); correct silence on the 2 wall-to-wall vocals.** Every failure mode errs toward a SHORTER/ABSENT echo, never an overrun. **Built:** `_voiced_runs` + `_gap_echo` (echo at each line-end, tail = `min(gap − guard, cap)`, cap **tempo-derived in bars**, taste knobs in ONE named block) + kept `_reverb_bed`; wired behind renamed flag `_RULE4_ENABLED` (accessor `rule4_enabled()`, `ENGINE_VERSION` tag `+m8echo`); `build_mix_plan` just flags `reverb_bed=True` (no per-throw plan data — the engine detects from audio). **Independent containment net** `render._echo_overruns` (render-side, like P2 — the tail is gap-sized by construction AND re-checked; kept per founder). **Deleted:** `throws.py`, `_throw_at_moments`/`_throw_echo`/the re-fire, the 5 cut ratios/energy→ratio model, the `Placement.throws` field, `validate._throw_violations`, and their tests (`test_phrase_throw.py`, `test_phrase_throw_e2e.py`). **Preserved:** `_punchy_bar` + the Father Ocean 3:56 breath fix as a real, self-contained parked helper `workers/rule3_parked.py` (comment says what/why parked for Rule 3) + a retargeted real-audio regression test `tests/test_rule3_parked.py`. **Kept:** level fix, joint wet-trim, `chain_guards`, length-preserving tail containment, `scripts/audio_diff.py`, reverb bed, byte-identical-OFF flag structure. **Tests:** `test_gap_echo.py` (10 hermetic acceptance + reverb-bed length) + independent test-author set; **490 backend tests green** with Rule 4; **byte-identical-OFF golden gate passes** (OFF == main). **Listening set** rendered (both takes pass the referee, peak 0.891, length unchanged; isolated-vocal spectral diff 1.28 / 0.79 ≫ 0.02 → echo/reverb audibly present) to the founder's Desktop `Prompt-DJ Rule4 gap-echo listening (2026-08-05)/` + README. **Ceremony:** founder approval recorded/cleared on both dangerous files; independent adversarial-safety-reviewer + test-author run (prove-it-unsafe on byte-identical-OFF, containment/R1, level/clip, determinism, dead-refs). **Pre-existing suite flake surfaced (NOT Rule 4):** the full suite intermittently fails ONE `test_cache_sweep.py` test — proven cross-file pollution from `test_mix_route.py` (both untouched; passes in isolation; reproduces as a 2-file pair); spun off as its own task. **OPEN: founder ear-test the listening set → tune the tail cap by ear if needed → flip `_RULE4_ENABLED` on. Nothing merged to `main`.**
- 2026-08-05 (Rule 4 FINALLY approved by ear — a REAL delay echo; ships OFF; `render.py` 🔒) — **New working rule adopted after THREE thrown-away Rule-4 features: prototype the SOUND in a throwaway script and get founder ear-approval BEFORE any production build/ceremony** (the build isn't the expensive part — building the wrong thing carefully is). Sound-first prototyping (throwaway `scripts/` + dated Desktop folders, 3-4 variations each, minutes per loop) converged fast: the founder rejected the gap-sized echo ("forceful, not DJ-like"), one-word echo ("hired background voice"), phrase-tail STAMP ("chop/stutter, from where did this come"), and a beat-boundary-anchored echo ("1-2s late"). **APPROVED: variant `d_quarter_long` = a real tempo-synced feedback DELAY on the vocal — 1/4-note delay, feedback 0.55, wet 0.45 — + the continuous reverb bed.** **Built into the real engine (`render.py`):** `_delay_echo` (wet-only feedback delay), joint level-trim (`_max_wet_gain`, +2.5 dB) + `chain_guards`, and `_echo_overruns` adapted to guard ONLY Song-1 leads (a Song-2 echo over Song-2's own next line is delay of the same voice, allowed). Deleted the gap-echo engine (`_gap_echo`/`_voiced_runs`/`_gap_echo_events`/`_duck_bed_under_echoes`/`_GAP_ECHO_*`) + its tests. **Real render_mix (flag ON) reproduces the approved prototype at 0.993 full-mix correlation; passes plan+render referee, peak 0.891, no clip; the pair has 4 Song-1 regions so the S1-containment is exercised.** **Bug caught by the golden gate during the build:** the new `_DELAY_ECHO_*` knobs were originally `_ECHO_*` and one (`_ECHO_FEEDBACK`) COLLIDED with the legacy produced-drop echo constant, changing the OFF path — renamed to `_DELAY_ECHO_*`; golden byte-identical-OFF restored. **Ceremony:** founder ear-approval + file approval recorded; independent test-author (`test_echo_independent.py`) + adversarial reviewer running. **189 affected tests green** incl. the golden gate. **OPEN: flip `_RULE4_ENABLED` on. Nothing merged to `main`.** See memories [[prototype-sound-before-ceremony]] + [[rule4-approved-delay-echo]].
- 2026-08-05 (Rule 4 DEPLOYED — the echo is LIVE; `render.py` + `plan.py` 🔒) — **Founder chose the BOLDEST echo level and deployed Rule 4.** After A/B-ing louder variants on Innerbloom×Dooriyan and Father Ocean×Der Lagi Lekin (rendered via the prototype-then-listen loop, echo levels −8/−6/−4 dB vs the vocal), the founder picked the boldest. **Set `_DELAY_ECHO_WET = 0.45 → 1.10`** (echo first tap ≈ −4.4 dB vs the vocal) and **flipped `_RULE4_ENABLED = False → True`** — Rule 4 is now the shipped behaviour: every mix renders with the 1/4-note delay echo + reverb bed, and `ENGINE_VERSION` gained `+m8echo` so the mix/set caches auto-invalidated and re-render with the echo. Level stays safe by construction (the joint `_max_wet_gain` trim caps the echo+reverb ≤ +2.5 dB over the dry, under the chain_guards +3 dB P2 ceiling — so a louder wet cannot clip or fail a render; on a hot pair the echo is trimmed, never the mix broken). **Ceremony:** render.py file-approval recorded; a second adversarial pass reviewed the LIVE activation at the louder level (level/clip/crest at wet 1.10, cache invalidation, stale OFF-state test pins). Updated the pinned-constants + ships-off tests to the live reality (`_DELAY_ECHO_WET == 1.10`, `_RULE4_ENABLED is True`, `+m8echo` in ENGINE_VERSION). **The OFF path stays byte-identical to the pre-Rule-4 engine (golden gate still passes) as a proven fallback.** Rule 4 is DONE — moving on from it.
- 2026-08-06 (Change ① — BPM band widened ±11%→±15%; PR open, NOT merged; `validate.py` behaviour changes by import) — **Founder-approved by ear** (on the real full demo mixes + the Innerbloom×Tere Bin "ribbon" takes) as the first of two production changes from the key+BPM prototype. Widened `fence.SAFE_STRETCH_LO/HI` 0.89/1.11→**0.85/1.15** and `WARP_LO/HI` 0.85/1.15→**0.81/1.19** (kept ±0.04 wider, moved together so per-bar re-lock still holds at the new edge); recovers ~15 more catalog pairs, still locks on-beat. **SINGLE SOURCE OF TRUTH:** `workers/set_render.py` now IMPORTS the band from `fence` (was a hardcoded 0.89/1.11 copy — the drift trap closed); `validate.py` already imported it, so B3/R7 widen in lockstep (the referee's warble ceiling moves with the planner — correct-by-design, invisible in its own diff). `_ENGINE_VERSION_BASE` "m6.11"→"m9band15" (mix_id + set_id both fold it → every stale ±11% mix/set re-renders; per-song analysis/stems are band-independent, untouched). **Ceremony:** founder "go" recorded, `approve.js` on the 5 test files; independent **test-author** updated the 6 declines-far-tempo example pairs (150→158, still declines — verified) + the ENGINE_VERSION assertion, and wrote `tests/test_bpm_band_regression.py` (**MONOTONICITY** — widening only ADDS pairs, proven on an 80–160 grid; edge-pair no-wrong-way-flip; single-source `is`-identity). **TWO independent adversarial reviewers** (correctness/blast-radius + reversibility/tests/cache) both returned **SAFE** — monotonicity proven analytically + on 78k pairs (0 removed, 8952 added), atempo/warp bounds hold with margin, no test weakened, cleanly reversible. **~504 backend tests green** (the lone `test_cache_sweep.py` flake is the known pre-existing order-dependent one). **Residual risk = the ear only** (does sustained ±15% / per-bar ±19% warble) — the founder's prior ear-approval closes it. **Process finding (flagged, not fixed):** Python `test_*.py` are NOT in `dangerousGlobs` (only `*.test.ts/tsx` are), so the guard would not have blocked these test edits — worth guarding. Nothing merged to `main`. See [[key-bpm-matching-approved]]. **Change ② (KEY matching) is next.**
- 2026-08-06 (Change ① MERGED; Change ② KEY matching BUILT + pushed, PR open, NOT merged, NOT re-cleared) — **Change ① (BPM ±15%) merged to `main` (`af90618`).** **Change ② (KEY matching) built on branch `feat/key-matching` (`00e9f33`, 18 files):** shift Song 2's vocal into a compatible key (fuzzy Camelot, cap ±2) BEFORE the mix, via a local Signalsmith Stretch helper (MIT, WASM-in-headless-Chromium, validation-grade). New non-dangerous: `app/planner/keys.py` (fuzzy_key_shift + resolve_key_shift with a CONFIDENCE GATE — 5 flagged songs + low-confidence keys → shift 0, logged), `app/audio/pitch.py` (executor: two independent renders must agree; content-addressed cache song-id+shift+formant+HELPER_VERSION; bounded-retry-then-`PitchError` = LOUD failure → the mix declines visibly), `app/audio/chroma.py`, `app/audio/pitch_helper/`. **Dangerous surfaces (founder confirm-and-apply):** `render.py` DELETED the GPL rubberband `_pitch_shift` (proven dead — only caller gated on `pitch_semitones`, always 0 in the live path; **golden mix byte-identical** before/after, hash `ec40e837…`; call site now RAISES on a nonzero pitch = never silently un-shifts); `validate.py` ADDED rule **K1** (independently re-derive the shifted vocal's chroma; decline a DECISIVE wrong rotation — consistency ≠ correctness; existing rules untouched); `storage.py` added `.pitchshift.wav` to the eviction allowlist. Wiring in `routes/mix.py` + `routes/live.py`; ENGINE_VERSION `+m10key` (off-switch drops it → byte-identical to pre-②). **Ceremony:** founder ear-approval + file approvals recorded/cleared; independent test-author wrote keys/pitch/K1/render-guard/sweep/mix-route tests (**529 backend green**); TWO adversarial reviewers found (A) the retired live-bus path served an un-shifted vocal [DEAD CODE — no UI caller — fixed for defense], and (B) K1 could mis-judge chroma-flat vocals [fixed via the decisive-disagreement rule]. **BLOCKER: the RE-REVIEW of those two fixes did NOT complete (session usage limit) — Change ② is tested-green + end-to-end verified but NOT independently re-cleared and NOT merged.** DO FIRST next session: re-run the two reviewers on the fixes; finish `technical-spec`/`functional-spec` for key-matching; then the founder decides on merge. Residuals: CI runs vitest not pytest (the ② safety net is local-only); Anchor Point gated despite an approved demo; a helper timeout can orphan Chromium on Windows. Nothing merged to `main`. See [[key-bpm-matching-approved]].
- 2026-08-06 (Change ② re-review COMPLETE + live-path hardened + docs finished — still NOT merged) — **The blocker above is cleared.** Re-ran TWO independent adversarial reviewers on the Change ② fixes. **K1 (`validate.py` decisive-disagreement rule): BOTH returned PROCEED/SAFE** — they attacked the sign math for ±1/±2 shifts, the `0.15` margin, the chroma-flat false-pass, and crash/false-decline; the only flat-pass cases are harmonically neutral, and K1 is confirmed wired into the real serve path (`mix.py` `_run_mix`, before render, declines loud). The reviewers **split** on the live-bus fix: R1 said PROCEED (it is dead code — I re-verified: no shipped component mounts `LiveMix`/`liveAudio`), R2 flagged a latent hole (the live path applied the shift but SKIPPED the K1 referee, so a swept `.pitchshift.wav` cache could let a stable-but-wrong re-render through IF that screen were ever re-enabled). **Founder chose to close it now:** `routes/live.py._run_vocal_bus` now runs `validate.assert_key_shift` on the shifted vocal, mirroring `mix.py` (commit `c66976f`, pushed to `feat/key-matching`). No behaviour change today (path unmounted); 86 targeted tests + 528/529 backend green (the 1 = the known disk-dependent `test_cache_sweep` flake). **Docs finished to mirror the code:** `technical-spec.md` (the stale GPL-pitch ⚠️ resolved → stage retired / key-matching moved upstream; the old Slice-2d in-render pitch note marked SUPERSEDED; new Change ① and Change ② as-built sections) and `functional-spec.md` (2026-08-06 plain-language update: wider tempo band + auto key-match, with the 5 gated songs named). **Correction:** the pitch helper is a **Node subprocess** (`node bridge.mjs`), NOT headless Chromium as earlier handoff notes loosely said (the code is the truth). **Change ② is now fully re-cleared; only the founder's PR review + merge decision remains.** See [[key-bpm-matching-approved]].
- 2026-08-06 (RULE 3 design finalized + consolidated into a Rule Book; code build NOT started; no dangerous files touched) — **After a long sound-first experiment loop (throwaway harness + dated Desktop folders, founder ear-reacting per pair), Rule 3 (chop-and-repeat) is design-finalized and written up as [`docs/RULEBOOK.md`](RULEBOOK.md)** — the new canonical description of ALL the mixing rules. **The consolidation the founder asked for:** (1) **BPM tracking+stretching and key tracking+changing are documented as the shared FOUNDATION of every rule** (Rule 1/3/4) — the pair is matched (vocal stretched to the beat's grid bar-by-bar; vocal key-shifted to fit, cap ±2–3 st) or DECLINED, before any rule's arrangement runs. (2) **Rule 3 spec, finalized:** sits on Rule 1's plan; the chop is a hand-picked HOOK line (curated `hooks.py` marker, or a founder timestamp — e.g. How Deep 61–71s); two blocks — **A** short "tease", **C** full "payoff" sentence (looped), **B** never played alone; **beat-locked** (cut on the vocal's own downbeats, each bar warped to a beat bar — no drift, every repeat starts on the beat); **word-safe endings — the LAW the founder twice flagged: never hard-cut the last word; it finishes past the bar line and FADES out** (regressed once when the harness was generalized — now written down); Rule-4 **echo+reverb tail** on each chop; **not too tight** (spaced/gated, on-grid pauses allowed); **trade** — the beat song's OWN vocal is KEPT and the chops answer in its instrumental gaps (call & response), or drop the beat vocal for a vocal-heavy/short beat. (3) **The set builder** vision recorded: a set = a sequence of mixes, each with a chosen rule (Rule 1→2→1→3→4…), foundation applied to every one. **Rule 2 = reserved, deliberately NOT integrated now (founder's call).** **Ear-tested in the harness** on Der Lagi Lekin×Father Ocean (the primary), plus generalized to Innerbloom×Dooriyan and Anchor Point×How Deep (How Deep freshly ingested — 20th catalog entry). **NEXT = the code build (heavy, dangerous-surface):** promote the harness into the real engine path — a Rule-3 arrangement in the planner + a chop-and-repeat renderer + validator coverage + a per-mix rule selector, touching the guarded `workers/render.py` / `planner/validate.py` on the confirm-and-apply safety path, with the functional & technical specs updated in that same change. Nothing merged; nothing in the app changed yet.
- 2026-08-06 (RULE 3 code build STARTED — Stages 2 & 3 built + tested; NOTHING wired/merged; NO guarded files touched) — **Founder said "build Rule 3 = the approved chop-and-repeat." Two core modules promoted from the throwaway harness into real, tested code, both NEW non-guarded files:** (1) **`app/planner/rule3.py`** — the Rule-3 PLANNER brain (deterministic, no audio, matching the codebase's brain/engine split): `pick_blocks` (A tease + C full sentence UNITS from the curated `hooks.py` hook or a founder timestamp; a unit is a list of phrases so C fades word-safe at each internal join; B never alone), `voiced_phrases`/`word_end_after` (word-safe endings — the last word's natural end, carried for the renderer to fade), `instrumental_gaps` (trade: beat's gaps, or whole track if `keep_beat_vocal=False`), `schedule` (weave A/C on the grid with on-grid pauses). (2) **`workers/rule3.py`** — the Rule-3 RENDERER: executes a `Rule3Plan` into a WAV by REUSING render.py's shared primitives (`_vocal_take_warped` beat-lock, `_echo`, `_reverb`, `_sum_stems`) **by import — so render.py is NOT edited** (a future `_dsp.py` extraction would clean the private-import coupling but touches the guarded file, deferred). BPM+key foundation is applied UPSTREAM (caller passes the tempo/key-matched vocal). **Tests (Python, non-guarded): 7 green** — `test_rule3_planner.py` (6 hermetic: phrases, word-end-stops-at-boundary, A-short/C-full, auto A+B→C, trade-vs-whole-track, schedule-on-downbeats-in-gaps) + `test_rule3_render.py` (1 integration: real ffmpeg DSP on synthetic audio → valid non-silent correct-length WAV with the 220 Hz chop present at its scheduled downbeats and absent in the bed gaps). All key modules import clean alongside the live engine; **the existing Rule-1/4 flow is untouched. Finding: reusing the guarded files' functions by import means the guarded `render.py`/`validate.py` likely need NO edits — `validate.assert_render` can guard the Rule-3 output by call, not by change.** **NEXT (Stage 1): wire the rule selector into `routes/mix.py`** (a `rule` field + cache-id + a `_run_mix` branch that builds the Rule3Plan and calls `render_rule3`, then `assert_render`) — a careful integration into the LIVE serving/caching path (MixPlan persistence, best-parts, `_ready`, set-join) that must not break Rule 1/4 — then a rule picker in the web UI + spec updates. Nothing merged.
- 2026-08-07 (RULE 3 WIRED into the live mix route + Rule 4 gated per-rule + best-parts made COMMON; 223 affected tests green; NO guarded files edited) — **Rule 3 is now reachable through the app and Rule 4 is a distinct pick, per founder direction.** (1) **Rule selector wired:** `MixRequest.rule` + `MixPlan.rule` (additive, default 1 = byte-identical), `mix_id_for(..., rule)` folds a `:r{rule}` tag ONLY for rule≠1 (existing rule-1 cache ids unchanged), `start_mix`/`_run_mix` thread it; `_run_mix` branches to a new `_render_rule3` (builds the `Rule3Plan` from the curated hook + the beat's gaps on the SHARED already-tempo/key-matched grid, then `workers.rule3.render_rule3`) — Rule 1/4 keep the exact `render_mix` path. **Verified end-to-end:** a real Father Ocean×Der Lagi Lekin Rule-3 mix rendered (476 s, valid), plan persisted `rule=3`. (2) **Best-parts is now COMMON to every rule** (founder correction — the highlight crop + set-transition treatment must apply to Rule 3/4, copied from Rule 1's `best_parts.py`): removed the rule-3 skip; a Rule-3 mix crops 476→**189 s** highlight. (3) **Rule 4 gated per-rule** (founder: rule 1 = dry, rule 4 = echo): `build_mix_plan(rule=...)` sets `reverb_bed` (the per-placement echo+reverb trigger) ONLY when `rule==4` — a **planner-only** change, the guarded `render.py` UNTOUCHED. The default mix is now **DRY** (was always-echo since the 2026-08-05 Rule-4 deploy); `_ENGINE_VERSION_BASE` "m9band15"→**"m11rule"** so every stale echo-default mix re-renders under the new gate (founder-approved: dry default, echo on Rule 4). **Foundation (BPM stretch + key shift + fence decline) still runs first for EVERY rule** — Rule 3/4 inherit it. **Tests:** 223 green across plan/mix-route/render/echo/validate/rule3 (pinned ENGINE_VERSION test updated m9band15→m11rule). **STILL OPEN before final testing:** per-song rule selection in the SET builder (set route threads a rule per pair; the web set UI gets per-song Rule 1/3/4 pickers) + VERIFY a Rule-3 mix joins a set with the shared `set_render.py` transitions (first song opens low, next drop lands 4/8 beats after the seam). Web plumbing (`api.ts`/`study.ts` `rule` param) threaded; the picker UI is the set-builder piece. Nothing merged.
- 2026-08-07 (SET BUILDER with PER-SONG rule selection — the four-rule product integration COMPLETE; 40 set + 49 web tests green) — **Users can now build a set and pick a rule per song, and the shared best-parts + transitions apply to every rule.** BACKEND (`routes/set.py`): `SetPairRequest.rule` (default 1); `set_id_for` folds a per-pair `:r{rule}` tag (omitted for rule 1 → all-Rule-1 set ids stable); `_run_set` threads the rule → `mix_id_for(...,rule)` + `_run_mix(...,rule)` so each song renders under ITS rule; the tempo/mixability reconciliation stays rule-independent (the shared foundation); the seam engine (`workers/set_render.py`) joins the finished, best-parts-cropped mixes unchanged — **so the transitions (blend where tempos agree, CUT where they don't; first song opens low; the next drop lands 4/8 beats past the seam) apply to Rule 3/4 for free, because a set joins finished mixes and reads only rule-independent metadata (wav + phrase_starts + bpm).** FRONTEND: `SetPick.rule` + a per-song **rule picker** (Simple / Chop & repeat / Echo pills) in `SetupScreen`, threaded `App → study.ts → makeSet/makeMix → api.ts` (all additive, rule defaults 1). **VERIFIED end-to-end: a mixed-rule set (song 1 = Rule 3 chop, song 2 = Rule 4 echo) rendered — both kept, joined into one 362 s set, seam at 173 s.** Tests: web typecheck clean, 49 web + 40 set green (+ the 223 backend from the rule wiring). **Rule 2 remains reserved (not integrated, founder's call).** The four-rule product (Rule 1/3/4 selectable per song, on the shared BPM+key foundation, with common best-parts + transitions) is now BUILT and internally verified — ready for the founder's final product testing. Nothing merged; specs (functional/technical) still to be updated to mirror this in the merge PR.
