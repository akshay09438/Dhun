# Prompt-DJ — Technical Spec (V1)

_How it is built. Starts as the intended design (from the PRD + discovery deltas); becomes as-built as code lands. Living document — if code and this doc disagree, the code wins and this doc is corrected. Full background: [reference/PRD.md](reference/PRD.md)._

## The one architectural principle (do not violate)

**The language model plans. The audio engine executes. They never mix.** The LLM turns (analysis + request) into a structured `MixPlan` (Feature 1) or a `LiveOp` (Feature 2). A deterministic render/playback engine executes those objects with DSP. The LLM never touches audio samples. This buys: editability, near-zero LLM cost, no hallucinated audio, and full debuggability.

## Stack (right-sized for validation scale; audio toolchain kept best-in-class)

| Layer                | Choice                                                             | Notes                                                                                           |
| -------------------- | ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------- |
| Frontend             | React + Vite + TypeScript, wavesurfer.js, Web Audio + Tone.js      | upload, arrangement view, stem-bus player, live prompt bar                                      |
| Backend              | Python 3.11 + FastAPI + Pydantic v2                                | Pydantic schemas = data source of truth                                                         |
| Jobs                 | **In-process background tasks** (not separate Redis + workers yet) | upgrade to Redis/RQ when past validation scale                                                  |
| DB                   | **SQLite** to start                                                | upgrade to Postgres when it grows; no audio-quality impact                                      |
| Storage              | **Local disk** (`data/`, gitignored) to start                      | upgrade to Cloudflare R2 / S3 later                                                             |
| Stems                | **AudioShake / Music.ai API** (or self-host Demucs on Modal later) | 🎯 quality-critical — best-in-class                                                             |
| Analysis (MIR)       | librosa + madmom + Essentia + allin1                               | BPM/beatgrid/downbeats/key/sections/energy/vocal regions                                        |
| Time-stretch / pitch | **SoundTouch (free)** for V1 by choice                             | kept swappable to Rubber Band (paid, better on big stretches) in one file; keep stretches small |
| Mux / encode         | FFmpeg (LGPL build) + numpy                                        |                                                                                                 |
| LLM                  | Anthropic Claude API (structured output)                           | `MixPlan` + `LiveOp` only                                                                       |

**Why simplified vs. the PRD:** the PRD pins Postgres + Redis + separate workers + object store for scale. For ~50 users we run a single FastAPI app + SQLite + local storage + in-process jobs. This is _plumbing only_ — it has zero effect on how the mix sounds, and each piece upgrades independently without touching the audio engine.

## Core data models (Pydantic — the source of truth)

`TrackAnalysis` (per song, cached by content hash) · `StemSet` (per song, cached) · `MixPlan` (Feature 1 spine: bed + topline + sync + arrangement[] + fx) · `LiveState` + `LiveOp` (Feature 2). See PRD §5 for full schemas.

## Feature 1 pipeline (stage by stage)

Ingest (FFmpeg normalize) → Analysis (cached, with per-field confidence) → Stem separation (cached) → Arrangement planning (Claude + deterministic helpers + validator) → Tempo lock (SoundTouch) → Key lock → Beat alignment → Render/mixdown (flat file + stems-preserved live bundle) → Post (loudness normalize + limiter). See PRD §6.

## The judgment layer (the moat)

Deterministic helpers the LLM calls (never guesses): `snap_to_phrase`, `camelot_fit`, `stretch_ratio`, `vocal_regions`, `section_windows`. The LLM picks taste among _legal_ options; a validator (`planner/validate.py`) enforces the hard rules it can check against the plan and the real render: **R1** one vocal at a time (Song 2 placements never overlap, and Song 1's contrast vocal never overlaps them), **R2** one bassline (by construction — only Song 1's), **R3** every entry on a Song-1 downbeat, **B3** the tempo stretch inside the safe band, **R6** the finished audio neither clipping nor silent, **R7** a beat-locked vocal re-locks cleanly (per-bar ratios in band, boundaries on the grid). Two further craft goals — a **key-clash guard** and **≥2 distinct vocal placements** — are _shaped upstream by the fence and driver_ (key fit is computed as informational `key_fit`; the arc arranger spreads 2–3 placements), **not** referee-enforced. Full rulebook: [reference/DJ-Judgment-Handbook.md](reference/DJ-Judgment-Handbook.md) (Parts 1–8 craft, Part 9 confidence/fallback, Part 10 live orchestration).

## Feature 2 (live, lean)

Playback holds separately-addressable stem buses (`s1.drums, s1.bass, s1.other, s1.vocals, s2.vocals`). Commands become on-beat `LiveOp`s (energy/element moves only in V1). Scheduler uses Song 1's grid as the master clock (Tone.js Transport / server-authoritative to start). Two invariants: music never stops; nothing fires off-grid. **Live BPM change is out of V1** (V2 stretch goal).

## The dangerous 5% (mirrors `.zuko/config.json`)

Secrets/keys · the upload handler (untrusted input) · storage deletes (irreversible) · the render pipeline + quality validator · CI/test harness. No real auth or payments in V1.

## As-built (M1)

- `services/api/app/`: `config.py` (limits), `audio/normalize.py` (FFmpeg two-pass peak-normalize → 44.1k/stereo/16-bit, with a subprocess timeout), `storage.py` (content-hash save/serve, hex-id validation), `routes/songs.py` (streaming size-capped upload + serve), `main.py`. 10 pytest tests.
- `apps/web/`: React+Vite+TS upload screen (`components/Uploader`), `lib/api.ts` client. 5 vitest tests.
- Time-stretch not yet wired (arrives with mixing in M3); SoundTouch chosen for then.

## As-built (M2a — stem splitting)

- **Runs in the cloud, not locally.** PyTorch/Demucs can't run on the founder's Windows-ARM machine (proven), so separation calls **Replicate** (`ryan5453/demucs`, htdemucs) over HTTP. The `replicate` client is pure-Python and runs fine locally.
- `services/api/app/audio/stems.py`: `separate_stems(song_id, wav)` → 4 stems, **cached by content id** (no repeat API cost). `app/routes/stems.py` is **asynchronous** (a ~2min cloud job can't be held on one request — long songs timed out): `POST /songs/{id}/stems` starts a background thread and returns `202 processing`; `GET /songs/{id}/stems` reports status (processing/ready/error); `GET /songs/{id}/stems/{stem}` serves a part (id+stem validated). In-memory `_jobs` registry (single-worker validation; persisted table when hosted). `StemSet` = {song_id, status, stems}. 8 tests (mocked Replicate).
- `apps/web`: each song card gets a "Split into parts" button → 4 stem players. +1 test. 6 web tests total.
- **Keys:** `REPLICATE_API_TOKEN` (+ `ANTHROPIC_API_KEY` for M3) in a gitignored root `.env`, loaded at startup via `python-dotenv` in `main.py`.
- **Cost:** ~2–6¢ per song, cached; validated live end-to-end.

## As-built (M2b — analysis)

- **Hybrid cloud + local math.** Rhythm/structure from Replicate `sakemin/all-in-one-music-structure-analyzer` (allin1): BPM, beats, downbeats, sections. Local pure-numpy (`app/audio/analysis.py`): key (FFT chromagram + Krumhansl–Kessler profiles → Camelot, confidence = winner margin), energy curve (RMS/bar), vocal regions (RMS of the split vocal stem/bar), phrase starts (every 8th downbeat), bpm confidence (beat-interval regularity). librosa/madmom/essentia cannot install on this ARM machine (numba/llvmlite lack wheels) — hence pure numpy.
- `app/routes/analysis.py`: async start-then-poll like stems; result cached as `{id}.analysis.json`. `TrackAnalysis` model carries per-field confidence (sections pinned at 0.6 — the known weak link the planner must distrust).
- `apps/web`: "Analyze track" per song → BPM chip, Camelot key chip (confidence on hover), proportional section-timeline bar.
- Verified live: Father Ocean → 122 BPM (correct), 925 beats / 232 bars / 29 phrases, full section map, 11 vocal regions.

## As-built (M3 — the basic mix)

The first mix implements the one principle literally: **the brain plans a `MixPlan`, the engine executes it; the LLM never touches audio.** Four small, independently-tested units:

- **The fence** (`app/planner/fence.py`, deterministic): reads both songs' `TrackAnalysis` and returns the legal, safe options — phrase-aligned high-energy drop points (with a runway check), a safe tempo stretch (`best_stretch` folds half/double-time and holds ±11% — widened from ±8% on 2026-07-07 to fit the curated catalog's key-perfect but tempo-borderline vocals), a capped vocal slice snapped to a downbeat (falling back to the song's own sung sections when vocal-region detection is empty), and Camelot key-fit (informational). Falls back down the ladder (phrase→downbeat→beat) when data is shaky, and declines a pair in plain language when tempos are too far apart or there's no beat. Pure arithmetic — no AI, no audio.
- **The AI driver** (`app/planner/plan.py`): Claude (`claude-sonnet-5`) picks the drop + a one-bar beat-breath among the fence's options; on any failure (no `ANTHROPIC_API_KEY`, network, bad output, out-of-range index) it falls back to the deterministic "best drop" pick, so a mix never blocks on the AI. Raises `MixDeclined` (with a reason) when the pair is unmixable.
- **The referee** (`app/planner/validate.py`, dangerous surface): `validate_plan` (R3 vocal on a downbeat, B3 safe stretch, non-empty slice) and `validate_render` (R6 no clip ≥0.999, and **not silent or near-silent** — at least 2% of samples above an audible floor, so a mostly-silent render with a stray blip is caught, not just exact-zero). R1 single-vocal / R2 single-bassline are guaranteed **by construction** — the bed is only S1's drums+bass+other and the only vocal added is S2's single slice. `assert_*` raise `ValidationError`.
- **The engine** (`workers/render.py`, dangerous surface): FFmpeg + numpy only, decoupled (takes plain file paths, no app/db coupling — imported by the API via a `sys.path` bootstrap). Sums S1 drums+bass+other into the bed; slices S2's vocal then `atempo`-stretches it to S1's tempo (pitch preserved); fades both edges (~8 ms) to kill clicks; optional one-bar beat-breath **ducks** the bed (to 35%, never silence) before entry; peak-normalizes to −1 dBFS with a 0.999 brickwall so the master never clips. Renders 44.1 kHz stereo WAV. Guards: decoded audio is capped at 12 min so a tiny-but-hours-long low-bitrate file can't balloon memory (the upload cap is on bytes, not duration); non-positive tempo is rejected and a negative anchor is clamped.

Route (`app/routes/mix.py`, async — mirrors stems/analysis): `POST /mix {song1_id, song2_id, prompt?}` starts a background job and returns `202` (or the cached result); `GET /mix/{mix_id}` polls; `GET /mix/{mix_id}/audio` serves the WAV. `mix_id` = SHA-256 of `(song1, song2, prompt)` → identical requests are free; preconditions (both uploaded + analyzed + split) are reported in plain language (409). Models: `MixPlan` (the recipe) and `Mix` (the job/result). Web: a `MixMaker` card (`apps/web/src/components/Mix/`) → poll → play + download + DJ-language note.

**Time-stretch:** FFmpeg `atempo` (pitch-preserved, LGPL-core, already installed) — the as-built realization of the spec's SoundTouch choice, kept small (±11%). GPL `librubberband` exists in the dev FFmpeg but the pipeline does not depend on it.

## As-built (M4 Slice A — the living arrangement + regenerate)

M3's single drop grows into a full arrangement; the brain still plans a `MixPlan`, the engine executes it, the LLM never touches audio.

- **Models** (`app/models.py`): `Placement(anchor, vocal_src, beat_breath)`; `MixPlan.placements: list[Placement]` + `take: int` — **additive** (the scalar `anchor`/`vocal_src` stay, so M3-era cached `*.mixplan.json` still parse).
- **The fence** (`planner/fence.py`): `arrangement_options` returns the M3 legal set plus ranked phrase anchors and the available vocal slices; `rendered_vocal_secs`/`placement_end` are the **single source of truth** for a placed vocal's real length (`source / stretch` — see the atempo note).
- **The driver** (`planner/plan.py`): Claude arranges 2–3 non-overlapping placements building an energy arc, with a deterministic fallback; `take` rotates the arrangement for Regenerate; `_dedupe_nonoverlapping` guards one-vocal-at-a-time before render. AI slices re-clamped to `MAX_VOCAL_SECS`.
- **The referee** (`planner/validate.py`, dangerous): now checks **no overlap across placements** (R1, via the shared `placement_end`) + each entry on a downbeat (R3), plus M3's B3/R6. R1 source-single holds by construction.
- **The engine** (`workers/render.py`, dangerous): loops placements, laying each vocal on the continuous bed; `beat_breath` **ducks the bed to 35%** for one bar (never silence — the M3 dead-air fix stays). Windows temp-cleanup made race-safe.
- **Route** (`routes/mix.py`): `/mix` takes an optional `take`; `mix_id` folds in `take` + `ENGINE_VERSION` (`m4a.2`) so each take is a distinct cached render. **Web:** a two-lane arrangement timeline + "Give me another take" + Download.

**The atempo length contract (a fixed bug worth remembering):** FFmpeg `atempo=stretch` outputs `source_duration / stretch`, **not** `* stretch`. For `stretch < 1` (a faster vocal, ~half of compatible pairs) the vocal plays _longer_; an early M4 cut used the inverted `* stretch` in the driver and referee, so vocals could overlap (two voices) past the checks. Caught by the adversarial review; fixed by routing both through `fence.placement_end`.

## As-built (M4 Slice B — contrast + subtle FX + confidence fallbacks)

Additive on Slice A; the two-voices rule now spans both songs.

- **Models:** `Placement.fx: str | None` (`"sweep_in"`), `MixPlan.s1_vocal_regions: list[tuple[float,float]]` — both additive (M3/M4a plans still parse). `fx` is left a bare string on purpose (an enum would make cached plans more brittle); the referee typo-guards it instead.
- **Fence:** `contrast_windows` — the beat-only gaps between Song-2 placements (via the shared `placement_end`) intersected with where Song 1 itself sings, margin-protected (2 s) so a contrast never touches a Song-2 vocal or the next duck/sweep bar.
- **Driver** (`_apply_flourishes`, `_confident`): on a confident Song 1 → one contrast window + one `sweep_in` on the final entry; on a shaky Song 1 (`bpm_confidence < 0.5`) → play safe (≤2 placements, no contrast/fx/breath). The AI's picks are stripped/gated after it runs, so a misbehaving model can't force fancy moves on bad data.
- **Referee** (dangerous): R1 now holds **across both songs** — every `s1_vocal_regions` span is checked against every Song-2 placement window (`placement_end`); overlap → violation. Also typo-guards unknown `fx`. Verified by an adversarial review that could not defeat the two-voices guarantee (straddles, sub-unity stretch, unsorted plans, evil-AI injection).
- **Engine** (dangerous): applies `_sweep_bed` (a rising low-pass crossfade, eased at the leading edge so it can't click; overshoot folded into the −1 dBFS normalize + clip guard) before a `sweep_in` entry; mixes Song 1's own vocal stem into each contrast span at ratio 1.0 (it is already Song 1's tempo). Degrades gracefully if the Song-1 vocal stem is missing.
- **Route:** passes Song 1's `vocals` stem; `ENGINE_VERSION m4b.1`. **Web:** the timeline shows Song 1's vocal in a distinct color + a sweep mark, with a legend.

## As-built (M4 Slice C — the energy arc: vocal spans the whole song)

The prior arrangement ranked anchors by **energy only**, so on a long song (e.g. a real 7:52 pair) the 2–3 placements clustered where the song is loudest (the middle), leaving the first half and the ending empty. This layer makes the arrangement follow an energy **arc** across the whole song — straight from the DJ Handbook (H1 shape-an-arc, H2 think-in-thirds, H5 don't-peak-early, H6 waves). Same placement count (no render-cost change); only _where_ they land moves. Purely in the planning code — the referee and engine are untouched.

- **Fence** (`planner/fence.py`): `arc_anchors(anchors_ranked, track_end, count, take)` — splits the timeline into `count` equal bands and takes the best-energy anchor in each, so one vocal moment lands in every third and the final band gives a strong late entry; `take` rotates the within-band pick for Regenerate variety; falls back to what exists when anchors are too few. `arrangement_options` now also returns `track_end` (the whole canvas) and `sections` (Song 1's shape, for the AI).
- **Driver** (`planner/plan.py`): `_default_arrangement` selects via `arc_anchors` (spread) instead of a sliding window of consecutive anchors. A new **arc guard** — `_spans_song` — rebuilds any plan (a clustering AI, or thin analysis) as the deterministic arc when it fails to reach both the first half and the final third; the guard makes the founder's acceptance test true **by construction**, regardless of how the plan was produced. The AI prompt + payload now include `track_length_seconds`, `song1_sections`, and a `recommended_spread_anchors`, and instruct it to spread across the thirds with a strong finish.
- **Route** (`routes/mix.py`): `ENGINE_VERSION m4c.1` so cached clustered mixes aren't re-served.
- **Verified end-to-end (no cloud cost)** on the real cached 122-BPM 7:52 beat + 125-BPM vocal: Take 1 vocal at 1:52 / 5:01 / 5:32 (+ Song 1's own vocal answering at 2:41, sweep on the last entry); Take 2 at 1:36 / 4:29 / 6:51 — both span first-half→final-third, takes differ, render passed the referee. 96 backend tests (was 91) + 10 web green.
- **Known limit (deliberate, next layer):** still 2–3 vocal moments, so a very long song can feel a touch sparse between entries. Adding _more_ vocal returns touches `workers/render.py` (a dangerous surface) and is the explicitly-deferred next layer, gated on the founder finding it too sparse by ear.

## As-built (M4 Slice D — per-bar beat-lock: the vocal stops drifting)

Slice C spread the vocal across the whole song, which exposed a pre-existing drift: a single global `atempo` ratio can't hold a long vocal on the beat, because real tempos wobble and BPM detection is imperfect. Measured on the real 122/125 BPM 7:52 pair, a 40 s vocal drifted **+315 ms** off the beat by its end. Slice D re-locks every bar.

- **Fence** (`planner/fence.py`): `warp_map(anchor, vocal_src, a1_downbeats, a2_downbeats, global_ratio)` maps each Song-2 bar in the slice onto the matching Song-1 bar and emits `(src_start, src_end, out_secs)` per bar, where `out_secs` is the Song-1 bar length — so every bar boundary lands on a Song-1 downbeat and drift resets each bar (~0 ms). It **starts on Song 2's first downbeat** in the slice (drops any mid-bar lead-in rather than emit a grid-shifting leading partial), keeps a trailing partial for the vocal's own tail, and **returns `[]` (legacy global stretch) on a genuine grid glitch** (a bar whose ratio is out of the safe band) rather than a half-locked map. `placement_end(..., warp=None)` returns `anchor + sum(out_secs)` when warped, so the driver and referee still measure a placed vocal's real end identically.
- **Models** (`app/models.py`): `Placement.warp: list[tuple] = []` — additive; empty ⇒ the legacy single-atempo path (M3/M4a–c cached plans still parse and render unchanged).
- **Driver** (`planner/plan.py`): `_attach_warp` gives each Song-2 placement a warp map; dedupe + contrast use the warp-aware `placement_end`.
- **Engine** (`workers/render.py`, dangerous): `_vocal_take_warped` stretches each bar to its `out_secs` and lays the bars at their locked offsets, joined by short equal-power crossfades (each bar rendered a hair long so the seam crossfades without moving the downbeat). Raises `RenderError` if a bar ratio is ever outside atempo's [0.5, 2.0] (defense-in-depth; unreachable for real plans). Legacy `warp=[]` path is byte-identical to before.
- **Referee** (`planner/validate.py`, dangerous): R1 no-overlap (both S2↔S2 and S1-contrast↔S2) now measures with the warp-aware `placement_end`; new **R7** — a warped placement's per-bar ratios must be in the per-bar **grip band** and its interior bar boundaries must land on Song-1 downbeats. `assert_plan` runs before render.
  - **Grip band widened (2026-07-08):** R7's per-bar ratio band was == the global `SAFE_STRETCH` band, so a vocal whose overall stretch sits near an edge (Tere Bina 0.89, Der Lagi 1.099) had bars straddling the edge → `warp_map` bailed to `[]` → a single global stretch that **drifts off the beat** over a long placement (founder heard it at ~4:18 on the real Tere Bina). Fix: a wider per-bar grip band `fence.WARP_LO=0.85`/`WARP_HI=1.15` (a per-bar re-lock is a _transient_ single-bar correction; the global band that decides which pairs mix / warble is **unchanged**), used by both `warp_map` and R7. The boundary-on-grid anti-drift check and R1/R2/R3/R6 are untouched (adversarially reviewed `safe`). Result: Tere Bina 0/3→3/3, Der Lagi 1/3→3/3, Tujhe 2/3→3/3 beat-locked. Residual: an occasional edge bar stretches ~15% for ~1.7s (transient, ear-gated) — far better than sustained drift.
- **Route** (`routes/mix.py`): `ENGINE_VERSION m4d.1`.
- **Reviews:** independent test-author (21 acceptance tests, hollow-test-hardened), + a 3-lens adversarial/scalability quorum. Scalability **ship**; DSP **safe**; the safety-guarantee lens **caught a real must-fix** (R7 falsely rejected ~29% of mid-bar-start plans by shifting the grid on a leading partial) — **fixed** (start-on-downbeat + glitch→`[]`) and the re-review confirmed **safe** (29% → 0.2%, 0 overlaps). **Verified end-to-end (no cloud cost)** on the real 7:52 pair: boundary drift **315/236/180 ms → 0/0/0 ms**, R7+R1+R6 pass, valid render. 127 backend + 10 web tests green.
- **Known residual (logged, low-severity, non-blocking):** a ~0.2% _loud_ R7 decline on band-edge pairs — `warp_map` band-checks the unrounded `out_secs` while R7 recomputes from the 4-dp-rounded value, so a trailing partial at the ~±8% edge can tip just past R7's tolerance. Never a silent bad mix or overlap; the user regenerates. Fix when next in `validate.py`: give R7's band check a rounding-aware tolerance (~2–5e-3), or have `warp_map` drop a trailing partial whose stored ratio would fail R7.
- **Perf follow-up (logged):** per-bar rendering spawns ~1 FFmpeg process per bar (~60 for a 7-min mix, ~+10 s, invisible behind stem separation at validation scale). Collapse to a single per-placement filtergraph when next in `render.py` — bundle with the mix-WAV eviction work (both share "Regenerate is the multiplier").
- **AI-path nicety (logged):** `_ai_arrange` doesn't snap a slice start to a Song-2 downbeat, so an AI placement drops up to ~1 bar of lead-in (warp_map starts on the downbeat). Correct and on-beat, just slightly less vocal; snap `s0` when convenient.

## As-built (M5 Slice 1 — live control foundation)

The first "steer the mix with words" slice. The M4 mix is a baked WAV (can't un-mute a part), so live control plays the **separate stems** in the browser and rides one part's gain on the beat. LLM plans (a `LiveOp`); the browser executes; nothing off-grid; the music never stops.

- **Models** (`app/models.py`): `LiveOp` (additive) — `op` ("mute" | "unmute" | "decline"), `target`, `when="next_bar"`, `say` (DJ-language reply), `reason`.
- **Live driver** (`app/planner/live.py`, not dangerous): `parse_command(text) -> LiveOp` — deterministic keyword parser for the lean set (Slice 1: bass mute/unmute) + an out-of-scope decline. An LLM path will sit in front later with this as the fallback (same pattern as `planner.plan`). Never touches audio.
- **Routes** (`app/routes/live.py`, not dangerous): `POST /live/command {song1_id, song2_id, text}` → `LiveOp`; `GET /live/context/{song1_id}` → `{bpm, downbeats}` (from the cached analysis — the grid the browser schedules on). Stateless; hex-id guarded.
- **Web — pure logic** (`apps/web/src/lib/liveSchedule.ts`, unit-tested): `barSeconds`, `nextBarTime(downbeats, songTime, bpm)` (next real downbeat, else next bar on the bpm grid), `applyOp` (bus on/off reducer), `rampTarget`. All the scheduling decisions live here so they're testable without Web Audio.
- **Web — audio engine** (`apps/web/src/lib/liveAudio.ts`, ear-verified not unit-tested): `LivePlayer` — decodes Song 1's `drums/bass/other` stems, one `GainNode` per bus, starts all sources at the same `AudioContext` time (sample-accurate sync), and `schedule(op, ctx)` ramps the target bus over one bar starting on the next downbeat. `new AudioContext()` is wrapped in try/catch so an unsupported environment degrades to "not ready" rather than crashing.
- **Web — screen** (`apps/web/src/components/Live/LiveMix.tsx`): play/pause, a command text box, a DJ-language status line, per-bus on/off indicators. Wired **additively** into `Uploader.tsx` alongside the M4 `MixMaker` (same both-songs-ready gate); the M4 finished-mix player is untouched.
- **Tests:** `test_live.py`, `test_live_route.py` (pytest); `api.test.ts`, `liveSchedule.test.ts`, `LiveMix.test.tsx` (vitest). Raw Web Audio playback is verified by ear (founder acceptance), not in jsdom. 136 backend + 18 web green.
- **Stack note:** uses the **raw Web Audio API** — Tone.js/wavesurfer named in the stack table are NOT installed and were not needed for this slice.
- **Danger-surface note:** the web `*.test.ts`/`*.test.tsx` files ARE on the dangerous list (the test-harness guard) — adding new web tests goes through confirm-and-apply. (Python `test_*.py` are not guarded; only `conftest.py`/`pytest.ini`.)

## As-built (M5 Slice 2 — the full mix, live, all parts controllable)

Slice 1 played only Song 1's stems. Slice 2 layers in **Song 2's arranged vocal** and makes **every** part switchable on the beat — so the live player plays the _whole_ mix. The key move: don't rebuild the M4 arrangement in the browser — have the trusted engine bake the vocal layer onto silence and let the browser play it as a fourth bus.

- **Arranged-vocal bus** (`workers/live_stems.py`, new — NOT a dangerous surface): `render_vocal_bus(plan, song1_stems, song2_vocal, out_path)` renders the arrangement's vocal layer (Song 2's placed/warped/edge-faded vocal + Song 1's contrast regions) onto a **silent** buffer, by **importing** `render.py`'s helpers (`_vocal_take`, `_vocal_take_warped`, `_edge_fade`, `_placements_of`, `SR`, `_CEILING`). It skips the bed sum, the master peak-normalize, and the bed-only effects (sweep, beat-breath). Vocal added at **ratio 1.0, not normalized** — the browser sums it live with the raw stems, so the vocal-vs-bed balance matches the Download (whose global normalize is uniform); only a safety clip is applied. **`render.py`/`validate.py` are untouched.**
- **Route** (`app/routes/live.py`, additive): `GET /live/vocal-bus/{mix_id}` → 200 audio when ready, 202 while rendering (async start-then-poll, mirrors `mix.py`'s `_jobs`), 409 if no cached mix plan, 404 on a bad id. Derives everything from the cached `MixPlan` JSON (which carries `song1_id`/`song2_id`) — no re-planning, no AI. Cached as `{mix_id}.livearr.wav` (the arranged, beat-locked vocal — same as the Download; renamed from the earlier `.vocalbus`/`.livevocal`, see the M5 note below).
- **Models** (`app/models.py`, additive): `LiveOp.targets: list[str]` — a command can name several buses at once ("drop everything but the beat"). `target` stays (Slice-1 back-compat; set when a single part).
- **Live driver** (`app/planner/live.py`): `parse_command` grows to the full lean set — remove/bring-back vocals, drums, melody (`other`); the combos "drop everything but the beat" (mute bass+other+vocals) and "bring it all back" (unmute all four); bass phrases unchanged. Order: combos → per-part unmute-before-mute → bass fallback. Still deterministic.
- **Web — pure logic** (`liveSchedule.ts`): `BusName` gains `"vocals"`; `busesOf(op)` returns the affected buses (prefers `targets`, else `target`, filters to valid names); `applyOp` flips every named bus. `barSeconds`/`nextBarTime`/`rampTarget` unchanged.
- **Web — audio engine** (`liveAudio.ts`): `load(song1Id, stemBuses, mixId?)` also fetches + decodes the vocal bus (via `fetchVocalBus`, which polls past 202) into a fourth bus when a mix exists; `schedule` computes the next-bar time once and ramps **every** bus in `busesOf(op)`. Sync-start unchanged (all four buses start at one ctx time).
- **Web — screen** (`LiveMix.tsx`): four **tappable** parts — **Beat**/Bass/**Melody**/Vocals (friendly labels over `drums`/`bass`/`other`/`vocals`). Tapping builds a local op and rides the same on-beat ramp (no server round-trip); typed commands still POST to the parser. Takes an optional `mixId`; the player **reloads** the vocal bus when the song or the take changes (Regenerate → live vocals follow). Vocals is disabled with a hint until a mix exists. `MixMaker` gained an optional `onMixReady(mix_id)`; `Uploader` lifts `mixId` and threads it to both.
- **The live player is a steerable _approximation_** (founder-approved): steady-gain parts, so it omits the Download-only sweep + beat-breath (bed effects). The **Download stays the polished master**; the live player is the steering room.
- **Tests:** `test_live_stems.py` (vocal-bus silent-outside/loud-inside, contrast region, no-placement silence), `test_live_route.py` (+vocal-bus 404/409/200), `test_live.py` (+multi-part/combo parsing); `api.test.ts` (+`fetchVocalBus` polling), `liveSchedule.test.ts` (+multi-target `applyOp`), `LiveMix.test.tsx` (four parts render). **149 backend + 22 web green.**
- **Danger-surface note:** only the web `*.test.ts`/`*.test.tsx` files were on the dangerous list (test-harness guard) — added via confirm-and-apply. `render.py`/`validate.py`/`storage.py`/`songs.py`/config untouched.

## As-built (M5 Slice 3 — AI smart-suggestion chips + "fade away")

As the mix plays, the live player surfaces 1–3 **context-aware suggestion chips** that change with Song 1's section timeline; tapping one applies the move on the beat. Same brain pattern as the arranger (LLM-plans / rules-fallback), now for the live moment — and it can only ever pick moves the engine already executes.

- **Suggestion brain** (`app/planner/suggest.py`, new — not dangerous): `suggest_moves(a1, prompt="") -> list[SectionSuggestions]`. For each of Song 1's sections it emits 1–3 chips from a **closed vocabulary** (`_VOCAB`: Bring the vocal in / Take the vocal out / Take the bass out / Drop to just the beat / Bring it all back / Fade it out → each a concrete `op`+`targets`). `_ai_suggest` makes one `claude-sonnet-5` call (JSON-only, menu-constrained) returning `{section_index: [text,…]}`; unknown texts are dropped; any failure/no-key → a deterministic label→chips fallback. Never touches audio. Mirrors `plan.py`.
- **Models** (`app/models.py`, additive): `LiveChip{text, op, targets}`, `SectionSuggestions{start, end, label, chips}`. `LiveOp.op` gains the value `"fade"`.
- **Route** (`app/routes/live.py`, additive): `GET /live/suggestions/{mix_id}` — hex-guarded; loads the cached `MixPlan` (→ `song1_id`) + Song 1's analysis; calls `suggest_moves`; caches to `{mix_id}.suggestions.json`; returns `{sections:[…]}`. **Synchronous** (one fast, fallback-safe call — deliberately not a 5th async-job copy). 404 bad id, 409 if the plan/analysis isn't ready.
- **The "fade away" move** — `parse_command` maps "fade away"/"fade it out"/… → `LiveOp(op="fade", targets=[all four])`; the browser ramps every bus to 0 over `FADE_BARS` (4) bars (a longer ramp than a 1-bar mute). Music keeps running silently (invariant); "bring it all back" restores it.
- **Web — pure logic** (`liveSchedule.ts`): `applyOp` treats `"fade"` as all-named-buses-off; `currentChips(sections, songTime)` picks the section the playhead is in; `Chip`/`Section` types.
- **Web — player** (`liveAudio.ts`): `schedule` handles `"fade"` (target 0, `FADE_BARS`-bar ramp) alongside mute/unmute, on one shared bar boundary. `api.ts`: `LiveChipDTO`/`SectionSuggestionsDTO` + `getSuggestions(mixId)`; `LiveOpDTO.op` widened to include `"fade"`.
- **Web — screen** (`LiveMix.tsx`): on a mix, `getSuggestions`; a ~250 ms playhead poll (only while playing) swaps to the current section's chips; chips render as a **swappable row** (built for the later UI design pass) that applies the chip's op through the shared on-beat `runOp`. Fetch failure → chips just don't show; parts + typed commands still work.
- **User stays in control:** chips only suggest; the four part controls + typed commands are unchanged. Chips are per-section (not live-mute-state-aware) in V1 — tapping an already-active move is a gentle no-op.
- **Tests:** `test_suggest.py` (fallback label→chips, no-sections default, AI-picked + unknown-dropped), `test_live_route.py` (+suggestions 404/409/200 with vocabulary chips), `test_live.py` (+"fade away"); `liveSchedule.test.ts` (+fade `applyOp`, `currentChips`), `api.test.ts` (+`getSuggestions`). **157 backend + 26 web green.** `LiveMix.test.tsx` unchanged (four-parts regression; chip logic covered in `liveSchedule`, raw-audio UI ear-verified).
- **Danger-surface note:** `render.py`/`validate.py`/`live_stems.py`/`storage.py`/`songs.py`/config untouched; only `liveSchedule.test.ts` + `api.test.ts` (test-harness guard) went through confirm-and-apply.

### Slice 3 fixes (founder ear-test)

- **The AI was silently never running.** Newer models return a _thinking_ block first, so `msg.content[0].text` raised AttributeError, swallowed by the broad `except` → both `plan.py` (arrangement) and `suggest.py` (suggestions) always fell back to rules. Fix: `app/planner/llm.py` (`MODEL`, `first_text` skips non-text blocks, `extract_json`), model `claude-sonnet-4-5`; both planners use it. AI now genuinely drives arrangements + suggestions (rules remain the fallback). Regenerate to get an AI mix (old caches are rules-made).
- **Live Vocals = the arranged, beat-locked bus (`render_vocal_bus`).** History: the live Vocals part briefly became a _continuous_ whole-vocal bus (`render_full_vocal`, one global stretch from t=0) so "bring the vocal in" worked anywhere; cache `.vocalbus`→`.livevocal`. **⟳ REVERSED 2026-07-06 (post-redesign ear-test):** that continuous vocal wandered off-beat and never rested, so the live route was switched **back** to the **arranged `render_vocal_bus`** (the same per-bar beat-locked vocal as the Download) — cache `.livevocal`→`.livearr` — so Play matches the finished mix. `render_full_vocal` sat unused after that and was **removed 2026-07-07** in the mixing-pipeline hygiene sweep.
- **Chips no longer feel static.** `suggest_moves` merges consecutive same-chip sections; `LiveMix` shows a "now playing: <part> · m:ss" indicator (from `currentSection` + the live playhead) so the user sees which part drives the current chips.

## As-built (one-click "Studying your songs" flow)

Removes the manual per-song **Split** + **Analyze** buttons: the user drops two songs, presses **Make my mix** once, watches an honest progress checklist, and lands on the finished mix. Frontend-only orchestration over the existing endpoints — **no backend change, no dangerous code surface** (only the web `*.test.*` files, via confirm-and-apply).

- **Shared job-poll helpers** (`apps/web/src/lib/api.ts`, additive): `splitSong`, `analyzeSong`, `makeMix` each do the start-then-poll dance (POST, then poll GET to `ready`/`error`/timeout) with an overridable `PollOpts {pollMs, maxTries}` so tests poll fast. These **de-duplicate** the three copy-pasted polling loops that lived in `Uploader`'s `SongCard` and `MixMaker`.
- **Orchestrator** (`apps/web/src/lib/study.ts`, new, unit-tested): `studyAndMix(song1Id, song2Id, onStage, prompt?, opts?)` runs **split both → analyze both → plan the mix**, emitting `StudyStage` updates ("splitting" → "analyzing" → "planning" → "done") for the progress screen. **Order is load-bearing:** every song is split _before_ it is analyzed, because `analysis.py` reads the split vocal stem to find the vocal regions (splitting after, or not at all, yields a weak read and an over-cautious plan — the old manual flow allowed analyzing-without-splitting; this closes that). The two songs are prepared in parallel (independent cloud jobs; no local load). `STUDY_STEPS` exports the labelled checklist.
- **Uploader** (`apps/web/src/components/Uploader/Uploader.tsx`): now a three-phase screen — **idle** (two drop zones + one **Make my mix** button) → **studying** (`StudyingScreen`: a ticking checklist; on failure, a plain message + **Start over**) → **mix** (the existing `MixMaker` + `LiveMix`). The per-song stem players and BPM/key/timeline insight cards (M2 dev affordances) are **removed** from the default flow — the casual-creator path is upload → mix → steer. The prompt sent to the mix stays `""` (unchanged cache key), so the cached demo pair is still instant.
- **MixMaker** (`apps/web/src/components/Mix/Mix.tsx`): gained an optional `initialMix` prop — when the studying flow hands in an already-rendered mix it shows the result immediately (no re-request); Regenerate still makes fresh takes via the shared `makeMix`.
- **Tests:** `study.test.ts` (stage order; split-before-analyze), `api.test.ts` (+`splitSong`/`analyzeSong`/`makeMix` poll + error), `Uploader.test.tsx` (rewritten: hands-free happy path lands on the mix with no Split/Analyze buttons; step-failure → error + Start over; upload-failure message), `Mix.test.tsx` (+`initialMix` renders without a network call). **162 backend + 32 web green.**
- **Danger-surface note:** only the web `*.test.ts`/`*.test.tsx` files (test-harness guard) went through confirm-and-apply. No backend/engine/storage/route change.

## As-built ("Beat up" — the last live energy move)

Completes the V1 live command set. Founder-chosen sound: **"the beat takes over"** — melody + vocals duck so drums + bass drive; no tempo change (that's V2), so the energy comes from ducking to the groove. Gain-only, clip-safe (never boosts above 1), reversible with "bring it all back".

- **Model** (`app/models.py`): no change — `LiveOp.op` is a free `str`, so `"beat_up"` is carried as-is.
- **Live driver** (`app/planner/live.py`): `_BEAT_UP` phrases ("beat up", "energy up", "pump it up", "amp it up", …) → `LiveOp(op="beat_up", targets=["other","vocals"], say="letting the beat take over…")`. Checked right after fade, before the combos, so "drop everything but the beat" still parses as a mute despite the word "beat".
- **Web — pure logic** (`liveSchedule.ts`): `applyOp` treats `"beat_up"` specially — every bus reads **on** (nothing is muted, the tops only duck), so the tap toggles reflect what's audible.
- **Web — audio engine** (`liveAudio.ts`): `schedule` handles `"beat_up"` — ramps every bus over one bar on the next downbeat to `BEAT_UP_LEVELS` (drums 1, bass 1, melody/vocals `BEAT_UP_DUCK` = 0.4). Only reduces gains → no added clipping.
- **Web — screen** (`LiveMix.tsx`): a **"Beat up"** button (`beatUp()` builds the op locally — no server round-trip) beside the four parts; typed "beat up" still routes through the parser. `api.ts` `LiveOpDTO.op` widened to include `"beat_up"`.
- **Tests:** `test_live.py` (+beat_up parse, synonyms, not-confused-with-the-combo); `liveSchedule.test.ts` (+beat_up → all buses on); `LiveMix.test.tsx` (+Beat up control renders). **168 backend + 33 web green.** The actual gain ducking is ear-verified (Web Audio isn't exercised in jsdom, consistent with the other live moves).
- **Danger-surface note:** only the web `*.test.ts`/`*.test.tsx` files (test-harness guard) went through confirm-and-apply; no engine/validator/storage/route change.

## As-built (UI redesign — "Electric Violet" + AI mix names)

The single long page became **four state-driven screens** in the founder's approved design. All the working logic (upload, study orchestration, live player, mix, regenerate, download, commands, chips, beat up) was preserved over the **unchanged `lib/` layer** (`api.ts`, `study.ts`, `liveAudio.ts`, `liveSchedule.ts`); only the presentation changed.

- **Shell + state** (`App.tsx`, `components/shell/Frame.tsx`): `App` is the state machine (`screen`, files, songs, mix, mixId, mixName, stage, regenerating). `Frame` renders the persistent 1280×800 frame (scaled to fit via a `--s` transform, never upscaled), the top bar (wordmark + a passive Setup/Generating/Play/Export progress nav), and the 360px console / stage body. Screens are `setup | generating | play | export` (`types.ts`), advanced by real state — not free tabs. **Responsive:** above 900px this framed/scaled desktop design is intact; **below 900px** (phone + tablet-portrait) a media block in `index.css` drops the fixed scaled canvas (`transform: none`, real pixels), lets the page scroll (`100dvh`, `overscroll-behavior` contain), and stacks the console over the stage full-width — with ≥44px touch targets, 16px inputs (no iOS zoom-on-focus), and, on Play, a reorder (stage/title/transport lead; the chat feed drops last) keyed off `data-screen` on `.body`. No horizontal scroll at 375/768.
- **Design tokens** (`index.css`): two-hue system — grey (`--page/--frame/--panel/--track`) + one violet (`--violet #8f00ff`, active/live/actionable only) + a white-alpha scale; Instrument Serif / Space Mono / system fonts (loaded via a Google Fonts `<link>` in `index.html`). Shared layout classes (`.frame/.topbar/.console/.stage/.kicker/.title/.btnPrimary`) are global; each screen adds a CSS module.
- **Screens** (`components/screens/`): **SetupScreen** (two upload cards + prompt + "Mix it" → stage tiles + tagline); **GeneratingScreen** (the real `STUDY_STEPS` checklist + pulsing ring + progress from the study stage); **PlayScreen** (absorbs the old LiveMix + arrangement view: a command **feed**, a **"＋ Next song"** button (back to Setup with cleared drop zones for a new pair), the four **part toggles**, **Beat up**, per-section **suggestion chips**, command input; stage = BEAT/VOX **arrangement lanes** derived from the plan, a **transport** with play/pause + a **click/drag-seekable** scrub (draggable knob + arrow-key ±5s; `LivePlayer.seek()`/`offset`, and pause/resume now resumes from position) + time + **"another take"** (regenerate, in place) + export, and a blinking **LIVE** dot; the live vocal is the arranged beat-locked bus, so Play sounds like the Download); **ExportScreen** (download the full mix now; the 14s clip + share links are flagged coming/M6). The retired `Uploader`/`Mix`/`LiveMix` components + their CSS/tests were deleted.
- **AI mix name** (`app/planner/name.py`, `POST /mix/name`): Claude coins a short playful mashup name from the two upload filenames (song 1 = beat, song 2 = vocals) — e.g. "Ocean Bina" — with a deterministic `_clean_title` + "A × B" fallback (no key / any failure). Cached by a hash of the two names + prompt to `{hash}.mixname.txt` (joins the cache-eviction backlog). The web `getMixName` fetches it after a mix; `App` shows it in place of "Untitled Mix". Mirrors the LLM-plans / rules-fallback shape of `plan.py`/`suggest.py`. Route lives in `mix.py` (not a dangerous surface); `name.py` reuses the shared `llm.py`.
- **Tests:** `App.test.tsx` (Mix-it-disabled gate; upload→generating→play lands on the mix with its AI name + parts + Beat up), `PlayScreen.test.tsx`, `ExportScreen.test.tsx` (name + download + clip-soon), `api.test.ts` (+`getMixName`); backend `test_name.py` (clean-title, fallback, no-key, route). **172 backend + 30 web green.** Web Audio playback stays ear-verified (jsdom degrades to not-ready). **Browser-verified:** Setup renders in the exact design (button `#8f00ff`, title Instrument Serif) with zero console errors; live `/mix/name` returned "Ocean Bina" for Father Ocean + Tere Bina.
- **Danger-surface note:** only the web `*.test.*` files (test-harness guard) went through confirm-and-apply. No engine/validator/storage/songs/config change.

## As-built (curated song catalog — the MVP pivot away from uploads)

Users pick both songs from a **preloaded, hand-verified catalog** instead of uploading (founder decision after repeated misread-BPM declines; research confirmed aggregator BPM data and our analyzer both mis-count non-4/4-pop grooves). Any catalog pairing blends by construction because we verify each song's tempo once.

- **Catalog** (`services/api/app/routes/library.py`, new — read-only, not a dangerous surface): `GET /library` reads `data/library/manifest.json` — a list of `{name, song_id, role_hint}` entries pointing at **already-ingested** songs — and returns Song-shaped records (`id`, `original_name`, `url`, `status:"ready"`, `role_hint`). Entries with malformed/non-hex ids or missing audio are silently skipped; a corrupt manifest degrades to an empty catalog (never a 500). Because entries reference existing song ids, picking a catalog song **reuses every cache** (stems, analysis, mixes) — zero cloud cost and zero fresh-misread risk.
- **Adding a song (operator flow):** ingest the file once through the normal upload route, verify its analysis (BPM/downbeats — hand-correct the cached JSON if misread), then add a manifest entry. Users never upload.
- **Web:** `api.getLibrary()`; `SetupScreen` rewritten — two slot buttons open a styled dropdown (listbox) of catalog songs; a song picked in one slot is disabled in the other; empty/error states in plain language; **no `<input type=file>` anywhere** (regression-tested). `App` holds `pick1/pick2` (`LibrarySongDTO`) and skips the upload step entirely (`studyAndMix` runs on the picked ids; the "uploading" study stage relabelled "Loading your two songs"). The backend upload route stays (operator ingestion).
- **Seeded manifest:** Father Ocean (Ben Böhmer Remix) `ac59f8c4…` (beat, 122 BPM verified) + Don't Start Now (Dua Lipa) `c0c6ab91…` (vocals, 125-read ≈ documented 124). _Naming note: research showed the real Tere Bina is ~144 BPM (a ~72 ballad counted double) — the 125-BPM vocal previously believed to be Tere Bina is almost certainly Don't Start Now; founder to confirm by ear._
- **Tests:** backend `test_library.py` (empty/missing manifest, hidden-when-audio-missing, malformed + traversal ids skipped, corrupt manifest); web `SetupScreen.test.tsx` (dropdown opens + picks, empty note, failed-load note), `App.test.tsx` rewritten for the catalog flow (+ no-file-inputs regression, + duplicate-pick disabled), `api.test.ts` (+`getLibrary`). **176 backend + 37 web green (213).** Browser-verified against the live backend: catalog lists both songs, picking fills the slots/tiles, duplicate disabled, Mix-it enables.
- **Danger-surface note:** `songs.py`/`storage.py` untouched (only imported); `main.py` registers the router; web `*.test.*` edits via confirm-and-apply.

## As-built (House × Bollywood judgment — Steps 1–2; branch `feat/house-bollywood-energy-sync`, NOT on main)

The genre-specific judgment upgrade (docs/house-bollywood-recipe.md + build-plan). All energy/vocal features are **derived from the already-cached analysis** (no re-analysis, no `analysis.py` change) except the catalog vocal-region repair. `ENGINE_VERSION` walked `m4d.1 → m5g.3`.

- **Energy detection (safe, `fence.py`):** `energy_drops` (low→high onsets into sustained high = the house DROPs), `vocal_peaks` (Song 2's loudest sung slices), `synced_anchors` (drop-preferring energy arc — prefers a real drop per third, falls back to the loudest phrase-anchor so the whole-song span holds).
- **Energy-sync arrangement (safe, `plan.py`):** `_default_arrangement` pairs Song 2's loudest peak onto the strongest drop (rotated by `take`); the AI payload/prompt got `house_drops_seconds` + `vocal_peaks_seconds`.
- **The produced drop (🔒 `render.py`):** `_build_bed` (multi-bar filter-open + volume rise into a drop, clamped off a prior vocal); `_echo` (decaying throw). `plan._produce_drops` marks drop placements with `build_bars` and throws an echo on every drop where the tail is clear. Additive `Placement.build_bars`/`echo`.
- **Both vocals trade (safe, `fence.py`+`plan.py`):** `lead_sections` (Song 1 leads its merged substantial passages in the gaps; scraps dropped) + `predrop_licks` (keeps Song 1's vocal lick into each drop). Replaces the old `contrast_windows`.
- **Natural hand-off (🔒 `validate.py` R1 relaxation + `render.py`):** `fence.LEAD_XFADE_SECS` (1.2s) lets the outgoing vocal ring past the entry; R1 now allows a bounded overlap only when Song 1 started before Song 2 AND ends ≤ the bound past the entry — the outgoing vocal's OWN natural decay is the blend (the engine's imposed fade was removed). **This loosens the app's most important guardrail; see the open safety item in the handoff.**
- **Per-phrase echo throw (🔒 `render.py`):** `_phrase_ends` splits the vocal on its pauses; `_echo` throws after EACH phrase into its pause. The tail past the vocal stays `delay*_ECHO_TAPS` (test-pinned so the R1 echo-tail guard — a plan-side check the referee can't see, since `placement_end` has no echo term — stays valid).
- **Catalog vocal-region repair (data):** 3 catalog vocals were analyzed pre-stem-split → empty `vocal_regions` → crude short slices. Recomputed locally from the present stems (Der Lagi 0→15 regions). Local-only (gitignored `data/`); ingest must split-before-analyze.
- **Tests:** all new logic TDD'd; **212 backend + 39 web green, typecheck clean.** Every `render.py`/`validate.py` edit went through confirm-and-apply. Independent adversarial reviews ran on the produced-drop and the fade-version hand-off (found + fixed 2 must-fixes on the former; no exploit on the latter). **The final no-fade R1 relaxation has NOT had a clean adversarial verdict — required before merge to main.**

### Movable-master tempo (2026-07-08) — unblock far-apart pairs, protect the house

Previously the app locked everything to Song 1's tempo and stretched only the vocal, so a far-apart pair (Father Ocean 122 × Tere Bina ~144 on the grid) needed a ~15% one-sided stretch and was **declined**. The movable master nudges the **whole house bed** to a shared tempo so the vocal's remaining stretch fits the safe band — **house-protective** (founder call): the house moves the MINIMUM, bounded by `fence.HOUSE_SLOW_MAX=0.04`/`HOUSE_SPEED_MAX=0.08` (the guest vocal absorbs the rest and sits at its band edge). Engages **only when needed** — an in-band pair stays native (`bed_stretch == 1.0`, byte-identical).

- **Math (`fence.py`, safe):** `tempo_plan(bpm1, bpm2) -> (master_bpm, bed_stretch, vocal_stretch, safe)` — octave-folds the guest, keeps the house native if the one-sided lock fits, else picks the vocal-legal tempo nearest the house (minimum house move) and declines if that exceeds the house band. `retimed_analysis(a1, target_bpm)` is the **single grid-rescale** (scales every time field by `a1.bpm/target_bpm`) that BOTH the planner and the referee call, so they can never drift onto different grids.
- **Models:** `MixPlan.bed_stretch: float = 1.0` (additive; old plans default to native).
- **Engine (`workers/render.py`, 🔒):** `_atempo_file` pre-stretches each Song-1 stem by `bed_stretch` when it differs from 1.0, then the existing render runs verbatim on the stretched stems (they now sit on the retimed grid the plan's anchors/warp already assume). Engages on the **same `1e-6` gate** the planner/referee use — an adversarial-review must-fix (a looser engine gate left a razor-edge band where the bed stayed unstretched while the plan/referee assumed the retimed grid → an off-beat mix the referee couldn't see). `bed_stretch == 1.0` is byte-identical to before.
- **Referee (`validate.py`, 🔒):** rescales `a1` to `plan.master_bpm` (same 1e-6 gate) before the on-beat/warp checks; adds B3 guards that `bed_stretch` is inside the house band AND that `master_bpm ≈ a1.bpm·bed_stretch` (independence — the referee no longer just trusts the plan tempo it judges). R6 real-audio backstop unchanged.
- **A follow-on fix (`fence.predrop_licks`, safe):** the retimed (non-round) anchors exposed a rounding bug — the Song-1 lick end rounded ~5e-4 past `a+LEAD_XFADE_SECS`, beyond the referee's 1e-6 crossfade tolerance, wrongly declining the pair for a vocal overlap. The lick end is now clamped to the crossfade boundary.
- **`ENGINE_VERSION m5g.3→m5h.1`. Tests:** all TDD (dangerous-file tests authored independently); a 2-lens adversarial quorum found + closed F1/F2. **234 backend + 39 web green, typecheck clean.** Machinery proven on a forced +5% Father Ocean × Tujhe Bhula Diya render; **Tere Bina awaits its audio** (not yet ingested).

### Step 3 Wave 1 (2026-07-08) — auto-performed stem dynamics (the bass pull-and-slam)

The mix engine gained a "mixing board": it can ride ONE of Song 1's bed stems (drums/bass/other) up/down over an on-beat window, so the beat MOVES like a DJ set instead of playing flat under the vocal (recipe §2.6 "PRODUCE, don't assemble"). Wave 1 ships one move — the **bass pull-and-slam**: the bass fades to silent across a produced drop's BUILD window and slams back on the anchor with the vocal. Same architecture — the brain plans, the engine executes, the LLM never touches audio — one additive plan dimension realises all four planned moves.

- **Models (`app/models.py`):** `StemMove(stem, start, end, gain_from=1.0, gain_to=0.0)` and `MixPlan.stem_moves: list[StemMove] = []` — additive; old plans default to `[]` (a flat bed, today's mix). One primitive expresses every move (pull `1→0`, hold-muted `0→0`, duck `1→0.4`, rebuild `0→1`); boosts above `1.0` are out of scope in v1, which keeps clip-safety trivial.
- **The fence (`planner/fence.py`, safe):** `stem_moves_for_drops(placements, a1_downbeats)` emits one `bass` pull (`1.0→0.0`) per placement carrying `build_bars` (a produced drop); the window is `[the downbeat build_bars bars before the anchor, the anchor]` taken from the REAL downbeat grid, so it is on-beat by construction and matches the engine's build window.
- **The driver (`planner/plan.py`, safe):** attaches `stem_moves` after `_produce_drops`, gated on `_confident(a1g)` and computed on the (possibly retimed) grid — so shaky songs stay flat and safe.
- **The engine (`workers/render.py`, 🔒):** each bed stem gets a per-sample **gain envelope** (`_stem_envelope`: a linear ramp inside a move's window, `1.0` outside, plus a ~8ms declick so a `0→1` slam is crisp but never clicks). The three enveloped stems are **summed into the bed FIRST**, then the ORIGINAL render pipeline (build/breath/sweep treatments + in-loop vocal placement) runs on the bed verbatim. This ordering is deliberate: with no `stem_moves` every envelope is all-ones, so the summed bed equals `_sum_stems([drums,bass,other])` exactly and the mix is **byte-identical to the pre-Step-3 engine** — the "old mixes still work" invariant holds by construction (confirmed by an independent re-review: max abs diff 0.000 across multi-placement plans).
- **The referee (`planner/validate.py`, 🔒):** `_stem_move_violations` checks every move rides a real bed stem, lands on Song-1 downbeats (R3), has a forward window, keeps gains in `[0,1]` (no boosts → the master can't be pushed to clip), and — the defense-in-depth guard — never leaves all three bed stems muted at once. The never-all-muted guard is **exact**: `_muted_spans` computes, per stem, the spans where its gain is at/under the audible floor (solving each ramp's single linear crossing) and flags if the three stems' muted-span unions share any instant (no sampling). Overlapping same-stem moves are **rejected** (the engine multiplies them, so their combined duck could hide a hole the min-model can't see — rejecting keeps the math exact); a moves-without-grid plan is flagged too.
- **Route:** `ENGINE_VERSION m5i.1→m5j.0` so no stale cached WAV is served for a plan that now carries moves. No new inputs — moves ride inside the plan; the engine already receives all four Song-1 stems.
- **Heavy-path ceremony (both protected files):** independent test-author wrote the tests first; a **3-lens adversarial quorum + a focused re-review** found and closed three findings — an old-mix-identity ordering regression (fixed → byte-identical), a coarse-sampling gap in the mute guard (fixed → exact interval math), and a min-vs-multiply soundness flaw for overlapping same-stem moves (fixed → overlaps rejected; not reachable by Wave 1 anyway). Founder confirm-and-apply recorded/cleared. **269 backend + 39 web green, typecheck clean.**
- **Verified end-to-end (no cloud cost)** on the real Father Ocean × Der Lagi pair: 3 produced drops (1:03/3:56/6:18), 3 bass pulls, `validate_plan`+`validate_render` CLEAN, peak 0.891 (no clip), the bass slams back ~350× on each drop. **Ear-test note:** the source bass is often already quiet in the pre-drop build, so the _pull_ is subtle — the **slam** carries the effect.
- **Wave 2, first move (2026-07-08) — "drop to just the beat" + a held-silent bass.** Founder feedback on Wave 1's real render: the bass genuinely dipped and slammed back (verified numerically), but the effect read as "just a fade in/out" — the pre-existing multi-bar build (drums+other climbing quiet→loud) is a smooth swell over the same 6–7 seconds, so the bass's sudden snap got buried inside it with nothing to contrast against. **Fix, entirely in `fence.py` (safe surface — `render.py`/`validate.py` UNCHANGED):** `stem_moves_for_drops` reworked to emit a 3-stage tension arc per produced drop, using the SAME `StemMove` primitive with different values: (1) an `other` move mutes the melody for `_CUT_BARS=2` bars right before the build (bass already silent too) — drums alone drive, a real "drop to just the beat" moment; (2) the `bass` move now spans the WHOLE cut+build window with `gain_from=gain_to=0.0` — held flat silent (not a fading ramp), so it's guaranteed inaudible regardless of the source material, rather than depending on the song's own bass being quiet; (3) both snap back together at the anchor via the engine's existing ~8ms declick. Both windows clamp against the previous placement's real vocal end (`fence.placement_end`), same guard the engine's own build clamp already uses. **Why no render.py/validate.py edit was needed:** the engine's per-stem gain envelope and the referee's checks (on-beat, gain range, never-all-muted, no same-stem overlap) were already fully general from Wave 1 — this move is just new _values_ passed into the same, already-reviewed machinery. **Verified on the real Father Ocean × Der Lagi pair:** `validate_plan`/`validate_render` both CLEAN, peak 0.891 (no clip); measured bass low-band energy stays at ~0.0003–0.0008 (near-silent) through the entire 9.8s cut+build, then jumps to 0.24 in the very next 0.1s window at the anchor — a genuine, unambiguous slam rather than a gradual rise. `ENGINE_VERSION m5j.0→m5k.0` (bumped per the fence-rule-change convention even though the two protected files didn't move). 270 backend + 39 web green, typecheck clean.
- **Wave 2, remaining (gated):** beat-up, breakdown — each a `fence` helper placed at a musical moment the app already detects, on the same proven engine; no new engine surgery expected.

## Known follow-ups

- **Re-run the adversarial safety review on the R1 crossfade relaxation** (bounded hand-off overlap, no engine fade) before merging `feat/house-bollywood-energy-sync` to main. See the handoff's open escalations.
- CI `verify` job is Node-only today; add a Python (pytest) job when a GitHub remote is set up. Also point the coverage job at `apps/web/coverage/` (or emit to repo-root `coverage/`).
- Non-Western (Indian/Bollywood/Punjabi) key + structure detection is weaker — hand-verify those demo pairs.
- Before any public exposure (per the M1 security review): sandbox/resource-limit FFmpeg on untrusted input, add rate-limiting + body-size limits at the proxy, and add HTTP-level traversal/oversize tests. **Also lock CORS down** — dev currently trusts any `localhost`/`127.0.0.1` port (`config.allowed_cors_origin_regex`); replace that with the real deployed origin for production.
