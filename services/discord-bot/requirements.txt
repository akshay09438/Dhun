# Prompt-DJ Discord bot — runtime dependencies.
# Voice playback uses PyNaCl (already installed). Pinned <2.6 ON PURPOSE: discord.py 2.6+ moved
# voice to the native 'davey' backend, whose wheel needs a Rust build toolchain this machine lacks
# (the build fails). The 2.5.x line does voice with PyNaCl — no compiler needed. (2026-08-10)
discord.py>=2.4,<2.6
httpx>=0.27,<1
